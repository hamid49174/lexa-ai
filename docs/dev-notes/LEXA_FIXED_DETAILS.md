# Lexa — Genau das wurde gefixt (Commit bb725db, 2026-06-14)

351 Funde behoben. Gruppiert nach Datei. (Offene Punkte: LEXA_FIX_SUMMARY.md)

## app_cache.json
- **[bug] Ungültiges JSON: überzähliges schließendes "}" am Dateiende**:1
  - Bestätigt: Datei hatte 357 öffnende, aber 358 schließende Klammern und endete mit '...336218}}'. Der Parser meldete 'Extra data: line 1 column 28114 (char 28113)'. Das letzte (überzählige) Byte '}' wurde entfernt, sodass die Datei nun mit '...336218}' abschließt. Keine sonstigen Inhalte verändert. Datei ist eine einzeilige Cache-Datei ohne Kommentare; Kommentarsprache somit nicht betroffen.

## auth.js
- **[quality] Recovery-Erkennung über hash.includes('recovery') ist zu breit und redundant**:271
  - Die redundante Bedingung `hash.includes('type=recovery') || hash.includes('recovery')` wurde durch eine präzise Prüfung ersetzt: `new URLSearchParams(hash.replace(/^#/,''))` parst den Hash, und `isRecovery` ist nur true bei `type === 'recovery'` (Supabase-Standard mit access_token im Hash) oder exakt `#recovery` (eigener redirectTo aus Zeile 241). Zusätzlich wurde der bisher fehlende else-Zweig ergänzt: Bei type=recovery aber fehlender Session (abgelaufener/bereits verwendeter Link) wird jetzt eine verständliche Fehlermeldung angezeigt (DE/EN, inline analog mapError, da t() bei fehlenden i18n-Keys nur den Roh-Key zurückgäbe und i18n.js nicht geändert werden darf), statt stillschweigend zur Login-Maske zu fallen.
- **[bug] Recovery-Formular bei mehrfacher Auslösung doppelt eingefügt und doppelte Submit-Listener**:290
  - In der Recovery-IIFE wurde direkt nach der isRecovery-Prüfung ein Guard ergänzt: `if (document.getElementById('recovery-password-form')) return;`. Damit wird bei erneuter Auslösung kein zweites Formular mit gleicher fester ID 'recovery-password-form' eingefügt und kein weiterer Submit-Listener registriert. getElementById('recovery-new-password') trifft somit immer das korrekte, einzige Feld. Der Guard steht vor dem await getSession(), sodass keine Race-Bedingung mit doppeltem Einfügen entsteht.
- **[improvement] Nach erfolgreicher Registrierung bleibt das Formular ausgefüllt und der Nutzer ohne klare nächste Aktion**:186
  - Im Erfolgsfall des Signup-Handlers wird jetzt `signupForm.reset()` aufgerufen (Passwortfelder bleiben nicht mehr im DOM befüllt), der Login-Tab wird per `loginTab.click()` programmatisch aktiviert (wiederverwendet die bestehende Tab-Switch-Logik inkl. Klassenumschaltung) und die Erfolgsmeldung 'Konto erstellt! Bitte bestätige deine E-Mail.' wird BEWUSST NACH dem Tab-Klick gesetzt, da der Tab-Handler hideMessage() aufruft — so bleibt die Meldung auf der Login-Maske sichtbar und der nächste Schritt (E-Mail bestätigen, dann anmelden) ist eindeutig. setLoading(btn,false) bleibt erhalten. Sichere, nicht-signaturbrechende UX-Verbesserung; nur lokal in auth.js.

## backend/action_parser.py
- **[bug] update_history-Default max_entries=40 ist inkonsistent mit MAX_HISTORY=80**:594
  - Default des Parameters max_entries von hartkodiert 40 auf None geaendert; wenn None, wird der Wert per Lazy-Import aus backend.config.MAX_HISTORY (=80) bezogen. Bestaetigt: router_chat.py uebergibt ueberall MAX_HISTORY, aber alle vier update_history-Aufrufe in router_agent.py (Zeilen 253, 275, 355, 387) liessen max_entries weg und kappten die geteilte conversation_history damit auf 40 statt 80. Lazy-Import gewaehlt (analog zum bestehenden tool_registry-Import in _validate_llm_tool_params), weil ein Modul-Level-Import von backend.config die isolierte Modul-Ladung im Test (test_action_parser.py stubt nur security und i18n) brechen wuerde. Oeffentliche Signatur bleibt kompatibel (None-Default, int weiterhin erlaubt). Verifiziert: 60x update_history ohne max_entries -> 80 Eintraege.

## backend/agent_loop.py
- **[bug] forced_direct_summary verwirft echtes Ergebnis bei leerer Summary-Formatierung**:1247
  - Den Forced-First-Tool-Summary-Block auf ein Formatter-Mapping umgestellt. Wenn ein direkt-summarisierendes Tool (desktop_position, desktop_engine_status/observe, ui_tree, ui_find, hermes_desktop_task, screen_read_text ohne LLM-Followup) erfolgreich war, faellt forced_direct_summary jetzt bei leerem Formatter-Ergebnis auf step.result bzw. einen generischen Abschluss-Text zurueck. Damit setzt der Block run.status='completed' statt stillschweigend in die LLM-Schleife (und potenzielle Doppel-Antwort) zu fallen. screen_read_text-Followup-Logik unveraendert (formatter=None).
- **[bug] Mehrere Tool-Calls pro LLM-Turn: nach dem ersten Schritt wird der Rest verworfen (break)**:1672
  - Das Verhalten ist bewusstes Sicherheitsdesign (Mini-Reflexion zwischen Schritten statt blindem Parallel-Ausfuehren). Statt einer riskanten Verhaltensaenderung (alle Calls sequentiell) wurde gemaess der vom Finding angebotenen Option der Code mit einem klaren Kommentar dokumentiert: pro LLM-Turn wird genau ein Tool-Call ausgefuehrt, die uebrigen werden nach Zwischenergebnis vom LLM neu bewertet. Kein Funktionsverhalten geaendert.
- **[bug] Repeat-Failure-Abbruch greift nur fuer exakt identische Argumente**:1598
  - Zusaetzlich zum signaturbasierten Zaehler (failed_tool_attempts) einen Pro-Tool-Namen-Zaehler (failed_tool_by_name) eingefuehrt. Der Agent-Loop bricht jetzt auch ab, wenn dasselbe Tool dreimal (unabhaengig von minimaler Arg-Variation) fehlschlaegt — neue Variable tool_failure_limit_hit steuert sowohl die Fehlertext-Anreicherung als auch run.status='failed' mit passender Summary.
- **[quality] Importiertes _TOOL_ARGUMENT_ERROR_REPLY wird sofort durch lokale Definition ueberschrieben**:38
  - Die lokale Neudefinition von _TOOL_ARGUMENT_ERROR_REPLY entfernt und die Konstante stattdessen aus backend.action_parser importiert. Beide Codepfade (Agent-Loop und action_parser) nutzen jetzt garantiert denselben Fehlertext, keine zwei Quellen der Wahrheit mehr.
- **[bug] duration_ms bei Timeout falsch berechnet (Konfig-Limit statt gemessene Zeit)**:1213
  - An beiden Timeout-Zweigen (Forced-Block und Hauptschleife) step.duration_ms = (time.time() - step.started_at) * 1000 statt AGENT_STEP_TIMEOUT * 1000 gesetzt, sodass die real verstrichene Dauer in Audit-/Ledger-/Trace-Daten landet statt der fiktiven Obergrenze.
- **[security] Agent-Pfad fuehrt kein check_action_rate_limit fuer riskante Tools durch**:441
  - check_action_rate_limit(action_name) in _execute_tool nach Permission/Reflection/validate_params/_sanitize_params und vor der Ausfuehrung (deckt sowohl companion- als auch personal_os-Pfad ab) aufgerufen. Bei Ueberschreitung wird {success:false, rate_limited:true} zurueckgegeben und agent_risk_rate_limited geloggt — gleicher Schutz wie im action_executor-Pfad. Read-only-Aktionen bleiben erlaubt (Verhalten der security-Funktion).
- **[bug] Personal-OS-Tool-Ergebnis wird nicht auf dict-Typ geprueft**:435
  - Das Ergebnis von execute_personal_os_action wird jetzt wie im companion-Pfad mit isinstance(result, dict) abgesichert; bei Abweichung wird derselbe {success:false, error:'Unerwartetes Ergebnis: <typ>'}-Fallback zurueckgegeben statt ein Nicht-dict an _format_tool_result durchzureichen.

## backend/agent_protocol.py
- **[security] trace_path_is_safe gibt fuer Pfade AUSSERHALB des Repos True zurueck — Traces koennen an beliebige Orte geschrieben werden**:412
  - Im ValueError-Zweig (Pfad ausserhalb repo_root) wird nicht mehr pauschal True zurueckgegeben. Stattdessen wird der aufgeloeste Pfad jetzt gegen das OS-Temp-Verzeichnis (tempfile.gettempdir()) geprueft: nur Ziele unterhalb dieses kontrollierten Scratch-Bereichs (deckt pytest tmp_path und ein auf Temp gesetztes LEXA_AGENT_TRACE_DIR ab) gelten als sicher; alle anderen externen Pfade (System-Verzeichnisse, Netzlaufwerke) werden mit False abgelehnt. Bei einem OSError aus gettempdir() wird sicher mit False abgelehnt. Dazu wurde 'import tempfile' ergaenzt und ein erklaerender englischer Kommentar (Dateisprache) eingefuegt. Die In-Repo-Logik (tmp/, .test-tmp/, evals/results/) blieb unveraendert.

## backend/agent_reflection.py
- **[improvement] createReviewDraft-Pruefung greift nur fuer exakt diesen Key — verwandte Draft-Flags werden nicht erfasst**:191
  - infer_reflection_risk prueft den Draft-Flag jetzt ueber den neuen Helfer _has_draft_flag() statt nur 'params.get("createReviewDraft") is True'. Hinzugefuegt: _TRUTHY_VALUES, Konstante _DRAFT_FLAG_KEYS (createReviewDraft, create_review_draft, applyDraft, apply_draft, approveDraft, approve_draft) sowie _is_truthy_flag() (akzeptiert bool True, numerisch !=0 und Strings '1'/'true'/'yes'/'on'/'enabled', bool-sicher gegen int-Verwechslung). Damit werden Draft-/Apply-erzeugende Writes auch bei snake_case-Key, String-Wahrheitswerten und verwandten Apply/Approve-Flags zuverlaessig als 'high' eingestuft (review-first-Absicht gehaertet). Real verifiziert: tool_registry.py:479 und router_os_agents.py:31 senden camelCase 'createReviewDraft', waehrend os_agent_runtime.py:489/530 intern snake_case 'create_review_draft' nutzt — beide Schreibweisen kommen also tatsaechlich vor. Falsy-Werte (False/'false'/'no'/0/fehlend) liefern korrekt False; bestehender Test mit bool True bleibt gruen.

## backend/agent_trace_sampling.py
- **[improvement] Trace-Sampling nutzt nicht-kryptografisches, nicht reproduzierbares random ohne Seed-Kontrolle**:79
  - should_sample nutzt jetzt eine dedizierte random.Random-Instanz (self._rng) statt des globalen random.random(). Dazu wurde ein optionales Feld seed: int | None = None zur frozen-Dataclass hinzugefuegt; in __post_init__ wird via object.__setattr__ self._rng = random.Random(self.seed) erzeugt (nicht als Dataclass-Feld, damit frozen-/eq-/hash-Semantik unveraendert bleibt). from_env liest den Seed ueber den neuen Helfer _env_optional_int aus LEXA_AGENT_TRACE_SEED (None bei fehlend/ungueltig). Dadurch sind Sampling-Entscheidungen vom prozessweiten RNG entkoppelt und bei gesetztem Seed fuer Eval-Laeufe reproduzierbar.

## backend/ai_engine.py
- **[performance] Gemini-API-Aufrufe setzen weder max_tokens noch temperature**:513
  - Real bestaetigt: _openai_compatible_api_call() baute kwargs nur aus model/messages/stream/timeout, ohne jede Begrenzung (Gemini-only-Pfad lief ungedeckelt). Neue Konstanten _GEMINI_MAX_TOKENS=4096 und _GEMINI_TEMPERATURE=0.7 (analog _GROQ_MAX_TOKENS) eingefuehrt und im kwargs-Dict gesetzt. Damit sind Latenz/Kosten begrenzt und das Verhalten deterministisch, konsistent zum Groq-Pfad.
- **[bug] Tool-freier Stream-Retry triggert bei Gemini praktisch nie**:3989
  - Real bestaetigt: Der Stream-Retry ohne Tools haengte an _categorize_error(e) in (TOOL_USE_FAILED, UNKNOWN); TOOL_USE_FAILED wird nur an Groq-Strings erkannt, sodass viele Gemini-Tool-Fehler (z.B. als RATE_LIMIT/MODEL/NETWORK kategorisiert) den Retry uebersprangen. Bedingung auf 'if tools:' gelockert — konsistent zum non-streaming chat()-Pfad, der immer einen Tools-freien Retry versucht. Gefahrlos, da der Branch erst greift, wenn noch kein Text gestreamt wurde (vorheriges 'if full_text_parts:' returned bereits).
- **[improvement] Veralteter Modul-Docstring beschreibt Multi-Provider-Setup**:1
  - Real bestaetigt: Docstring bewarb 'Groq (primary) + OpenAI + Gemini + Claude', obwohl AI_MODEL_REGISTRY und _PROVIDER_FALLBACK_ORDER=('gemini',) gemini-only sind. Docstring auf Gemini-only (3.5-flash / 3.1-flash-lite / 3.1-pro via OpenAI-kompatibler Schnittstelle, native function calling) aktualisiert, mit Hinweis dass die Multi-Provider-Gerueste nur noch Legacy/Reserve sind.
- **[performance] Pro Memory-Suche ein neuer ThreadPoolExecutor**:3683
  - Real bestaetigt: In _build_messages wurde pro substanzhaltiger Nachricht ein frischer ThreadPoolExecutor(max_workers=1) erzeugt und mit shutdown(wait=False, cancel_futures=True) verworfen. Ersetzt durch einen modul-globalen, langlebigen Executor _memory_search_executor = ThreadPoolExecutor(max_workers=2, thread_name_prefix='lexa-memsearch'); pro Request wird nur noch das Future mit 0.5s-Timeout abgewartet. Damit bleibt die Anzahl der Hintergrund-Threads beschraenkt (kein Pro-Request-Overhead, keine unbegrenzte Akkumulation bei haengender DB), waehrend der nicht-blockierende 0.5s-Timeout erhalten bleibt.

## backend/companion_confirmation.py
- **[performance] _cleanup_expired wird nur bei create aufgerufen — abgelaufene Confirmations bleiben im Speicher**:70
  - Real bestätigt: _cleanup_expired wurde ausschließlich in create_confirmation (Z.93) aufgerufen; consume_confirmation entfernte nur den einzelnen angefragten abgelaufenen Eintrag. Bei prepare-Spam ohne folgendes create wuchs _store unbegrenzt. Fix: _cleanup_expired(now, keep=cid) wird jetzt zu Beginn von consume_confirmation unter dem _lock ausgeführt, sodass alle abgelaufenen/verbrauchten Records beim Konsumieren mitbereinigt werden. _cleanup_expired erhielt dafür einen optionalen keep-Parameter, der den gerade angefragten confirmation_id von der Bereinigung ausnimmt — so bleiben dessen spezifische Fehlercodes (confirmation_expired/confirmation_replay) unverändert erhalten. Keine Signaturänderung an öffentlichen Funktionen, kein neuer Import, Kommentarsprache (Deutsch/Englisch gemischt, hier Deutsch passend) beibehalten.

## backend/context_monitor.py
- **[performance] Clipboard-Polling startet alle ~10s einen PowerShell-Prozess — Dauerlast und Latenz**:179
  - get_clipboard() liest jetzt direkt ueber das Win32-API via ctypes (OpenClipboard/GetClipboardData CF_UNICODETEXT, GlobalLock/Unlock, CloseClipboard) statt bei jedem Tick einen powershell.exe-Prozess zu spawnen. Die noetigen ctypes-Prototypen (restype/argtypes fuer HANDLE/Pointer) wurden gesetzt, damit Pointer auf 64-bit nicht trunkiert werden. opened-Flag + finally garantiert CloseClipboard. import subprocess entfernt (nicht mehr genutzt). Oeffentliche Signatur (-> str) und Max-Laenge 200 unveraendert. Keine neuen Dependencies (nur stdlib ctypes).
- **[performance] Event-Handler werden synchron im Monitor-Thread aufgerufen und koennen den Loop blockieren**:328
  - emit() ruft die Handler nicht mehr synchron im context-monitor-Thread auf, sondern uebergibt jeden Handler-Aufruf an einen kleinen ThreadPoolExecutor (max_workers=2, in __init__ erstellt). Fehlerbehandlung pro Handler in neuer Statik-Methode _run_handler() ausgelagert. Ein langsamer/blockierender Handler verzoegert damit nicht mehr CPU/Memory/Idle/Clipboard-Detection im Tick. Pool lebt fuer die Singleton-Lebensdauer (start/stop steuern nur den Monitor-Thread), daher kein Dead-Pool nach stop().
- **[bug] ctypes.windll-Zugriff auf Modulebene bricht Import auf Nicht-Windows / in Tests**:25
  - user32/kernel32 werden nur noch unter sys.platform == 'win32' aus ctypes.windll geholt, sonst auf None gesetzt; das Modul ist damit auf Nicht-Windows importierbar. Die nutzenden Methoden get_active_window(), get_idle_time() und get_clipboard() haben einen Guard (Rueckgabe der jeweiligen Leer-Defaults), falls user32/kernel32 None sind. import sys ergaenzt.

## backend/conversation_summary.py
- **[bug] Assistant-Nachrichten werden bei jeder Zusammenfassung mit json.loads geparst — falsche Annahme ueber History-Format**:171
  - Verifiziert ueber das ganze Projekt: update_history (action_parser.py:611-612) und alle Aufrufer in router_chat.py/router_agent.py speichern als Assistant-content immer formatierten Antworttext (oft mit '[Hermes]'/'[Agent]'/'[Aktion: ...]'-Praefixen), nie rohes JSON — der json.loads-/_CREATED_ACTION_LABELS-Zweig war damit toter Code und das _ACTION_PATTERN ('"action":"..."') matchte praktisch nie. Toten Code entfernt: json.loads-Block, _CREATED_ACTION_LABELS, lexa_created-Liste, die 'Lexa erstellte'-Ausgabezeile und den ungenutzten 'import json'. Statt der unzuverlaessigen JSON-Erkennung erfasse ich Aktionen jetzt ueber den real existierenden Textmarker via neues _ACTION_MARKER_PATTERN (r'\[Aktion:\s*([a-z_][a-z0-9_]*)', genau das Format aus router_chat.py:1783); die Ausgabezeile lautet jetzt korrekt 'Lexa schlug Aktionen vor', da der Marker nur bei bestaetigungspflichtigen Vorschlaegen gesetzt wird. Strukturierte Aktions-Metadaten zu persistieren waere dateiuebergreifend und wurde bewusst nicht gemacht; die finding-eigene Alternative (JSON-Zweig entfernen, auf Textformat ausrichten) ist rein lokal umgesetzt.
- **[quality] extract_keywords: words.index() pro Wort — O(n*k) und intransparentes Ranking**:109
  - Den dict-Comprehension {word: words.index(word) for word in counts} (O(n*k) durch list.index pro distinct word) durch einen einzigen Durchlauf ersetzt: first_index = {}; for i, word in enumerate(words): first_index.setdefault(word, i). Liefert identisches Ergebnis (erstes Vorkommen je Wort, Originalreihenfolge), aber O(n) statt O(n*k). Erklaerender Kommentar ergaenzt.

## backend/hermes_adapter.py
- **[bug] Telegram-Selftest macht echte HTTP-Calls gegen das eigene Backend**:2143
  - Zwei deterministische Fakes (_selftest_fake_read_json, _selftest_fake_post_json) hinzugefuegt und in _run_lexa_status_plugin_command nun fuer ALLE Befehle plugin._read_json UND plugin._post_json (vorher nur _post_json bei lexa-draft) per setattr ersetzt und im finally wieder restauriert. Dadurch keine realen, re-entranten Requests mehr an 127.0.0.1:8000; die Zusagen externalSends=False/stableWrites=none stimmen jetzt und der Selftest ist nicht mehr laufzeitabhaengig/flaky.
- **[bug] _build_run_command nutzt shlex.split mit posix=True auf Windows**:2550
  - shlex.split(run_args, posix=True) -> shlex.split(run_args, posix=os.name != "nt"), konsistent zu _split_command (Zeile 231). Verhindert das Verschlucken von Backslashes in Windows-Pfaden des Run-Arg-Templates.
- **[performance] Autostart-Batch: gateway.log waechst unbegrenzt (keine Rotation)**:2369
  - In _gateway_autostart_script_body() eine Batch-Rotationszeile ergaenzt: vor dem Start wird gateway.log via 'for %%I ... if %%~zI GTR 5MB move /y' nach gateway.log.1 verschoben (Schwelle _GATEWAY_LOG_ROTATE_BYTES=5MB). Schreib- und Vergleichspfad nutzen dieselbe Body-Funktion, daher bleibt _gateway_autostart_script_is_current konsistent.
- **[bug] Autostart-Deaktivierung: unlink() ohne missing_ok kann FileNotFoundError werfen**:2405
  - exists()-Check + unlink() ersetzt durch startup_path.unlink(missing_ok=True) (Python 3.8+). Operation jetzt race-frei und idempotent.
- **[bug] Autostart-Batch escaped Prozentzeichen in eingebetteten Pfaden nicht**:2363
  - Helper _cmd_escape_percent('%'->'%%') eingefuehrt; _cmd_quote escaped jetzt zusaetzlich '%', und die wortwoertlich per 'set' eingebetteten Pfade HERMES_HOME/LEXA_PERSONAL_OS_ROOT werden explizit ueber _cmd_escape_percent gefuehrt. Die beabsichtigten Batch-Expansionen %date%/%time% in der Echo-Zeile bleiben unangetastet (separates String-Literal).
- **[quality] Selftest ruft Plugin-internen Hook _pre_gateway_dispatch statt des registrierten Hooks auf**:2266
  - hook = getattr(plugin, '_pre_gateway_dispatch', None) ersetzt durch hook = context.hooks.get('pre_gateway_dispatch'). Es wird jetzt der real ueber ctx.register_hook verdrahtete Callable getestet (echter Vertrag), nicht eine namensbasierte Annahme.
- **[improvement] configure_hermes_telegram validiert home_channel inhaltlich kaum**:2454
  - Neue Full-Match-Regex _TELEGRAM_HOME_CHANNEL_RE = ^(?:-?\d{5,}|@[A-Za-z0-9_]{4,})$ und Validierung in configure_hermes_telegram ergaenzt (status 'invalid_home_channel' mit klarer Fehlermeldung), bevor in die .env geschrieben wird. Akzeptiert numerische Chat-IDs (Gruppen negativ) und @usernames.
- **[performance] _lexa_mcp_command_ready ruft shutil.which bei jedem Server pro Request**:1419
  - TTL-gecachter Wrapper _cached_which (30s TTL via time.monotonic) eingefuehrt und in _lexa_mcp_command_ready statt shutil.which(command) verwendet. Kurzer TTL haelt frisch installierte Befehle nach Sekunden sichtbar, vermeidet aber PATH-Scans pro Capabilities/Extensions-Request.
- **[bug] _write_hermes_env_values liest .env ohne errors-Toleranz**:514
  - read_text(encoding='utf-8') -> read_text(encoding='utf-8', errors='replace'), konsistent zu _read_hermes_env_file. configure_hermes_telegram kann die .env jetzt auch bei leicht beschaedigtem Bestand neu schreiben statt mit UnicodeDecodeError (502) abzubrechen.
- **[quality] Inkonsistente Telegram-Token-Regex-Benennung (Anchoring)**:75
  - _TELEGRAM_TOKEN_RE umbenannt in _TELEGRAM_TOKEN_FULLMATCH_RE (mit Kommentar zur Abgrenzung von der Wort-Grenzen-Redaktions-Regex in router_hermes.py) und an der einzigen Nutzung (configure_hermes_telegram, match) aktualisiert. Beide Vorkommen lagen ausschliesslich in dieser Datei.
- **[bug] run_hermes_task uebergibt vollstaendigen Prompt via -z statt stdin (Windows-argv-Limit)**:2540
  - stdin-Uebergabe ist laut Hermes-CLI nicht moeglich (vendor _parser.py: -z/--oneshot ist ein argparse-Optionswert, kein stdin-Modus). Daher die zweite vom Finding genannte Variante umgesetzt: _cap_argv_prompt() deckelt den Prompt auf _MAX_ARGV_PROMPT_CHARS=30000 (Sicherheitsmarge unter 32767) mit Truncation-Marker und Warn-Log; angewandt im Default-Pfad (-z) und im Template-Pfad, wenn der Prompt ueber argv eingesetzt wird. Verhindert obskure OSError bei sehr grossen Prompts.
- **[performance] shutil.which('whisper') bei jedem Media-Statusabruf**:1095
  - In get_hermes_media_status local_stt_command_ready nutzt jetzt _cached_which('whisper') statt shutil.which('whisper'); konsistent mit dem Caching fuer MCP-Command-Pruefungen.

## backend/i18n/__init__.py
- **[improvement] t() laesst nicht ersetzte {{platzhalter}} im Text stehen**:48
  - In t() wird nach der kwargs-Ersetzung per Regex r'\{\{(\w+)\}\}' auf verbleibende Platzhalter geprueft. Fehlende Parameter werden jetzt via _log.warning mit Key und Parameternamen gemeldet, sodass defekte Tokens in den Logs sichtbar werden. Der Rueckgabewert bleibt unveraendert (Text wird nicht stillschweigend geleert), um keine UI-Ausgabe zu brechen. Dafuer wurde 'import re' ergaenzt.
- **[improvement] init() laedt nur hartkodierte Locales 'de'/'en' — inkonsistenter Zustand bei anderer Sprache**:34
  - init() iteriert nun ueber sorted({'de','en',lang}), laedt also auch die zur Laufzeit angeforderte Sprache. _lang wird nicht mehr unbedingt vorab gesetzt, sondern erst NACH dem Laden: nur wenn lang erfolgreich in _translations vorliegt, wird es aktiviert; andernfalls Warnung und Fallback auf 'de'. Damit entfaellt der inkonsistente All-Fallback-Zustand bei z.B. init('fr'). Oeffentliche Signatur init(lang='de')->None unveraendert; bestehender Aufruf i18n_init('de') in backend/main.py:345 verhaelt sich identisch.

## backend/integrations.py
- **[quality] get_spotify_status: process_iter ohne NoSuchProcess/AccessDenied-Behandlung kann iterieren-während-Mutation crashen**:156
  - In get_spotify_status() wird der Attributzugriff (proc.info["name"]) jetzt pro Iteration einzeln gegen psutil.NoSuchProcess/AccessDenied/ZombieProcess abgesichert (try/except mit continue). Vorher fing nur das äußere try/except, wodurch ein einzelner flüchtiger Fremdprozess die gesamte Spotify-Erkennung mit 'error' abbrach, obwohl Spotify lief. Zusätzlich wurde das fragile for/else durch ein explizites pid=None + if-Check ersetzt, da das ursprüngliche else nur bei einer durchlaufenen Schleife greift, nicht aber wenn eine Exception die Schleife verlässt.

## backend/intent_engine.py
- **[bug] 'has_spotify and not play_idx' behandelt play_idx==0 falsch**:426
  - Zeile 426 von 'if has_spotify and not play_idx:' auf 'if has_spotify and play_idx is None:' geaendert. Beseitigt den fragilen Truthiness-Check, der einen Play-Index 0 faelschlich als 'kein Treffer' wertete. Verhalten bleibt aktuell identisch (vorgelagerter Block faengt play_idx==0 ab), ist aber jetzt semantisch korrekt und reihenfolge-robust.
- **[bug] YouTube-Befehle werden von _RE_SPOTIFY abgefangen, _RE_YOUTUBE nie erreicht**:643
  - In _RE_SPOTIFY einen Negative-Lookahead '(?!.*\b(?:auf|on|in)\s+youtube\b)' ergaenzt, damit 'spiele X auf youtube' nicht mehr als Spotify-Suche abgefangen wird, sondern zu _RE_YOUTUBE durchfaellt. Zusaetzlich im _RE_YOUTUBE-Handler das offene Verbindungswort am Query-Ende getrimmt (re.sub r'\s+(?:auf|on|in)$'), damit die Suche 'lofi' statt 'lofi auf' enthaelt.
- **[bug] Wetter-City-Regex erfasst nachfolgende Zeitwoerter als Stadtnamen**:714
  - Nachgestellte Zeit-/Fuellwoerter (heute|morgen|gerade|jetzt|aktuell|draussen|drauußen|bitte) werden jetzt am City-Slot entfernt. Da der Wetterpfad in der Praxis zuerst in _try_smart_intent (vormals ohne Trailing-Strip) gematcht wird, wurde der Trim an BEIDEN Stellen eingefuegt: in der fuzzy-Extraktion in _try_smart_intent (nach dem rstrip) und nach dem _RE_WEATHER_CITY-Match.
- **[bug] Compound-Suche strippt Such-Praefix nur bei Leerzeichen-Trennung**:471
  - Strip-Regex im Compound-Zweig 'oeffne chrome und suche nach Y' von r'^(?:such(?:e?)|search|google)\s+(?:nach\s+)?' auf r'^(?:such(?:e?)|search|google)\b[\s:]*(?:nach\s+)?' geaendert, sodass auch Doppelpunkt/fehlendes Leerzeichen (z.B. 'suche:python') abgedeckt sind.
- **[bug] _clip_context_path strippt ']' und '}' und beschaedigt valide Pfade**:99
  - rstrip-Zeichensatz in _clip_context_path von '.,;:!?) ]}' auf '.,;:!?) ' reduziert. ']' und '}' werden nicht mehr abgeschnitten, sodass Windows-Pfade wie 'C:\Daten\[2024]' erhalten bleiben; Satzendzeichen und ')' werden weiterhin entfernt.
- **[improvement] Mehrsatz-Filter laeuft erst nach den Smart-Intents**:932
  - Den Mehrsatz-/Mehrfachfrage-Guard (msg.count('.')>2 or msg.count('?')>1 -> None) vor _try_smart_intent verschoben (jetzt nach dem Math-Handler, vor der fuzzy App-/Musik-/Such-Heuristik). Kontextuelle und deterministische Math-Intents laufen weiterhin davor; klar konversationelle Eingaben werden nicht mehr von der Schnellpfad-Heuristik abgefangen.

## backend/lexa_system_answer.py
- **[quality] Toter Code: Grossbuchstaben-Umlaut-Mappings nach .lower() wirkungslos**:24
  - Real bestaetigt und korrigiert. _normalize lowercaste zuerst und ersetzte erst danach — dadurch waren nicht nur die Grossbuchstaben-Keys (Ä/Ö/Ü/ẞ) tot, sondern (per Codepoint-Pruefung verifiziert) AUCH die Mojibake-Sequenzen 'Ã¤'/'Ã¶'/'Ã¼'/'ÃŸ' (U+00C3 wird durch .lower() zu U+00E3 und matcht nie). Statt nur tote Keys zu loeschen (was die Mojibake-Behandlung weiterhin wirkungslos gelassen haette), wurde der Ersetzungs-Schritt VOR das .lower() gezogen: value = str(text or ''); replacements; value = value.lower(). Damit greifen jetzt alle Eintraege wie urspruenglich beabsichtigt (Gross/Klein + Mojibake), und die 4 redundanten Duplikat-Keys ('ä'/'ö'/'ü'/'ß' Zweitvorkommen) wurden entfernt. Output fuer alle bisher funktionierenden Eingaben bleibt identisch (lowercase-Umlaute -> ae/oe/ue/ss). Kurzer deutscher Kommentar ergaenzt.
- **[quality] Ungenutzter Parameter user_message in build_lexa_system_answer**:131
  - Real bestaetigt: build_lexa_system_answer(user_message) verwendete den Parameter nirgends (Antwort ist laut Modul-Docstring bewusst deterministisch/zustandsbasiert). Projektweiter Grep zeigt, dass build_lexa_system_answer nur ein einziger interner Aufruf hat (try_lexa_system_answer, Zeile 164) und von keinem Test/keiner anderen Datei direkt aufgerufen wird. Die statische Antwort ist gewollt, daher wurde — wie im Fix vorgeschlagen — die irrefuehrende Signatur bereinigt: Parameter aus build_lexa_system_answer() entfernt und den einzigen Aufruf auf build_lexa_system_answer() angepasst. Die oeffentliche Signatur try_lexa_system_answer(user_message) bleibt unveraendert (nutzt user_message weiterhin fuer looks_like_lexa_system_question).

## backend/local_auth.py
- **[security] is_public_path öffnet im Dev-Modus /docs, /openapi.json etc. ohne Auth — LEXA_ENV ist beliebig setzbar**:32
  - Problem am echten Code bestätigt: is_public_path() gab im Dev-Modus alle Doku-Pfade (_DEV_DOC_PATHS) ungeauthentifiziert frei, allein gesteuert durch die frei setzbare Env-Var LEXA_ENV (genutzt in der local_auth_middleware in backend/main.py:198). Der primäre fix-Vorschlag (LEXA_ENV im PyInstaller-Entry hart auf 'production' erzwingen) liegt außerhalb dieser Datei. Stattdessen die in-file-Alternative aus dem fix-Feld umgesetzt: Doku-Endpunkte nur bei zusätzlich gesetztem explizitem Flag freigeben. Neue Helper-Funktion are_docs_exposed() eingeführt, die jetzt BEIDE Bedingungen verlangt — is_dev_mode() UND LEXA_EXPOSE_DOCS in _TRUE_VALUES ({'1','true','yes','on'}). is_public_path() ruft jetzt are_docs_exposed() statt is_dev_mode() auf. Neue Konstante _TRUE_VALUES analog zum vorhandenen _FALSE_VALUES ergänzt. Damit kann ein versehentlich mit LEXA_ENV=development ausgeliefertes Build die OpenAPI-Spezifikation nicht mehr ohne bewusstes zweites Opt-in offenlegen; der legitime Dev-Workflow bleibt durch Setzen von LEXA_EXPOSE_DOCS=1 möglich. Öffentliche Signatur von is_public_path() unverändert, keine neuen Abhängigkeiten, deutsche/englische Kommentarsprache der Datei beibehalten. Hinweis: Die neue Env-Var LEXA_EXPOSE_DOCS sollte in .env.example dokumentiert werden — diese Datei wurde gemäß Auftrag nicht angefasst.

## backend/main.py
- **[bug] CORS-Origins passen nicht zur tatsächlichen Frontend-Herkunft (allow_credentials mit toten Origins)**:163
  - Bestätigt: frontend/main.js lädt den Renderer per pathToFileURL().href (file://, Origin 'null'), preload.js spricht http://127.0.0.1:8000 an — die Port-3000-Origins existieren nie. allow_origins auf ['null'] korrigiert (reale file://-Herkunft) und allow_credentials von True auf False gesetzt, da die Auth header-basiert (LOCAL_AUTH_HEADER) und nicht cookie-basiert ist. Erläuternder Kommentar ergänzt, dass der eigentliche Schutz aus local_auth_middleware + 127.0.0.1-Binding stammt. Keine öffentliche Signatur geändert.
- **[performance] Sentry-DSN-Keyring-Lookup beim Modulimport blockiert synchron vor App-Start**:31
  - Bestätigt: keyring.get_password lief auf Modulebene im Import-Pfad. Init in Helper _init_sentry(dsn) ausgelagert. Am Modulimport wird nur noch der nicht-blockierende env-DSN (SENTRY_DSN) gelesen und ggf. sofort initialisiert. Der potenziell blockierende keyring-Lookup wurde in startup_event verlagert und läuft dort über asyncio.to_thread (Projektmuster für blockierendes I/O), gefolgt von _init_sentry. Verhalten/DSN-Quellen identisch, nur Timing/Threading geändert.
- **[quality] Optionale Phase-39+-Router scheitern still bei echten Importfehlern (nicht nur fehlenden Modulen)**:293
  - Bestätigt: try/except ImportError -> router=None verschluckte echte Folge-Importfehler ohne Log. Sieben Einzelblöcke durch Helper _load_optional_router(module_name) ersetzt. Helper unterscheidet ModuleNotFoundError für das Modul selbst (erwartet -> debug) von fehlender Abhängigkeit/echtem ImportError im Modul (-> logger.warning mit Modul-/Fehlerdetails). Da logger erst weiter unten via basicConfig konfiguriert wird, nutzt der Helper logging.getLogger('lexa.server') direkt (analog zum bestehenden print-Muster bei sentry). Verhalten bei tatsächlich fehlenden optionalen Modulen unverändert (router=None).
- **[improvement] /ai/title validiert message nicht auf Leerstring und wendet sanitize_input nicht an vor teurem AI-Call**:718
  - Bestätigt: ai_generate_title rief generate_title(req.message) ohne Validierung/Sanitizing auf. Jetzt wird req.message getrimmt und durch sanitize_input (aus backend.security, gleiches Muster wie router_chat) geführt; bei leerem Inhalt wird sofort der lokale Fallback-Titel 'Neue Unterhaltung' ohne Provider-Call zurückgegeben. Kein i18n-Key existierte für den Fallback, daher deutsche Literal-Zeichenkette (passend zur Datei-/Projektsprache). Import von sanitize_input ergänzt; kein zirkulärer Import (security.py importiert weder main noch Router).

## backend/mcp_client.py
- **[bug] JSON-RPC-Response wird nicht zugeordnet, wenn der Server die id als String zurückgibt**:374
  - Im _read_loop wird msg_id jetzt vor dem _pending-Lookup normalisiert: Ist die eingehende id ein numerischer String (z.B. "1", auch mit führendem '-'), wird sie via int(msg_id) zu int gecastet, sodass sie zu den stets ganzzahligen Keys aus _next_id() passt. Damit wird das Future korrekt aufgelöst statt in einen Timeout zu laufen. Echte String-IDs (nicht-numerisch) bleiben unverändert; da der Client selbst nur int-IDs vergibt, kann es solche ohnehin nicht in _pending geben.

## backend/mcp_registry.py
- **[bug] get_all_mcp_tools iteriert die Live-Dict _clients ohne Snapshot (RuntimeError-Risiko)**:414
  - In get_all_mcp_tools() die Iteration von 'for name, client in self._clients.items()' auf einen Snapshot 'for name, client in list(self._clients.items()):' umgestellt — konsistent mit list_servers() (Zeile 264), das aus demselben Grund snapshotet. Damit kann ein nebenläufiges connect() (self._clients[name]=client) oder disconnect() (pop) auf dem Event-Loop den Tool-Aufbau nicht mehr mit 'RuntimeError: dictionary changed size during iteration' crashen. Erklärenden Kommentar im Projektstil ergänzt. Verifiziert per Grep: einziger Aufrufer ist get_merged_tool_definitions (Zeile 443); Fix ist rein lokal und bricht keine Signatur.

## backend/memory.py
- **[quality] Agent-Schritte verschmutzen die interactions-Tabelle mit Platzhalter-Zeilen**:937
  - In auto_remember() ein frühes return ergänzt, das reine Agent-Step-/Tool-Platzhalter überspringt (user_msg == '[agent-step]' UND ai_reply beginnt mit '[Tool:'). Damit landen inhaltslose Mehrschritt-Agent-Zeilen nicht mehr in interactions; echte User-Nachrichten (auch wenn mit Tool-Platzhalter gepaart) werden weiterhin gespeichert.
- **[bug] add_memory: Re-Fetch der eingefügten Zeile per content kann falsche ID liefern**:488
  - Der nachträgliche 'SELECT id ... WHERE content = ? ORDER BY id DESC' wurde entfernt. Stattdessen wird cursor.lastrowid des INSERT verwendet (new_id), an _embed_memory_row übergeben. ID ist garantiert die eingefügte Zeile, Full-Table-Scan auf unindiziertem content entfällt.
- **[bug] routine_toggle meldet Erfolg auch bei nicht existierender Routine**:1158
  - routine_toggle prüft jetzt result.rowcount und gibt bei 0 betroffenen Zeilen "Routine '<name>' nicht gefunden." zurück statt fälschlich Erfolg. routine_create bleibt unverändert (Upsert via ON CONFLICT — Erfolg ist dort korrekt).
- **[bug] reindex_embeddings: toter offset-Zähler und potenzielle Endlosschleife**:805
  - Toten offset-Zähler (Init + Increment) entfernt und das überflüssige OFFSET aus der Query gestrichen. Endlosschleife abgesichert: ein failed_ids-Set sammelt Zeilen, deren Embedding dauerhaft None liefert; diese werden aus jeder Folge-Batch herausgefiltert (LIMIT um len(failed_ids) erhöht), sodass garantiert Fortschritt entsteht und die Funktion terminiert.
- **[improvement] note_create kappt Titel/Content nicht — inkonsistent mit note_update_by_id**:304
  - note_create kappt jetzt title/content/category über die Config-Konstanten MAX_NOTE_TITLE/MAX_NOTE_CONTENT/MAX_NOTE_CATEGORY (lokaler Import, wie bestehendes Muster für EMBEDDING_ENABLED). Konsistent zu note_update_by_id und begrenzt unkontrolliertes DB-Wachstum auch bei internen Quellen.
- **[improvement] _finalize_memory_results trackt Zugriff auch bei reinen Such-/Listen-Operationen**:119
  - track_access=False gesetzt in full_text_search, _like_search_all und global_search, damit reine Such-/Listen-Übersichten den access_count nicht künstlich erhöhen und das Ranking nicht selbstverstärken. search_memory (echte Kontext-Injektion in den Chat, ai_engine.py) bleibt bewusst mit track_access=True. memory_graph nutzt _finalize_memory_results nicht und blieb unberührt.
- **[bug] restore_database: prod_db.ROLLBACK referenziert ggf. ungebundene Variable**:2108
  - prod_db = None vor dem try initialisiert; im except wird vor dem ROLLBACK 'if prod_db is not None' geprüft. Damit kein NameError mehr, wenn productivity._get_db()/Import fehlschlägt, und die echte Fehlerursache wird nicht verschleiert.
- **[bug] restore_database zählt INSERT OR IGNORE-Treffer als 'inserted', auch bei Konflikt-Verwurf**:2063
  - In beiden Restore-Schleifen (Haupt-DB und Productivity-DB) wird statt blindem 'inserted += 1' jetzt cursor.rowcount summiert. Bei OR-IGNORE-Konflikten ist rowcount 0, sodass die an die Backup-UI zurückgegebene Statistik die tatsächlich geschriebenen Datensätze widerspiegelt.

## backend/obsidian_context.py
- **[quality] resolve_personal_os_root kann bei gesetzter, falscher Env-Var still einen nicht existierenden Vault liefern**:175
  - Modul-Logger 'lexa.obsidian' (logging.getLogger, Projektmuster) ergaenzt. Den toten else-Zweig 'candidates[0] if candidates else ...' entfernt (candidates ist nie leer, da drei feste Pfade immer extend-et werden). Bei keinem OS_MANIFEST.md-Treffer wird jetzt deterministisch PROJECT_ROOT/'personal_os' zurueckgegeben statt eines moeglicherweise falsch konfigurierten Env-Pfads, und die Fehlkonfiguration wird per logger.warning sichtbar gemacht.
- **[performance] build_obsidian_context_payload: bis zu ~1000+ Datei-Prefix-Reads pro Aufruf (Latenz/IO)**:256
  - Per-Aufruf-Cache fuer das Scoring-Prefix eingefuehrt: neue _read_score_prefix_cached (functools.lru_cache, maxsize=2048) liest den festen 2200-Zeichen-Prefix (Konstante _SCORE_PREFIX_CHARS) pro Pfad nur einmal und cached die lowercased Variante. _score_path nutzt jetzt diesen Cache statt bei jedem der mehreren Ranking-Durchlaeufe (Priority-Seiten, Area-Indexe, Markdown-Ranking) dieselbe Datei erneut zu oeffnen. build_obsidian_context_payload ruft zu Beginn _read_score_prefix_cached.cache_clear(), sodass Staleness auf einen einzelnen Aufruf begrenzt bleibt und ein zwischen Aufrufen veraenderter Vault weiterhin frisch gelesen wird. Public-Signaturen unveraendert. Eliminiert die innerhalb eines Aufrufs redundanten Scoring-Reads desselben Pfads; aggressivere Eingriffe (scan_limit senken/scoreloses Ranking) wurden bewusst vermieden, da sie die Auswahlqualitaet aendern wuerden.
- **[bug] _clip gibt bei sehr kleinem limit nur den Truncation-Marker statt Inhalt zurueck**:146
  - Im Zweig 'if limit <= len(marker)' wird jetzt value[:limit] (echter Inhaltsanfang) statt marker[:limit] (abgeschnittenes Marker-Fragment wie '\n\n[trunc') zurueckgegeben. _clip wird ausschliesslich modulintern genutzt (bodyPreview, format_obsidian_context_for_prompt) - keine externen Aufrufer betroffen.
- **[improvement] Verbesserung: _priority_context_paths triggert nur bei Treffer in fixer Wortliste**:365
  - Sicher und additiv umgesetzt: Schneller Pfad bleibt erhalten (bei exaktem Treffer in _LEXA_PRIORITY_TERMS werden alle existierenden Priority-Seiten geliefert). Zusaetzlich Fallback ergaenzt - wenn kein Schluesselwort matcht, werden die High-Signal-Seiten ueber den vorhandenen _score_path-Mechanismus eingesammelt (nur jene Priority-Dateien, deren Inhalt/Pfad gegen die Topic-Terms positiv scored). Damit erreichen auch semantisch nahe/deutsche Topics die priorisierten Seiten, ohne bestehende Treffer zu veraendern oder unrelated Seiten faelschlich zu priorisieren.

## backend/os_agent_runtime.py
- **[bug] SDK-Auto-Build schlaegt auf Windows fehl: bare 'npm' statt 'npm.cmd' bei shell=False**:130
  - In _ensure_os_sdk_build wird der Aufruf jetzt ueber npm_cmd = 'npm.cmd' if sys.platform.startswith('win') else 'npm' getaetigt (analog router_personal_os). 'import sys' wurde ergaenzt (war zuvor nicht importiert). Damit wird das os-sdk auf Windows tatsaechlich gebaut statt lautlos per FileNotFoundError/except OSError zu scheitern.
- **[bug] node-Aufruf fuer SDK-Draft-Write nutzt bare 'node' (Inkonsistenz/Portabilitaet)**:199
  - In _write_os_draft_via_sdk wird node_cmd = 'node.exe' if sys.platform.startswith('win') else 'node' eingefuehrt und im subprocess.run-Aufruf statt des bare 'node' verwendet — konsistent zum Muster in router_personal_os.
- **[security] Hermes-stdout/stderr werden unredigiert auf Platte und in der API gespeichert**:475
  - Neuer Helfer _redact_result(result) redigiert stdout/stderr/error via _SECRET_REDACTIONS (Limit _MAX_STORED_TEXT=16000). In _run_task wird das Roh-Ergebnis von run_hermes_task (raw_result) sofort durch _redact_result geschickt; nur das redigierte result wird in task['result'] persistiert und an _append_evidence/_write_review_draft weitergereicht. Damit landen Tokens/Bearer/API-Keys weder im Task-JSON auf Disk noch in der read-only Cockpit-API.
- **[performance] Blockierender npm-Build (bis 60s) im OS-Agent-Threadpool kann beide Worker-Slots blockieren**:123
  - Modulweites Cache-Flag _SDK_BUILD_OK (unter _LOCK) eingefuehrt. _ensure_os_sdk_build setzt es bei vorhandenem dist/ auf True, cached Build-Erfolg/-Misserfolg und bricht bei zuvor fehlgeschlagenem Build (_SDK_BUILD_OK is False) sofort ab, statt pro Review-Draft erneut den bis zu 60s blockierenden npm-Build zu starten. Erscheint spaeter ein dist/, greift weiterhin der frueh-True-Pfad.
- **[improvement] Task-Pruning/Listing sortiert nach Dateiname ohne Praefix-Validierung**:233
  - In _prune_task_store und list_os_agent_tasks wird der Glob von '*.json' auf 'osagt_*.json' eingeschraenkt. Fremddateien ohne osagt_-Praefix verfaelschen damit weder die namensbasierte Sortierung noch das Pruning; nur Dateien des erwarteten Schemas werden gelistet/geprunt.

## backend/personal_os_actions.py
- **[quality] personal_os_lexa_code_loop wirft bei ungueltigem Tag-Parameter statt Default zu nutzen**:531
  - Die Tag-Aufloesung in personal_os_lexa_code_loop in try/except ValueError gekapselt: ist das uebergebene Tag (z.B. '!!!') nicht sanitisierbar und _normalize_tag wirft ValueError, wird jetzt auf den Default 'lexa' zurueckgefallen (loop_tag = _normalize_tag('lexa') or None), statt das gesamte Code-Loop-Briefing abzubrechen. Das Tag wird in eine lokale Variable loop_tag aufgeloest und an _build_lexa_code_loop_payload uebergeben.
- **[quality] Breites except Exception leakt rohe Exception-Strings an den Chat-Nutzer**:600
  - logging importiert und Modul-Logger logger = logging.getLogger('lexa.personal_os_actions') ergaenzt (analog zum Projektmuster in router_personal_os.py). Der generische except-Block protokolliert die Exception jetzt via logger.exception('Personal OS Aktion "%s" fehlgeschlagen', command) und gibt dem Nutzer nur noch die generische Meldung 'Personal OS Aktion fehlgeschlagen (interner Fehler).' zurueck, statt die rohe exc-Repraesentation (potenziell interne Pfade/MCP-Interna) durchzureichen. HTTPException-/ValueError-Pfade bleiben unveraendert.

## backend/plugin_manager.py
- **[security] allow_private_hosts erlaubt trusted Plugins Zugriff auf localhost und Cloud-Metadata (SSRF)**:392
  - Neue Helfer _is_always_forbidden_ip()/_is_always_forbidden_host() eingefuehrt, die link-local/metadata (inkl. 169.254.169.254, auch IPv4-mapped), reserved, multicast und unspecified IMMER blockieren. validate_url() wirft jetzt vor der allow_private_hosts-Pruefung PluginPermissionError fuer solche Literal-Hosts. _resolved_network_target_error() prueft nach DNS-Aufloesung auf diese verbotenen Bereiche selbst bei allow_private_hosts=true und gibt erst danach (nur ohne Flag) den bestehenden private/loopback-Fehler zurueck. Loopback/private LAN-Dienste bleiben mit gesetztem Flag bewusst erreichbar (legitimer Use-Case).
- **[quality] LEXA_PLUGIN_HTTP_GET wird bei jeder Ausfuehrung neu auf das Modul gesetzt (Cross-Call-Ueberschreibung)**:1083
  - Injektion idempotent gemacht: Da jede Plugin-Datei ein eigenes Modul (lexa_plugin_<stem>) erhaelt und module<->info 1:1 ist, wird LEXA_PLUGIN_HTTP_GET nur noch einmal gesetzt (nur wenn None), statt bei jedem Aufruf das geteilte Modul-Global zu mutieren. Zusaetzlich vorwaertskompatible, sauberere Uebergabe: akzeptiert execute() einen http_get-Parameter (per inspect.signature geprueft), wird der Helper explizit als kwarg uebergeben. import inspect ergaenzt. Backward-compat mit web_search.py (liest weiterhin das Modul-Global) bleibt erhalten, da dessen execute(tool_name, args) keinen http_get-Param hat.

## backend/plugins_builtin/web_search.py
- **[bug] Snippet-Zuordnung per Index fuehrt zu falschen Snippets bei den Suchergebnissen**:127
  - _parse_ddg_html ordnete Snippets bisher rein positionell zu (snippets[i] zu links[i]), was bei fehlenden Snippets (Werbe-/Sonderbloecke) alle nachfolgenden Ergebnisse verschob. Statt zwei getrennter findall-Listen wird nun mit link_pattern.finditer ueber die Ergebnis-Links iteriert; fuer jeden Treffer wird der Block vom Ende des aktuellen Links bis zum Anfang des naechsten Links isoliert und das result__snippet NUR innerhalb dieses Blocks gesucht. Dadurch stammen Titel/URL und Snippet immer aus demselben Ergebnis; fehlt ein Snippet, bleibt es leer ('') statt das Snippet eines anderen Treffers zu erben. Erlaeuternder Kommentar (Sprachstil der Datei beibehalten) ergaenzt. Keine Signaturaenderung, keine neuen Abhaengigkeiten.

## backend/proactive.py
- **[bug] Festplatten-Warnung prueft auf Windows den falschen Pfad**:800
  - import os ergaenzt und in _check_disk_warning() shutil.disk_usage("/") durch das explizite Systemlaufwerk ersetzt: system_drive = os.environ.get("SystemDrive", "C:") + os.sep, anschliessend shutil.disk_usage(system_drive). Damit wird auf der Windows-only App zuverlaessig das Systemlaufwerk statt eines unsicheren "/"-Pfads geprueft.
- **[bug] E-Mail-Benachrichtigung verpasst neue Mails nach Nullstand**:732
  - Neues State-Flag self._email_baseline_set in __init__ eingefuehrt. In _check_email_notifications() die Baseline-Erkennung von 'prev == 0' auf 'not self._email_baseline_set' umgestellt (Flag wird beim ersten Check gesetzt). Dadurch werden nach einem Nullstand (alle Mails gelesen) neu eintreffende Mails wieder korrekt als current_count > prev gemeldet statt faelschlich als Baseline verschluckt.
- **[performance] On-Demand-Generierung laeuft ungesperrt parallel zum Background-Loop**:178
  - Dediziertes self._generation_lock (threading.Lock) in __init__ ergaenzt. _generate_suggestions() wrappt nun den Lauf in diesem Lock und prueft bei respect_interval=True das Zeit-Gate (time.time()-self._last_check < self._check_interval) INNERHALB des Locks; die eigentliche Logik wurde in _generate_suggestions_locked() ausgelagert. get_suggestions() ruft _generate_suggestions(context, respect_interval=True) auf. Dadurch laeuft pro Intervall nur genau ein Generierungslauf, doppelte teure Checks (Kalender/Wetter/IMAP) sind ausgeschlossen. Der Background-Loop ruft weiter ohne respect_interval auf und serialisiert ebenfalls ueber das Lock.
- **[improvement] Cooldown-Dedup pro Typ unterdrueckt mehrere Kalender-Erinnerungen**:263
  - Im Cooldown-/Dedup-Filter von _generate_suggestions_locked() eine Ausnahmeliste cooldown_exempt = {"calendar_reminder"} eingefuehrt. Fuer ausgenommene event-spezifische Typen wird der Typ-Cooldown uebersprungen (kein Setzen/Pruefen von self._cooldowns) und der Dedup-Key um s.message erweitert (type+title+message), sodass verschiedene Termine mit gleichem Titel 'Termin in Kuerze' nicht gegenseitig unterdrueckt werden. Wiederholungen desselben Events bleiben durch das bestehende _reminded_events-Set verhindert.

## backend/productivity.py
- **[bug] todo_update meldet Erfolg auch für nicht existierende Todos**:279
  - In todo_update wird das UPDATE-Ergebnis jetzt in `result` gespeichert und nach dem commit geprüft. Bei `result.rowcount == 0` wird `{"error": f"To-Do #{id} nicht gefunden."}` zurückgegeben — konsistent zur Dict-Fehlerform von todo_delete. Der Router erkennt damit fehlende Todos statt fälschlich HTTP 200 zu liefern.
- **[bug] Streak-Berechnung und habit_list verwenden uneinheitliche Erfüllungskriterien**:470
  - _calc_streak_sql joint jetzt habit_logs auf habits und zählt einen Tag nur bei `hl.count >= h.target` als erfüllt (statt `count > 0`). Damit stimmt der Streak mit der today_done/week-Logik aus habit_list überein; bei target>1 wird ein Tag erst ab Erreichen des Ziels gezählt.
- **[quality] Pomodoro-Status-Cache wird ohne gesetzten Timestamp invalidiert**:386
  - Helper _invalidate_pomodoro_cache() eingeführt, der data=None UND ts=0.0 atomar setzt (Aufruf unter _pomodoro_lock). Alle vier bisherigen `_pomodoro_status_cache["data"] = None`-Stellen (pomodoro_start, Timer-Thread, pomodoro_stop, pomodoro_acknowledge) nutzen jetzt diesen Helper. Beseitigt die latente data/ts-Inkonsistenz.
- **[quality] Lokale Variable t überschattet das importierte i18n-t innerhalb pomodoro_start**:363
  - Thread-Variable in pomodoro_start von `t` zu `timer_thread` umbenannt (Erstellung, start(), Zuweisung an _active_pomodoro['thread']). Damit bleibt die modulweite i18n-Funktion t() innerhalb der Funktion aufrufbar; Namenskollision beseitigt.
- **[bug] Pomodoro-DB-Completion markiert per MAX(id) potenziell die falsche Session**:357
  - In pomodoro_start wird der INSERT jetzt über `cursor = db.execute(...)` ausgeführt und `session_id = cursor.lastrowid` als Closure-Variable gemerkt. Der Timer-Thread markiert nun `UPDATE pomodoro_sessions SET completed=1 WHERE id=?` mit dieser konkreten session_id statt MAX(id) — race-sicher bei schnell aufeinanderfolgenden Sessions.
- **[performance] Zeiterfassung kompiliert pro 30s-Tick C# via PowerShell Add-Type**:650
  - _get_active_window komplett auf ctypes (stdlib, keine neue Abhängigkeit) umgestellt: GetForegroundWindow/GetWindowTextW (user32) für Titel und GetWindowThreadProcessId + OpenProcess/QueryFullProcessImageNameW (kernel32) für den Prozessnamen (Basename ohne .exe, analog zu p.ProcessName). Kein Subprozess und kein C#-Recompile pro Tick mehr; Rückgabe-Semantik (app_name, title) und Fallback 'unknown' bleiben erhalten. subprocess-Import bleibt, da weiterhin für ipconfig /flushdns genutzt.

## backend/reminders.py
- **[performance] _fired_reminders wächst bei ausbleibendem Acknowledge unbegrenzt**:444
  - Bestätigt: reminder_check hängt jede gefeuerte Erinnerung an _fired_reminders an, bereinigt wird nur in acknowledge_fired (entfernt acknowledged Einträge). Ohne Frontend-Acknowledge wächst die Liste unbegrenzt. Fix: Konstante _MAX_FIRED_REMINDERS = 200 hinzugefügt und in reminder_check (unter _fired_lock) nach dem Anhängen die Liste hart begrenzt (del _fired_reminders[:-_MAX_FIRED_REMINDERS]), sodass nur die jüngsten 200 Einträge im RAM bleiben. Keine Signaturänderung.
- **[quality] reminder_create akzeptiert vergangene/zu nahe Zeiten ohne sinnvolle Untergrenze**:111
  - Bestätigt: Minuten-Pattern erlaubte \d{1,4} inkl. 0 ('in 0 min' = jetzt), dateparser-Pfad prüft nur result > now, keine Mindest-Vorlaufzeit. Fix 1: Minuten-Pattern auf [1-9]\d{0,3} eingeschränkt (schließt 0 aus). Fix 2: In reminder_create nach dem Parsen geprüft, ob fire_at <= now + 30s; falls ja, klare deutsche Fehlermeldung statt sofortigem Feuern. timedelta ist bereits importiert.

## backend/response_cache.py
- **[bug] Cache-TTL/Max aus Umgebungsvariablen ohne Validierung — ungueltige Werte sprengen Import oder deaktivieren Cache**:14
  - Neue Helper-Funktion _env_int(name, default, minimum=1) eingefuehrt, die LEXA_RESPONSE_CACHE_TTL und LEXA_RESPONSE_CACHE_MAX parst. Nicht-numerische oder leere Werte fuehren nicht mehr zum ValueError beim Import, sondern fallen auf den Default zurueck; das Ergebnis wird per max(minimum, parsed) auf >=1 geklemmt, sodass ein negativer/null TTL den Cache nicht mehr stillschweigend deaktiviert. _CACHE_TTL_SECONDS und _CACHE_MAX_ENTRIES nutzen jetzt diesen Helper.
- **[performance] Cache-Lookup: linearer Scan ueber alle Eintraege bei jedem Cache-Miss**:99
  - In get_cached_chat_response wird der exakte Cache-Key (gleiche sha256-Ableitung aus signature+normalized wie in remember_chat_response) vor der Similarity-Suche per dict-Lookup geprueft (Fast-Path) und bei Treffer mit similarity=1.0 direkt zurueckgegeben — kein O(n)-Scan und keine SequenceMatcher-Berechnungen mehr fuer wiederholte identische Fragen. Zusaetzlich bricht die Similarity-Schleife bei score>=1.0 sofort ab. Die Expiry-Bereinigung laeuft weiterhin zuerst, sodass der Fast-Path keine abgelaufenen Eintraege zurueckgibt. Oeffentliche Signatur unveraendert.

## backend/router_agent.py
- **[performance] Shielded-Save-Retry-Schleife kann bei wiederholter Cancellation eng pollen / haengen**:314
  - Die while-True-Schleife im finally von event_stream() wurde durch eine begrenzte for-Schleife (max. 3 Versuche) ersetzt. Jeder Versuch wartet jetzt via asyncio.wait_for(asyncio.shield(save_task), timeout=5.0) statt blank await asyncio.shield(...). Dadurch kann (a) ein haengender DB-Write (z.B. SQLite-Lock) den Stream-Cleanup nicht mehr endlos blockieren und (b) wiederholtes CancelledError-Re-Raise keinen engen Busy-Loop ohne Pause mehr erzeugen, da jedes await durch das Timeout zeitlich begrenzt ist. Original-Intent bleibt erhalten: bei Cancellation wird cancelled=True gesetzt und der Save NICHT abgebrochen (Shield bleibt). Nach Erschoepfung der Versuche wird per else-Zweig geloggt und der Cleanup fortgesetzt, ohne den noch laufenden Save-Task zu canceln. Die abschliessende CancelledError-Propagation (Zeile 349-351) bleibt unveraendert.

## backend/router_chat.py
- **[bug] Tool-Pattern-Text wird faelschlich in den Response-Cache geschrieben**:1841
  - Vor remember_chat_response im event_stream-Textzweig wird jetzt geprueft, dass full_text kein Tool-Aufruf-Muster enthaelt (neuer Helper _looks_like_text_tool_call mit den modulweiten _STREAM_TOOL_CALL_PATTERNS). Halb-kaputte Modellantworten wie 'weather_current(...)' oder 'Fuehre X aus', fuer die kein echtes Tool gefunden wurde, werden damit nicht mehr gecacht.
- **[bug] Bei Stream-Abbruch (CancelledError) geht der bereits gestreamte Text fuer die History verloren**:1853
  - Neues Flag history_saved markiert, ob full_text bereits persistiert wurde (gesetzt im Standard-Textzweig). Im except asyncio.CancelledError-Block wird vorhandener, noch nicht gespeicherter full_text unter _history_lock via asyncio.shield in conversation_history geschrieben, danach wird CancelledError korrekt re-raised (statt nur geloggt), damit das finally-Cleanup laeuft.
- **[bug] gen.close() im finally kann mit laufendem run_in_executor-Future kollidieren**:1727
  - Das run_in_executor-Future wird jetzt in chunk_future referenziert. Im finally wird, falls chunk_future noch nicht done ist, via asyncio.shield(chunk_future) auf dessen Abschluss gewartet, bevor gen.close() aufgerufen wird; gen.close() ist zusaetzlich in try/except gekapselt. Damit wird 'generator already executing' beim Schliessen eines noch im Worker-Thread iterierten Generators vermieden.
- **[bug] Fallback-Tool-Detektor matcht generisches Muster '(\w+)\(.*?\)' und kann Text als Tool-Aufruf fehlinterpretieren**:1779
  - Das generische Muster wurde auf ein verankertes, eigenstaendiges Token verengt: '(?m)^\s*(\w+)\([^()]*\)\s*$' (nur am Zeilenanfang, flache Argumentliste, Zeile besteht praktisch nur aus dem Aufruf). Prosa wie 'max(3, 5)', 'print(x)' oder 'Punkt (a)' wird dadurch nicht mehr als Tool-Aufruf interpretiert. Die expliziten 'Fuehre X aus'/'Rufe X auf'-Muster blieben unveraendert. Muster sind jetzt modulweit als _STREAM_TOOL_CALL_PATTERNS definiert und werden vom Fallback-Detektor und vom Cache-Guard gemeinsam genutzt.
- **[quality] History-Marker fuer Tool-Calls nutzt Umlaut trotz ASCII-Fold-Konvention des Files**:1763
  - Der History-Marker wurde von 'wartet auf Bestätigung' auf das ASCII-gefaltete 'wartet auf Bestaetigung' geaendert, konsistent mit dem restlichen File und den ASCII-basierten Folge-/Marker-Erkennern.
- **[quality] Dict-zu-Text-Fallback erzeugt unkontrollierte 'key: value'-Dumps in der Chat-Antwort**:1256
  - In /chat (lokaler Intent-Exec) und /chat/stream (lokaler Intent-Exec) wurde der generische ', '.join(f'{k}: {v}' ...)-Dump des kompletten data-Dicts entfernt. Es werden nur noch die expliziten user-facing Felder data['summary'] bzw. data['message'] verwendet, sonst Fallback auf die bereits sichere local-intent-Nachricht reply_msg. Damit koennen keine internen Feldnamen/Rohwerte/Pfade mehr ungewollt in die Chat-Antwort gelangen.

## backend/router_companion.py
- **[bug] Batch dry_run-Eintraege zaehlen als 'executed', obwohl nichts ausgefuehrt wurde**:403
  - In execute_batch einen separaten Zaehler executed_count eingefuehrt, der nur bei tatsaechlicher Ausfuehrung (nach erfolgreichem companion.execute) erhoeht wird. Der Rueckgabewert nutzt jetzt 'executed': executed_count statt len(results), sodass dry_run-Eintraege (die via continue uebersprungen werden) nicht mehr faelschlich als ausgefuehrt gezaehlt werden. Den bewussten continue-Sprung im dry_run-Zweig zusaetzlich mit einem erklaerenden Kommentar abgesichert. Kein Test prueft den konkreten Wert von 'executed', daher keine Regression.
- **[improvement] /companion/plugins gibt internes _loaded_plugins-Dict direkt nach aussen**:269
  - list_plugins_endpoint reicht nicht mehr das private companion._loaded_plugins direkt durch, sondern baut eine defensive, stabile Projektion: dict comprehension {str(cmd): str(name) ...} ueber getattr(companion, '_loaded_plugins', {}) or {}. Da info['plugin'] laut plugin_loader.py bereits ein String (display_name) ist, bleibt das Antwortschema (command_name -> plugin_name) unveraendert und JSON-serialisierbar; die API wird aber von der internen Objektidentitaet/Struktur entkoppelt und gegen None/fehlendes Attribut abgesichert. Eine echte oeffentliche Engine-Methode waere sauberer, ist aber nicht moeglich, da nur diese Datei geaendert werden darf.

## backend/router_context.py
- **[performance] clipboard_history-Endpoint ruft blocking get_history() ohne asyncio.to_thread im Event-Loop**:194
  - In clipboard_history() wurde der direkte Aufruf clipboard_intel.get_history() durch await asyncio.to_thread(clipboard_intel.get_history) ersetzt. Verifiziert in integrations.py: get_history() haelt self._lock (Zeile 491-494), greift also auf gemeinsamen Zustand zu. Damit ist der Endpoint jetzt konsistent zu allen uebrigen Endpoints der Datei und blockiert den Event-Loop bei Lock-Contention nicht mehr.
- **[security] GET /context/clipboard ohne Rate-Limiting trotz PowerShell-Prozess-Spawn**:62
  - check_rate_limit('execute') (mit HTTP 429 bei Ueberschreitung) wurde nach dem Muster von GET /context allen prozess-/subprocess-spawnenden bzw. Win32-aufrufenden Endpoints vorangestellt: /context/active-window, /context/clipboard, /context/apps, /context/idle, /context/recent-files, /context/project, /context/browser/tabs, /context/browser/current, /context/spotify und POST /context/spotify/control. Real bestaetigt: analyze_clipboard (integrations.py:553-566) und get_active_window (context_monitor.py:189-194) spawnen PowerShell via subprocess.run; ohne Limit konnten Renderer-Bugs diese in Schleife aufrufen. /context/history bleibt bewusst ohne Limit, da rein In-Memory ohne Prozess-Spawn.

## backend/router_conversations.py
- **[security] Conversation-CRUD-Endpoints ohne Rate-Limiting**:40
  - check_rate_limit aus backend.security importiert und an den vier schreibenden/zustandsändernden Endpoints (create_conversation, update_conversation, delete_conversation, load_conversation) jeweils am Funktionsanfang einen Guard 'if not check_rate_limit("execute"): raise HTTPException(status_code=429, detail="Rate limit exceeded")' ergänzt — exakt nach dem Muster aus router_context.get_context. Lesende Endpoints (list/get) bleiben unverändert, da der Fund explizit auf die schreibenden Pfade (DB-Flut, große Message-Blobs) abzielt. check_rate_limit ist synchron und gibt bool zurück; Import ist konsistent mit router_context/router_chat und führt zu keinem Zirkelimport.

## backend/router_embeddings.py
- **[bug] _reindex_running ist nicht thread-/task-safe (Race beim Start)**:18
  - Ein modulweites asyncio.Lock (_reindex_lock) eingefuehrt und die Pruefung/das Setzen von _reindex_running im reindex_embeddings-Endpoint in 'async with _reindex_lock:' gekapselt, sodass Check und Set atomar als Compare-and-Set ablaufen. Damit koennen zwei parallele POST /memory/embeddings/reindex-Requests nicht mehr beide die Pruefung passieren und zwei konkurrierende Reindex-Tasks starten. Hinweis: In reinem asyncio liegt zwischen Pruefung und Setzen kein await, der Code war also schon weitgehend atomar; der Lock macht die Absicht explizit und haertet gegen kuenftige Refactorings/Thread-Aufrufe. Minimaler lokaler Diff, oeffentliche Signaturen unveraendert, keine neuen Abhaengigkeiten.

## backend/router_hermes.py
- **[security] Mehrere Hermes-GET-Endpoints ohne Rate-Limiting**:458
  - check_rate_limit('execute') mit 429-Antwort in hermes_status (Z.458), hermes_telegram_status (Z.603), hermes_gateway_autostart_status GET (Z.618) und hermes_gateway_logs (Z.624) ergänzt, konsistent zu den übrigen Routen. Damit ist auch der CPU/IO-intensive Log-Endpoint (bis 1000 Zeilen + Regex) gedeckelt.
- **[performance] _safe_draft_queue ruft hartkodiert maxDrafts=50, ignoriert Overview-Kontext**:246
  - In _safe_draft_queue() den Aufruf von list_hermes_os_drafts('all', 50, True) auf maxDrafts=1 reduziert, da build_hermes_overview nur drafts.get('counts') verwendet. Die counts werden vom MCP-Tool unabhängig von maxDrafts geliefert; es werden nun nicht mehr bis zu 50 Drafts über die MCP-Brücke gelesen, kompaktiert und verworfen.
- **[bug] Overview-Healthcheck schliesst den 'drafts'-Check doppelt/inkonsistent ein (magischer Slice checks[:6])**:312
  - Magischen Slice checks[:6] durch all(check['state']=='ok' for check in checks) ersetzt. Da der drafts-Check (Index 6) seinen state via _state(drafts_ok) bereits exakt spiegelt, bleibt das Ergebnis identisch, aber ein künftig angehängter 8. Check würde nicht mehr stillschweigend ignoriert. Das verbleibende 'and drafts_ok' ist nun redundant-explizit und harmlos.
- **[improvement] _safe_to_thread maskiert nicht-dict-Resultate still ohne Log**:233
  - Bei nicht-dict-Resultat wird vor der Rückgabe von {} jetzt eine logger.warning mit Funktionsname und Typname ausgegeben, sodass stille Vertragsverletzungen (None/Liste statt dict) sichtbar werden. Öffentliche Signatur und Rückgabewert unverändert. **kwargs bewusst nicht ergänzt, da die Funktion nur mit Positionsargumenten aufgerufen wird und die Sonderbehandlung von build_obsidian_context_payload ein separater Pfad bleibt (kein riskanter Umbau).

## backend/router_mcp.py
- **[quality] Doppeltes Timeout beim MCP-Tool-Aufruf**:168
  - Das aeussere asyncio.wait_for(..., timeout=MCP_CALL_TIMEOUT) in call_server_tool() wurde entfernt; der Aufruf lautet jetzt schlicht 'await mcp_registry.call_tool(name, req.tool, req.arguments)'. Verifiziert am echten Code, dass das innere Timeout vollstaendig greift: mcp_registry.call_tool delegiert direkt an client.call_tool -> _send_request (mcp_client.py:308-337), das bereits mit timeout=MCP_CALL_TIMEOUT arbeitet UND bei TimeoutError self._pending.pop(req_id) ausfuehrt und MCPError wirft. Damit existiert nur noch eine Timeout-Ebene, der Race um den verwaisten _pending-Eintrag entfaellt. Der nun unerreichbare 'except asyncio.TimeoutError'-Zweig (HTTP 504) wurde entfernt; das innere MCPError-Timeout wird vom bestehenden 'except MCPError' sauber auf eine client-sichere 502-Antwort abgebildet. Den dadurch ungenutzten Import MCP_CALL_TIMEOUT aus backend.config entfernt (nur noch in einem erklaerenden Kommentar erwaehnt). Ein erklaerender Kommentar dokumentiert die bewusste Single-Timeout-Entscheidung. asyncio-Import bleibt (weiterhin fuer asyncio.to_thread in list_servers genutzt).

## backend/router_os_agents.py
- **[bug] sanitize_input kuerzt OS-Agent-Instructions still auf 2000 Zeichen (Datenverlust)**:73
  - Bestaetigt am Code: OSAgentTaskRequest.instructions erlaubte Field(max_length=8000), aber os_task_start ruft sanitize_input(req.instructions) auf, und sanitize_input (security.py:311-312) kappt hart auf 2000 Zeichen ohne Parameter. Da security.py nicht geaendert werden darf und sanitize_input projektweit nur einargumentig aufgerufen wird (keine max_chars-Option vorhanden), wurde das Field-Limit konsistent gesenkt: instructions max_length von 8000 auf 2000. Dadurch liefert die API fuer >2000 Zeichen jetzt einen klaren 422-Validierungsfehler statt stiller Abschneidung (kein unbemerkter Datenverlust mehr). Erlaeuternder DE-Kommentar ergaenzt. start_os_agent_task kappt zwar noch auf 8000, sieht aber nie mehr als 2000 Zeichen.

## backend/router_personal_os.py
- **[bug] Falscher Personal-OS-Root-Fallback bricht alle Raw-Inbox-/Draft-/SDK-Endpoints**:248
  - Bestätigt: parents[3]/'OS' = 'OneDrive - Office/OS' existiert nicht, während parents[1]/'personal_os' (= PROJECT_ROOT/personal_os) OS_MANIFEST.md sowie die Worker-/SDK-Verzeichnisse enthält. _personal_os_root() neu an hermes_adapter._resolve_personal_os_root angeglichen: zuerst LEXA_PERSONAL_OS_ROOT/PERSONAL_OS_ROOT (Env), dann project-lokales personal_os mit OS_MANIFEST.md-Prüfung, dann _mcp_personal_os_root_candidate() (liest mcp_servers.json), sonst project_link. Zwei Helfer (_has_personal_os_manifest, _mcp_personal_os_root_candidate) lokal ergänzt statt hermes_adapter zu importieren (vermeidet schwere Importe/Zyklen). Der defekte parents[3]/'OS'-Fallback wurde entfernt.
- **[performance] Blockierender mcp_registry.load_config() im async-Pfad**:177
  - Bestätigt: load_config() ist synchron (mcp_registry.py:184) und liest/parst mcp_servers.json, lief aber direkt im Event-Loop. Aufruf in 'await asyncio.to_thread(mcp_registry.load_config)' gekapselt, konsistent zu main.py und router_mcp.py.
- **[quality] raw_inbox_extract: AI-Engine-Aufruf maskiert alle Fehlerklassen als generisches 502**:1717
  - Der breite try/except wurde aufgeteilt: Import-/Konfigurationsfehler (AI-Engine nicht verfügbar, kein Modell-Meta) liefern jetzt 503 'Lexa AI engine is not available'; Provider-Laufzeitfehler liefern 502 'Lexa extraction provider call failed' (mit Exception-Typ im Log via type(exc).__name__, ohne Internas zu leaken). Der 'kein Ergebnis'-Pfad (None-Client bei fehlendem API-Key) gibt einen klaren 502-Hinweis auf die Provider-Konfiguration. Der bestehende _parse_extraction-ValueError-Pfad bleibt unverändert.
- **[quality] Raw-Inbox-Worker-Status verlangt gebautes dist/index.js, prueft aber nur Verzeichnis**:358
  - In _run_raw_inbox_worker_status UND _run_raw_inbox_worker (beide im Finding genannt) wird nun vor dem node-Aufruf zusätzlich (worker_dir/'dist'/'index.js').exists() geprüft. Bei fehlendem Build kommt eine klare 502-Meldung 'Raw inbox worker nicht gebaut: npm run build im raw-inbox-worker ausfuehren.' statt eines kryptischen Node-MODULE_NOT_FOUND-Stacktraces. Deutsche Kommentar-/Meldungssprache passend zur Datei.
- **[quality] raw_inbox_submit: kein Cleanup der geschriebenen Datei bei Worker-Fehler**:1689
  - _write_raw_inbox_file gibt nun zusätzlich 'absolutePath' zurück. In raw_inbox_submit wird absolutePath aus dem Antwort-Dict entfernt (nicht geleakt) und der _run_raw_inbox_worker-Aufruf in try/except HTTPException gekapselt: bei Worker-Fehler (Build fehlt, Timeout 504, Verarbeitungsfehler 502) wird die soeben geschriebene Rohdatei via asyncio.to_thread(Path(absolute_path).unlink, True) (missing_ok=True) gelöscht und der Fehler weitergereicht. Damit bleibt kein unverarbeiteter Rest im inkonsistenten Zustand zurück.

## backend/router_productivity.py
- **[quality] Tote status_code-Annahme bei Fehler-Dicts — Not-Found wird als 400 ausgeliefert**:111
  - Bestätigt: Die productivity-Funktionen (todo_update Z.292, todo_delete Z.302, todo_complete, habit_log Z.533, habit_delete Z.614) geben Not-Found als dict {"error": "... nicht gefunden."} OHNE status_code-Schlüssel zurück. Der alte dict-Pfad lieferte daher immer 400, und der nachgelagerte 'nicht gefunden'-Textcheck war für Dicts toter Code (unerreichbar). Neue Hilfsfunktion _result_response(result) eingeführt (nach _validate_todo_fields): Sie wertet bei Dicts den 'error'-Text aus und liefert 404 bei 'nicht gefunden' (sonst 400), respektiert weiterhin einen optionalen status_code-Schlüssel für Vorwärtskompatibilität, und behält den Str-Pfad (404 bei 'nicht gefunden', sonst {"status": result}) bei. Alle fünf Endpoints (update_todo, delete_todo, complete_todo, log_habit, delete_habit) nutzen jetzt diesen Helper statt der duplizierten Inline-Logik. Hinweis: Die alte 'id fehlt'->404-Sonderbehandlung wurde nicht übernommen, da 'ID fehlt.' nur als dict (Validierungsfehler) auftritt und korrekt 400 ist; zudem ist der Pfad über die int-typisierten Path-Params praktisch unerreichbar.

## backend/router_search.py
- **[security] Such-Endpoints ohne Rate-Limiting**:19
  - Real bestätigt: keiner der vier Endpoints rief check_rate_limit auf. Import von check_rate_limit und get_rate_limit_info aus backend.security ergänzt. Jeder Handler (/search, /search/fts, /memory/rebuild-fts, /conversations/{id}/export) prüft nun zu Beginn check_rate_limit('chat') und wirft bei Überschreitung 429 mit Standard-Headern (X-RateLimit-Limit/-Remaining, Retry-After). Es wurde das bestehende 'chat'-Bucket genutzt (kein neues Config-/security.py-Bucket, da das andere Dateien berühren würde); 429-Aufbau via lokalem Helper _rate_limited() konsistent zu router_chat.py.
- **[performance] rebuild_fts ohne Rate-Limit und ohne Schutz vor parallelen Rebuilds**:42
  - Real bestätigt: rebuild_fts war ungeschützt. Modulweiter asyncio.Lock (_rebuild_lock) eingeführt. rebuild_fts prüft nun zuerst check_rate_limit('chat') (429), dann _rebuild_lock.locked() → bei laufendem Rebuild 409 'FTS-Rebuild läuft bereits.', und führt den eigentlichen Rebuild unter 'async with _rebuild_lock' aus, sodass nur ein Rebuild gleichzeitig läuft. Die locked()-Prüfung und das Acquire sind durch kein await getrennt und im Single-Thread-Eventloop daher atomar (kein TOCTOU). Vermeidet 'database is locked'-Contention auf der SQLite-DB.

## backend/router_smart.py
- **[bug] _build_insights_prompt crasht bei fehlendem 'count'/'hour'-Schluessel in Aktivitaetsdaten**:252
  - In _build_insights_prompt (Zeilen 252-263) alle direkten Dict-Zugriffe auf optionale Schluessel durch .get() mit Defaults ersetzt: sort-Lambdas nutzen x.get('count', 0); die f-Strings nutzen h.get('hour','?'), h.get('count',0), d.get('name','?'), d.get('count',0), c.get('command','?'), c.get('count',0), a.get('name','?'), a.get('frequency',0). Unvollstaendige Datensaetze aus sm.get_insights_data() loesen damit keinen KeyError mehr aus und brechen die AI-Insight-Erzeugung nicht mehr ab.
- **[performance] /insights cached AI-Ergebnis nicht — teure Daten-Aggregation + AI-Call pro Aufruf**:195
  - Selbst-enthaltenen Kurzzeit-Cache fuer den /insights-Endpoint ergaenzt (nur in dieser Datei, keine neuen Abhaengigkeiten): Modul-Konstante _INSIGHTS_CACHE_TTL=120.0 plus _insights_cache/_insights_cache_ts. import time hinzugefuegt. get_insights gibt innerhalb der TTL das gecachte Ergebnis zurueck (vor Daten-Aggregation und AI-Call) und speichert jedes berechnete Ergebnis ueber eine lokale _cache()-Hilfsfunktion an allen Return-Pfaden. Rate-Limit-Check bleibt vor dem Cache-Treffer erhalten. Wiederholte Aufrufe loesen damit nicht mehr alle 2s die teure Aggregation + AI-Engine aus.
- **[improvement] DELETE /profile/{key:path} ohne Laengen-/Inhaltsvalidierung des Keys**:63
  - delete_profile_entry konsistent zu update_profile/manual_learn gemacht: key wird einmal getrimmt, bei leerem key weiterhin 400, zusaetzlich Laengenpruefung len(key) > 200 mit 400 ('Key zu lang (max 200 Zeichen).'); der getrimmte key wird an sm.profile_delete uebergeben. Ein striktes Zeichenmuster wurde bewusst NICHT erzwungen, da Profil-Keys legitim Punkte/Slashes (z.B. 'preferences....') enthalten und ein restriktives Regex valide Keys brechen koennte — die Laengenpruefung deckt das im Fund genannte Hauptrisiko (ueberlange Keys) ab.

## backend/router_stripe.py
- **[security] Webhook gibt HTTP 200 zurueck, wenn Webhook-Secret nicht konfiguriert ist**:660
  - Bei fehlendem STRIPE_WEBHOOK_SECRET wird jetzt Status 503 statt 200 zurueckgegeben, damit Stripe die Zustellung erneut versucht und Abo-Events nicht still verloren gehen. Kommentar ergaenzt. Den vorgeschlagenen harten Startup-Fail habe ich nicht ergaenzt, da dieser ausserhalb dieser Datei (App-Registrierung/Lifespan) liegt.
- **[bug] _extract_plan_name faellt bei unbekanntem Plan auf 'pro' zurueck statt 'free'**:782
  - Fallback von 'pro' auf 'free' geaendert (sowohl bei Exception als auch bei nicht erkennbarem Label), plus Warn-Log mit price_id und Label bei nicht klassifizierbarem Plan. Verhindert unbezahlte Feature-Freischaltung durch Fallback. Die vorgeschlagene Umstellung der Klassifizierung primaer auf price_id habe ich bewusst minimal gehalten (Nickname/Produkt-Heuristik bleibt), da eine price_id->plan-Mapping-Tabelle eine groessere, riskantere Aenderung waere.
- **[bug] current_period_start/end basieren auf veralteten Stripe-Subscription-Top-Level-Feldern**:354
  - Neue Helfer _subscription_field/_subscription_first_item/_subscription_period_value eingefuehrt. _subscription_period_start/-end lesen nun zuerst das item-level Feld (subscription['items']['data'][0]['current_period_*'], Stripe API 2025-*) und fallen auf das Top-Level-Feld zurueck. Installierte Stripe-SDK 14.4.1 bestaetigt neue API-Generation. Stripe-API-Version habe ich NICHT gepinnt, da stripe.api_version ein globaler Prozess-State waere, der andere Module betreffen koennte (dateiuebergreifend).
- **[quality] SQLite-Pfad speichert current_period_start/price_id/cancel_at_period_end nicht — inkonsistent zu Supabase**:416
  - SQLite-Schema um price_id, current_period_start, cancel_at_period_end erweitert; idempotente Migration via PRAGMA table_info + ALTER TABLE ADD COLUMN fuer bestehende DBs ergaenzt. _sqlite_upsert_subscription und _sqlite_update_subscription schreiben die drei Felder nun mit. Beide Backends fuehren damit denselben Datenstand; _subscription_period_start ist im SQLite-Modus nicht mehr toter Code.
- **[bug] Webhook quittiert Handler-Fehler mit 200 — verlorene Events ohne Retry**:683
  - Bei Handler-Exceptions wird jetzt Status 500 statt 200 zurueckgegeben, sodass Stripe das Event gemaess Retry-Policy erneut zustellt. Idempotenz ist durch ON CONFLICT/upsert gegeben. Kommentar ergaenzt.
- **[security] checkout.session.completed verifiziert client_reference_id nicht gegen Stripe-Customer/Subscription**:701
  - In _handle_checkout_completed nach stripe.Subscription.retrieve eine Defense-in-Depth-Pruefung ergaenzt: Wenn der Customer der abgerufenen Subscription vom Customer der Session abweicht, wird das Event verworfen, error-geloggt und per audit_log('customer_mismatch') alarmiert. Der optionale metadata.user_id-Abgleich auf dem Customer wurde weggelassen, da er einen zusaetzlichen Stripe-API-Call (Customer.retrieve) erfordern wuerde; der customer-Abgleich deckt das Hauptrisiko ab.
- **[bug] get_subscription/validate_license greifen mit ['key'] auf Supabase-Rows zu — KeyError bei Schema-Drift**:805
  - In get_subscription und validate_license Index-Zugriff result['plan']/['status']/['current_period_end'] durch result.get(..., default) ersetzt (Defaults 'free'/'inactive'/None). Fehlende Felder liefern nun robuste Defaults statt 500/KeyError.
- **[improvement] License-Validierung behandelt 'past_due'/'unpaid' wie inaktiv ohne differenzierte Rueckmeldung**:878
  - validate_license liefert bei status past_due/unpaid zusaetzlich reason='payment_failed' im Response (nicht-breaking; Frontend liest weiterhin valid/expires, Feld main.js ist additiv). Differenziert Zahlungsproblem von ungueltigem/abgelaufenem Key. Eine Kulanzperiode (past_due als valid behandeln) habe ich bewusst NICHT umgesetzt, da das eine Policy-Entscheidung mit Sicherheitsbezug ist (Zugriff trotz ausstehender Zahlung).

## backend/router_vision.py
- **[performance] analyze-file liest gesamten Upload in den Speicher, bevor die Groesse geprueft wird**:214
  - In analyze_uploaded_file wird der Upload nun chunkweise gelesen (await file.read(_UPLOAD_CHUNK_SIZE) in einer Schleife) und bei Ueberschreiten von 10MB sofort mit 413 abgebrochen, analog zum STT-Endpoint in router_voice.py. Zwei Modul-Konstanten _MAX_UPLOAD_SIZE (10MB) und _UPLOAD_CHUNK_SIZE (64KB) wurden ergaenzt. Damit wird kein ueberdimensionierter Body mehr vollstaendig in den RAM geladen, bevor die Ablehnung erfolgt. Die Leere-Datei-Pruefung (400) und die nachfolgende Signatur-Pruefung bleiben unveraendert.

## backend/router_voice.py
- **[performance] WebSocket-Heartbeat-Task wird bei Disconnect nicht zuverlaessig abgebrochen (Task-Leak)**:462
  - _consume()/_heartbeat() werden jetzt als explizite Tasks via asyncio.ensure_future erzeugt. Im finally-Block werden beide Tasks gecancelt und mit asyncio.gather(*tasks, return_exceptions=True) abgewartet. Damit endet die jeweils noch laufende Coroutine (z.B. der Heartbeat) sicher, statt auf einen geschlossenen Socket weiterzuschreiben bzw. an queue.get() haengenzubleiben.
- **[bug] is_listening-Zugriff im Detector-Lock kann AttributeError werfen**:929
  - In wakeword_start (Z.929) und conversation_start (Z.1007) wurde der direkte Zugriff _wake_detector.is_listening durch getattr(_wake_detector, 'is_listening', False) ersetzt, konsistent zum getattr/hasattr-Schutz im restlichen Code. Die except-Pfade setzen _wake_detector bereits auf None zurueck (inkl. defensivem stop() in den Not-ready-Zweigen), das wurde unveraendert belassen.
- **[performance] _last_poll_times waechst bis zur Bereinigung unbegrenzt und nutzt ungepruefte client_id**:971
  - wakeword_events begrenzt client_id jetzt per Regex auf [A-Za-z0-9_-] und max. 64 Zeichen (Fallback 'default'). Die Bereinigung veralteter Eintraege (>300s) laeuft nun bei JEDEM Aufruf, nicht erst ab >100 Eintraegen. Zusaetzlich wird ein hartes Gesamtlimit erzwungen: ist _MAX_POLL_CLIENTS erreicht und kommt eine neue ID, wird der aelteste Eintrag verdraengt. Throttle-Reihenfolge beibehalten.
- **[bug] TEMP_DIR.mkdir nutzt kein parents=True und Limit-/Timeout-Zweige loeschen Temp-Datei doppelt**:102
  - TEMP_DIR.mkdir(exist_ok=True) zu TEMP_DIR.mkdir(parents=True, exist_ok=True) geaendert. Die frueh aufgerufenen audio_path.unlink(missing_ok=True) in den Timeout- (408) und Groessenlimit-Zweigen (413) wurden entfernt; das Aufraeumen uebernimmt ausschliesslich der bestehende finally-Block (unconditionales unlink). Damit gibt es keinen doppelten Loeschpfad mehr, die Datei wird weiterhin in allen Faellen entfernt.
- **[improvement] TTS-Endpoint prueft Rate-Limit erst nach Textlaengen-Validierung — Inkonsistenz**:656
  - In text_to_speech wurde check_rate_limit('voice') (429) an den Anfang des Handlers gezogen, vor die Text-/Laengenvalidierung. Damit zaehlen auch leere/zu lange Requests ins Voice-Rate-Limit, konsistent zu speech_to_text und den uebrigen Endpoints.

## backend/scheduler.py
- **[bug] Scheduler schließt die thread-lokale Memory-DB-Verbindung**:173
  - Die drei db.close()-Aufrufe in den finally-Blöcken von _run_routine, _scheduler_loop und get_scheduler_status wurden entfernt. memory._get_db() liefert eine bewusst thread-lokal gecachte Verbindung, die laut memory.py NICHT geschlossen werden darf (close_db() wäre der korrekte Weg, ist hier aber nicht gewünscht). Die try/finally-Wrapper, die nur dem Schließen dienten, wurden entsprechend aufgelöst; die Verbindung persistiert nun wie in reminders.py/productivity.py. Kurze deutsche Kommentare ergänzt. Real bestätigt: memory.py dokumentiert 'The connection is NOT closed — it persists in thread-local storage'.
- **[bug] Intervall-Routinen: Sekunden-Intervalle wirkungslos, Modulo-Logik unzuverlässig**:91
  - interval-Zweig in _parse_schedule überarbeitet. Da der Loop effektiv nur minütlich tickt und last_run minutengenau dedupliziert, wird nun ausdrücklich nur Minuten-Granularität unterstützt: 'interval:Xs' < 60s oder kein Vielfaches von 60s wird abgelehnt (return False + Warn-Log) statt still auf 1 Minute aufgerundet zu werden; gültige Sekundenwerte werden ganzzahlig in Minuten umgerechnet, interval_minutes < 1 wird abgewiesen. Klarer deutscher Kommentar zur Granularität ergänzt. Echte Sub-Minuten-Unterstützung (Timestamp pro Routine) wäre architekturübergreifend und wurde bewusst nicht erzwungen; stattdessen die vom fix-Feld angebotene Validierungs-/Dokumentationsvariante umgesetzt.
- **[quality] Routine-Schedule 'Mo,Mi,Fr' ignoriert ungültige Tageskürzel still**:77
  - Im Listen-Format-Zweig von _parse_schedule werden ungültige Tageskürzel jetzt erkannt und per logger.warning gemeldet (mit Kürzel-Liste und Schedule-String), bevor die gültigen Kürzel zu Wochentag-Nummern gemappt werden. day_nums enthält keine None-Werte mehr. Strikte Ablehnung beim Anlegen/Speichern liegt außerhalb dieser Datei (anderer Router) und wurde nicht hier erzwungen; die in dieser Datei mögliche Sichtbarmachung per Log ist umgesetzt.

## backend/security.py
- **[security] Pfad-Traversal-Prüfung greift auf rohen String statt auf den aufgelösten Pfad**:373
  - Den naiven Substring-Check 'if ".." in path_str' ersetzt durch '".." in Path(path_str).parts', sodass nur echte Pfad-Segmente ('..') als Traversal gelten und Dateinamen mit '..' als Teilstring (z.B. 'release..notes.txt') nicht mehr fälschlich blockiert werden. Die Traversal-Prüfung läuft jetzt vor der BLOCKED_PATHS-Prüfung. Die im Finding zusätzlich geforderte Sandbox-Root-Eingrenzung (resolve+relative_to gegen erlaubte Wurzel) wurde NICHT umgesetzt, da validate_path projektweit mit beliebigen Nutzerpfaden aufgerufen wird und kein einheitlicher erlaubter Root existiert — das wäre dateiübergreifend riskant. Der Falsch-Positiv-Teil und der Whitespace-Bypass sind behoben.
- **[security] _BLOCKED_HOSTS enthält Duplikate und deckt IPv6-Metadaten-Hosts nicht ab**:384
  - Die dreifach vorhandene IP 169.254.169.254 (AWS/DigitalOcean/Oracle) zu einem einzigen, kommentierten Eintrag zusammengeführt; Kommentare präzisiert, dass IP-Literale ohnehin von is_dangerous_network_ip abgedeckt werden und die benannte Host-Liste nur für Nicht-IP-Sonderfälle (metadata.google.internal) gepflegt wird. Funktional unverändert (Set dedupliziert ohnehin), aber Konsistenz/Lesbarkeit hergestellt.
- **[performance] is_command_allowed normalisiert die gesamte Whitelist bei jedem Aufruf neu**:151
  - Neue Hilfsfunktion _normalize_whitelist_sets() berechnet die drei Tiers einmalig als lower-cased frozensets; Ergebnis im Modul-Cache _whitelist_sets gespeichert und in reload_whitelist() neu befüllt. is_command_allowed prüft jetzt nur noch O(1)-Set-Membership statt drei List-Comprehensions pro Aufruf. .get()-Zugriffe machen die Funktion zudem robust gegen fehlende Whitelist-Schlüssel.
- **[improvement] audit_log spiegelt die fertige Audit-Zeile zusätzlich über den Standard-Logger (zweite Senke ohne Rotation)**:695
  - logger.info(entry.strip()) auf logger.debug(...) herabgestuft, mit erklärendem Kommentar. Die (bereits redigierten) Audit-Zeilen landen damit standardmäßig nicht mehr im allgemeinen Anwendungslog/stdout (anderes Retention/Rotation), audit.log bleibt die autoritative, rotierte Senke. Bei Bedarf via Debug-Level weiterhin sichtbar.

## backend/smart_memory.py
- **[bug] Konfligierende user_profile-Tabellenschemata in derselben DB-Datei**:74
  - Real bestätigt: backend/memory.py und backend/memory_core/schema.py erstellen in derselben lexa_memory.db (LEXA_DATA_DIR/'lexa_memory.db') ebenfalls 'user_profile' per CREATE TABLE IF NOT EXISTS, aber OHNE Spalte 'confidence'. Initialisiert schema.py/memory.py zuerst, brechen alle confidence-Queries in smart_memory.py mit 'no such column: confidence'. memory.py ist zudem das aktiv ans LLM/Router verdrahtete Modul. Da smart_memory.py vollständig eigene, in sich geschlossene Accessoren (profile_get/set/delete/get_all) hat und Aufrufer (router_smart.py, proactive.py) nur die Funktionsnamen — nie den Tabellennamen — nutzen, habe ich die SQLite-Tabelle in smart_user_profile umbenannt. Geänderte Stellen: CREATE-Statement (Z.74) sowie alle 5 Query-Referenzen in profile_get, profile_get_all, profile_set (inkl. MAX()-Ausdruck im ON CONFLICT), profile_delete und get_stats. Keine öffentliche Signatur verändert, rein dateilokal.
- **[performance] interaction_log wächst unbegrenzt — keine Cleanup-Logik**:81
  - Real bestätigt: learn_from_interaction() INSERTet bei jedem Aufruf in interaction_log, ohne je zu bereinigen. Nach dem identischen Projektmuster aus memory.py (auto_remember, Zeile 1044-1059) einen thread-sicheren Modul-Counter (_interaction_counter unter _maintenance_lock) ergänzt. Nur alle 50 Interaktionen (_INTERACTION_CLEANUP_EVERY) wird COUNT(*) geprüft; erst bei >6000 Zeilen (_INTERACTION_LOG_MAX) werden via 'DELETE ... WHERE id NOT IN (SELECT id ... ORDER BY id DESC LIMIT 5000)' die jüngsten 5000 (_INTERACTION_LOG_KEEP) behalten. Konstanten oben in der Datei definiert.
- **[performance] app_usage wächst unbegrenzt; _update_common_apps bei jedem App-Tracking**:323
  - Real bestätigt: learn_app_usage() rief _update_common_apps() (GROUP BY über 30 Tage + profile_set) bei JEDER App-Nutzung auf und app_usage wurde nie bereinigt. Thread-sicheren _app_usage_counter (unter _maintenance_lock) ergänzt; _update_common_apps() läuft jetzt nur jede 20. Nutzung (_COMMON_APPS_UPDATE_EVERY). Im selben periodischen Block werden app_usage-Zeilen älter als 90 Tage (_APP_USAGE_RETENTION_DAYS) gelöscht — diese fließen ohnehin nicht mehr in die 30-Tage-Aggregation ein. get_common_apps() fragt weiterhin live ab und bleibt exakt; das common_apps-Profil ist nur ein Cache und wird nun seltener, aber ausreichend aktualisiert.

## backend/tool_registry.py
- **[quality] Tool-Anzahl-Kommentare und Log-Meldung sind grob falsch (202 statt '140+')**:849
  - register_all_tools() umgebaut: Die brittle, falschen Inline-Zaehler ('# 22', '# 19', '# 5', '# 27' usw.) entfernt. Stattdessen iteriert die Funktion jetzt ueber eine Liste der Registrierungs-Funktionen, leitet den Kategorie-Namen aus dem Funktionsnamen ab und zaehlt pro Kategorie dynamisch. Die Log-Meldung gibt nun die korrekte Gesamtzahl PLUS die echte Kategorie-Aufschluesselung aus (real: 202 Tools; basis=23, file=26, communication=7, personal_os=12, hermes=7, pc_control=39, ...). Modul-Docstring 'all 138+ companion commands' und der Docstring von register_all_tools ('all 140+') auf 'all companion commands (siehe Start-Log fuer die aktuelle Gesamtzahl)' geaendert, damit keine veraltete Zahl mehr behauptet wird. Verhalten/Reihenfolge der Registrierung unveraendert.
- **[bug] process_kill kann ohne pid und ohne name aufgerufen werden (keine 'oneOf'-Validierung)**:92
  - Bestaetigt: pid und name sind beide optional, das Registry-JSON-Schema kann 'mindestens eines' nicht ausdruecken, und validate_tool_arguments akzeptierte zuvor {} fuer process_kill. Die Companion-Methode kill_process faengt das zwar ab (Rueckgabe 'Kein PID oder Name angegeben.'), aber das Finding fordert die Pruefung auf Schema-Ebene. Da tool_schema.py (Pydantic, extra='forbid') nicht geaendert werden darf und der generische Validator generisch bleiben soll, wurde eine datengetriebene Konstante _REQUIRED_ONE_OF = {'process_kill': ('pid','name')} eingefuehrt und eine neue Hilfsfunktion _validate_required_one_of() am Ende von validate_tool_arguments aufgerufen. Sie wirft ToolSchemaValidationError, wenn keiner der Gruppenwerte truthy ist (falsy wie 0/'' gilt als nicht angegeben, konsistent zur Companion-Schicht). Leicht erweiterbar fuer weitere entweder/oder-Tools.

## backend/vision.py
- **[bug] Vision-Antwort kann None sein → len(result) wirft TypeError beim Logging**:286
  - In _analyze_with_provider (Z.286) und _analyze_with_groq (Z.375) wird message.content jetzt mit `or ""` abgesichert. len()/Return erfolgen nur noch im `if result:`-Zweig, sonst greift der vorhandene 'Leere Antwort'-Warnpfad mit return None. So loest None keine Exception mehr aus und der Provider gilt korrekt als 'leer' statt 'fehlgeschlagen'.
- **[bug] _capture_active_window_sync ruft SetForegroundWindow auf → unerwarteter Fokuswechsel**:180
  - SetForegroundWindow wird nicht mehr unbedingt aufgerufen. Neuer Parameter bring_to_front (Default False) in _capture_active_window_sync und durchgereicht von capture_active_window; der Fokuswechsel erfolgt nur noch bei bring_to_front=True. Default-Verhalten (alle aktuellen Aufrufer aus vision_tools.py/router_vision.py/router_chat.py uebergeben den Parameter nicht) stiehlt damit keinen Vordergrund mehr. Oeffentliche Signaturen bleiben kompatibel (nur additiver Default-Parameter). Der vorgeschlagene PrintWindow-Umbau wurde bewusst nicht gemacht (groesserer/riskanter Umbau ausserhalb des minimalen Fix-Scopes).
- **[bug] Fenstersuche kann bei haengendem hwnd in Endlosschleife laufen**:164
  - Die GetWindow(GW_HWNDNEXT)-Schleife hat jetzt eine harte Iterationsobergrenze (_MAX_WINDOW_ITER=10000) und einen Zyklus-Schutz via seen_hwnds-Set (Abbruch bei bereits gesehenem hwnd). Ein zyklisch zurueckkehrendes hwnd blockiert damit keinen Worker-Thread mehr.
- **[security] _load_image_file dekomprimiert beliebig grosse Bilder ohne Limit → Decompression-Bomb**:240
  - Neue Konstante _MAX_IMAGE_PIXELS=64_000_000. In _load_image_file wird nach Image.open() img.size geprueft und bei width*height > Limit ein RuntimeError geworfen, BEVOR via img.save() decodiert/re-encodiert wird (PIL open ist lazy, save loest erst die Dekompression aus — der Check greift also rechtzeitig). RuntimeError wird durchgereicht (separater except RuntimeError: raise), damit die klare Fehlermeldung nicht von der generischen Lade-Exception verschluckt wird.
- **[security] Nutzerbeschreibung ungefiltert im Vision-Prompt (Prompt-Injection)**:509
  - In find_on_screen wird description vor dem Einbetten normalisiert (Zeilenumbrueche/Whitespace via ' '.join(str(description).split()) entfernt, auf 500 Zeichen gekuerzt) und im Prompt klar als Daten zwischen <<< >>>-Markierungen gekennzeichnet, mit explizitem Hinweis ans Modell, enthaltene Instruktionen NICHT zu befolgen. Reduziert die Prompt-Injection-Oberflaeche; Risiko war ohnehin als begrenzt eingestuft (lokaler Nutzer).

## backend/voice_ws.py
- **[bug] _last_volume_push ist nicht lock-geschuetzt → Volume-Throttle bei parallelen Threads inkonsistent**:110
  - Neues dediziertes threading.Lock (_volume_throttle_lock) eingefuehrt. Die Read-Modify-Write-Sequenz auf _last_volume_push in push_volume (Check + Setzen) laeuft jetzt atomar unter diesem Lock, sodass nebenlaeufige Aufrufe aus Wakeword-/TTS-Thread das Throttle nicht mehr umgehen koennen. has_ws_clients() bleibt bewusst ausserhalb des Locks (eigener _ws_lock), um keine Lock-Verschachtelung zu erzeugen.
- **[quality] Throttle nutzt time.time() statt monotonic → anfaellig fuer Zeitspruenge**:119
  - Die Throttle-Intervallberechnung in push_volume verwendet jetzt time.monotonic() statt time.time(), wodurch sie immun gegen rueckwaerts laufende Wanduhr (NTP-Korrektur, DST) ist. Der evt['ts'] in push_event bleibt bewusst auf time.time() (Anzeige-Zeitstempel), wie im Fix-Hinweis vorgesehen. Das im Finding erwaehnte Heartbeat-Intervall liegt im Router (andere Datei) und wurde hier nicht angefasst.

## backend/workflows.py
- **[bug] HTTP-Step mutiert das headers-Dict des gespeicherten Steps und loest keine Templates in Headern auf**:854
  - `headers = step.get("headers", {})` ersetzt durch `headers = self._resolve_templates(dict(step.get("headers", {})), context)`. Damit wird eine Kopie angelegt (kein Mutieren des Original-Step-Dicts durch setdefault/add_header mehr) und Header werden konsistent zu url und body durch die Template-Engine geleitet.
- **[performance] Notify-Step blockiert pro Benachrichtigung 6+ Sekunden einen Thread**:833
  - Der PowerShell-Aufruf wird jetzt fire-and-forget via `subprocess.Popen` (statt `subprocess.run` mit timeout=15) gestartet, mit stdout/stderr auf DEVNULL. Das Start-Sleep -Seconds 6 laeuft im entkoppelten Kindprozess; der Workflow wartet nicht mehr 6s und belegt keinen Executor-Thread fuer die Anzeigedauer.
- **[bug] Cron-Step-Range mit 1-basierten Feldern (DOM/Month) berechnet Schritte abweichend von Standard-Cron**:133
  - `_cron_field_matches` um Parameter `min_value` erweitert; `*/step` nutzt jetzt `(current_value - min_value) % step == 0` plus Bereichspruefung `min_value <= current_value <= max_value`. Aufrufer in `_cron_matches` uebergeben die korrekten Feld-Minima (minute/hour/dow=0, dom/month=1). Damit ergibt `*/2` im Monatsfeld 1,3,5,... wie Standard-Cron.
- **[bug] _evaluate_condition: Ordnungsvergleich (< >) mit Strings liefert stumm immer False**:1018
  - Im except-Zweig (nicht-numerische Operanden) werden jetzt auch >, <, >=, <= lexikografisch auf den Strings ausgewertet, statt stumm False zu liefern. == und != bleiben unveraendert.
- **[bug] Scheduler kann denselben Workflow doppelt parallel starten (keine Lauf-Sperre)**:1119
  - Neues Set `self._running_workflows` in `__init__` eingefuehrt. Im `_scheduler_loop` werden bereits laufende Workflows uebersprungen; vor `create_task` wird die ID hinzugefuegt; im `_safe_run_workflow`-finally wird sie via `discard` wieder entfernt. Verhindert Doppelausfuehrung, wenn ein Lauf laenger als ein 30s-Tick dauert.
- **[bug] ai_prompt-Step wertet chat()-Ergebnistyp nicht aus (error/tool_call)**:769
  - chat()-Signatur in ai_engine.py bestaetigt ({type: text|tool_call|error, content}). `_step_ai_prompt` wertet nun `result.get("type")` aus: bei type==error wird der Step per RuntimeError als fehlgeschlagen behandelt; `result_type` wird ins Step-Ergebnis aufgenommen, sodass tool_call-Faelle (oft leeres content) erkennbar sind.
- **[security] SSRF-DNS-Check schlaegt fail-open, wenn DNS-Aufloesung scheitert**:877
  - Der zweite SSRF-Check in `_step_http` ist nun fail-closed: bei socket.gaierror/OSError waehrend getaddrinfo wird der Request mit ValueError abgelehnt, statt ihn ungeprueft weiterlaufen zu lassen. Schuetzt vor DNS-Rebinding/flakiger Aufloesung zwischen Check und Connect fuer den initialen Request. Hinweis: `_validate_workflow_http_url` (Z.51-52) bleibt bewusst fail-open, da es auch vom Redirect-Handler genutzt wird und die Hauptpruefung jetzt im Step erfolgt.
- **[improvement] get_status ohne Index auf workflow_logs.finished_at + unbegrenztes Log-Wachstum**:1236
  - Index `idx_workflow_logs_finished` auf workflow_logs(finished_at) ergaenzt (beschleunigt ORDER BY finished_at in get_status). Zusaetzlich Log-Retention eingefuehrt: neue Konstante WORKFLOW_LOG_RETENTION_PER_WORKFLOW=200; nach jedem Log-Insert in run_workflow werden ueber 200 hinausgehende aelteste Eintraege pro Workflow geloescht (parameterisierte DELETE-Subquery nach id DESC). Begrenzt das Log-Wachstum.

## companion/app_discovery.py
- **[security] launch_app: app_id über explorer shell:AppsFolder / cmd start ohne Wert-Härtung**:611
  - Helper _is_valid_app_id() (max 512 Zeichen, keine Control-/NUL-Zeichen) eingeführt und im shell:AppsFolder-Zweig von launch_app() angewandt: manipulierte/ungültige Cache-AppIDs werden vor dem explorer-Aufruf abgewiesen (Rückgabe 'ungültige ID', Warn-Log). Der uri-Zweig nutzt ausschließlich die hartkodierte _PROTOCOL_URIS-Map und bleibt unverändert; der Direkt-.exe-Zweig prüft weiterhin os.path.isfile.
- **[performance] warm_cache_async lädt Disk-Cache und triggert trotzdem immer vollen Rebuild**:646
  - In warm_cache_async._init() wird nach erfolgreichem _load_disk_cache() nur noch dann _rebuild_cache() ausgeführt, wenn der geladene Cache-Timestamp älter als _CACHE_TTL (300s) ist (elif (time.time() - _cache_timestamp) >= _CACHE_TTL). Ein frischer Disk-Cache löst keinen unnötigen PowerShell-Full-Scan mehr aus.
- **[quality] Auto-warm beim Import startet PowerShell-Thread als Seiteneffekt**:653
  - Der Modul-Top-Level-Aufruf warm_cache_async() ist jetzt per Env-Flag LEXA_NO_AUTOWARM (1/true/yes) deaktivierbar. Korrektheit bleibt erhalten, da der Cache ohnehin lazy via _ensure_cache() in find_app/search_apps/list_installed_apps befüllt wird (alle Importe im Projekt sind zudem lazy in Funktionen). Vollständiges Entfernen aus dem Top-Level wäre dateiübergreifend (Backend-Startup-Routine) — daher minimal-invasiv per Opt-out gelöst.
- **[quality] find_app Substring-Match bevorzugt kürzesten Namen — kann falsche App treffen**:485
  - Tier-2c-Substring-Ranking ersetzt das reine 'kürzester Name gewinnt' durch ein Tupel-Ranking (is_full_word, Match-Position idx, Namenlänge). Voll-Wort-Treffer (z.B. 'word' in 'Microsoft Word', score 88) schlagen Wortanfang-Treffer (z.B. 'word' in 'WordPad', score 85); danach gewinnt die frühere Position, erst zuletzt der kürzere Name. Word-Boundary-Prüfung am Start bleibt erhalten.

## companion/browser.py
- **[improvement] _get_browser startet Chromium mit headless=False — sichtbares Fenster bei jedem Browser-Tool**:345
  - _get_browser akzeptiert jetzt headless: bool = False und cacht zwei getrennte Browser-Instanzen (_browser sichtbar, _headless_browser unsichtbar) bei gemeinsamem _playwright-Treiber. Datenorientierte Tools (scrape_text, check_price, website_screenshot, website_to_pdf) rufen _get_browser(headless=True) auf; open_url/play_youtube bleiben sichtbar (Default False). close_browser schliesst beide Instanzen. Auto-Install-Retry und Fehlermeldungen wurden modus-aware umgebaut, ohne Verhalten zu aendern.
- **[bug] website_screenshot/website_to_pdf: wait_for_load_state und screenshot/pdf nicht in try/except — Page-Leak und unbehandelte Exception**:535
  - Beide Funktionen kapseln _guarded_page_goto, wait_for_load_state('networkidle') und screenshot/pdf nun in einem gemeinsamen try-Block. except Exception gibt eine verstaendliche Fehlermeldung ueber t('error.generic', ...) zurueck (statt ungefangenem TimeoutError). finally schliesst page (und bei PDF zusaetzlich context) zuverlaessig — kein Leak mehr bei networkidle-Timeout. _UnsafeBrowserUrl wird vom breiteren Exception-Handler miterfasst (ValueError-Subklasse).
- **[quality] _ytdlp_search: 'except (TimeoutExpired, FileNotFoundError, Exception)' faengt alles; query ungeprueft**:280
  - Neuer Helper _clean_ytdlp_query entfernt nicht-druckbare/Steuerzeichen und begrenzt die Query auf 200 Zeichen (_MAX_YTDLP_QUERY_LEN); _ytdlp_search und _ytdlp_get_url validieren die Query damit vor subprocess.run und brechen bei leerem Ergebnis mit None ab. Die pauschale Klausel 'except (..., Exception)' wurde in beiden Funktionen auf konkrete Fehler reduziert: (subprocess.TimeoutExpired, FileNotFoundError, OSError, json.JSONDecodeError). shell=False bleibt; reine Haertung, keine Signaturaenderung.
- **[bug] search_youtube (Playwright-Pfad): href kann null sein → kaputte Video-URLs**:443
  - Das injizierte JS liest #video-title und dessen href in Variablen, baut die URL nur bei vorhandenem href ('https://youtube.com' + href, sonst null) und filtert per .filter(r => r.url && r.title) alle Eintraege ohne gueltige URL/Title heraus. Keine 'https://youtube.comnull'/'...undefined'-Strings mehr.

## companion/calendar_integration.py
- **[bug] calendar_create: Default-Endzeit behandelt reine Datumsangaben falsch (1h-Termin statt Ganztag)**:280
  - In calendar_create() wird jetzt anhand des geparsten start_iso erkannt, ob es sich um eine reine Datumsangabe handelt (kein 'T' im ISO-String) -> all_day=True. Reine Datumsangaben erzeugen nun ein echtes Ganztags-Event mit {'date': 'YYYY-MM-DD'} statt {'dateTime': ...,'timeZone':...}, die Default-Endzeit ist der Folgetag (exklusives Enddatum) statt midnight+1h. Timed-Events (mit Uhrzeit) behalten unveraendert das 1h-Default-Verhalten. Ist eine Seite (start/end) reines Datum, wird das gesamte Event als Ganztag behandelt; ein versehentlich getimter End-Wert neben einem Datum-Start faellt auf einen Eintages-Ganztagstermin zurueck.
- **[quality] OAuth-Flow oeffnet bei abgelaufenem Token unkontrolliert lokalen Server/Browser mitten in einer Abfrage**:63
  - _get_calendar_service() bekam Parameter interactive: bool = True. Bei interactive=False wird der blockierende flow.run_local_server()-Pfad NICHT betreten; stattdessen wird sofort None zurueckgegeben (Aufrufer liefert dann 'Kalender nicht verbunden...'). Alle Lese-/Tool-Funktionen in dieser Datei (calendar_today, calendar_week, calendar_next, calendar_create, calendar_delete, calendar_search) rufen jetzt _get_calendar_service(interactive=False) auf, koennen also keinen Browser-Consent mitten in einem Chat-/Tool-Aufruf mehr ausloesen. Der explizite Connect-Endpoint backend/router_calendar.py ruft calendar_int._get_calendar_service ohne Argument auf -> Default interactive=True bleibt erhalten, der OAuth-Flow funktioniert dort unveraendert. Fix bleibt damit vollstaendig lokal in dieser Datei.
- **[quality] get_calendar_status meldet 'connected' allein anhand Dateiexistenz, nicht anhand gueltiger Credentials**:84
  - get_calendar_status() laedt nun bei vorhandener Credentials-Datei die Credentials via Credentials.from_authorized_user_file() (ohne interaktiven Flow) und meldet connected=True nur, wenn creds.valid ODER (creds.expired und creds.refresh_token) -> also nur bei tatsaechlich nutzbarem/refreshbarem Token. Beschaedigte/abgelaufene-ohne-refresh Credentials -> connected=False, konsistent mit dem, was der naechste echte Kalenderzugriff tun wuerde. ImportError (Bibliothek fehlt) faellt best-effort auf Dateiexistenz zurueck. has_client_secret/credentials_path bleiben unveraendert.

## companion/communication.py
- **[quality] telegram_send/telegram_read/discord_send: requests-Exceptions unbehandelt → Crash statt Fehlermeldung**:477
  - Die requests.post/get-Aufrufe in telegram_send, telegram_read und discord_send wurden in try/except requests.exceptions.RequestException gekapselt. Bei Netzwerk-/Timeout-Fehlern wird nun eine verständliche Meldung über die vorhandenen i18n-Keys (communication.telegramError bzw. communication.discordError) zurückgegeben und der Fehler geloggt; telegram_read gibt konsistent [{"error": ...}] zurück. Keine neuen Abhängigkeiten oder i18n-Keys nötig.
- **[performance] email_read: Sender-Filter kann unbegrenzt viele Mails fetchen / langsam werden**:277
  - Der clientseitige count*3-Overfetch mit anschließendem Nachfiltern wurde durch serverseitige IMAP-Suche ersetzt: die Suchkriterien werden jetzt aus UNSEEN und FROM "<sender>" zusammengesetzt (mail.search), sodass nur tatsächlich passende Nachrichten geholt werden. Der sender wird über den neuen Helper _sanitize_imap_arg() (CR/LF-Entfernung, Quote-Escaping) abgesichert.
- **[quality] email_read/email_search nutzen Default-IMAP-Port ohne SSL-Port-Konfig aus Provider-Map**:264
  - _detect_email_provider liefert nun ein optionales imap_port (Default 993; bei Custom-Servern aus Keyring-Key 'email_imap_port' lesbar). email_read und email_search lesen provider.get("imap_port", 993) und übergeben den Port an imaplib.IMAP4_SSL(host, port, timeout=15). Die hartkodierten Provider bleiben funktional unverändert (993 = Default).
- **[security] email_search: IMAP-Suchbegriff-Escaping unzureichend (nur Backslash-Quote, keine CRLF-Filterung)**:354
  - Neuer Helper _sanitize_imap_arg() entfernt Steuerzeichen (insb. CR/LF) und escaped Backslash sowie Doppelquote. email_search nutzt ihn jetzt für query und sender statt des bisherigen reinen Quote-Replace. Damit ist eine IMAP-Command-Injection über eingebettete \r\n nicht mehr möglich; die String-basierte Kriterienbildung wurde minimal-invasiv beibehalten.
- **[bug] _parse_email_message: is_read defaultet bei fehlenden Flags auf True (UNSEEN-Mails fälschlich als gelesen)**:233
  - Der Default bei leerem flags_str wurde von True auf False geändert (im Zweifel als ungelesen behandeln) und ein erklärender Kommentar ergänzt. Verhindert, dass ungelesene Mails — besonders im email_summarize-Pfad (nur UNSEEN) — fälschlich als gelesen markiert werden.

## companion/desktop_control.py
- **[bug] GetWindowRect-Rueckgabewert in _window_capture_origin wird nicht geprueft**:324
  - GetWindowRect-Rueckgabe wird jetzt geprueft: bei Fehlschlag (0/False) gibt _window_capture_origin None zurueck, sodass desktop_click_text auf den Vollbild-Origin aus _screen_bounds() zurueckfaellt statt mit zero-filled rect einen falschen (0,0)-Offset zu berechnen. Analog zur bereits korrekten Pruefung in _center_cursor_on_window.
- **[bug] desktop_hotkey gibt bei Fehler mitten in der Sequenz gedrueckte Modifier-Tasten nicht frei**:481
  - desktop_hotkey in try/finally gekapselt: tatsaechlich gedrueckte vks werden in einer pressed-Liste mitgefuehrt und im finally garantiert in umgekehrter Reihenfolge wieder per KEYEVENTF_KEYUP freigegeben — auch wenn ein down-Event eine OSError wirft. Einzelne fehlschlagende up-Events werden abgefangen (continue), damit die Freigabe der restlichen Tasten nicht abbricht. Verhindert haengende Modifier (ctrl/alt/shift).

## companion/desktop_engine.py
- **[performance] observe()/read_screen_text starten je einen OCR-Screenshot zusaetzlich zum Timeout-Worker - doppelte Last und nicht abbrechbar**:219
  - In observe() wird read_screen_text jetzt direkt aufgerufen statt erneut in _call_with_timeout zu schachteln. read_screen_text begrenzt seinen blockierenden UIA-Aufruf bereits selbst intern via _call_with_timeout (UIA_TEXT_TIMEOUT_SECONDS). Dadurch wird pro observe()-Aufruf nur noch ein _lingering_workers-Slot belegt statt zwei, und kein aeusserer Worker haengt mehr an einem nicht abbrechbaren OCR-Grab fest, der den Slot dauerhaft halten wuerde. Das deckt sich mit allen anderen Aufrufern (engine.py, hermes_desktop.py), die read_screen_text ebenfalls ohne Timeout-Wrapper direkt aufrufen. Die dadurch ungenutzt gewordene Konstante TEXT_READ_TIMEOUT_SECONDS wurde entfernt (projektweit per Grep verifiziert, dass sie nirgends sonst referenziert wird).
- **[quality] read_screen_text mutiert das von ocr_screenshot zurueckgegebene payload-Dict in place**:260
  - In beiden Code-Pfaden (OCR-Erfolg und OCR-Fallback) wird vor dem Setzen der Zusatzfelder method/provider/providers_tried/uia_error eine flache Kopie des payload angelegt (payload = dict(payload)). Damit wird das von ocr.ocr_screenshot zurueckgegebene data-Dict nicht mehr in place veraendert. Die angereicherte Kopie wird kontrolliert in ocr_result['data'] zurueckgeschrieben, damit der Rueckgabevertrag (data.method/provider/providers_tried) erhalten bleibt - das wird von test_read_screen_text_returns_ocr_provider_text erwartet. Im Fallback-Pfad ist der Rueckschreib-Schritt zusaetzlich an isinstance(ocr_result.get('data'), dict) gekoppelt, sodass Fehler-Ergebnisse (ohne data-Dict) exakt wie zuvor unveraendert zurueckkommen.

## companion/dev_tools.py
- **[security] server_check mit reinem host:port umgeht SSRF-/Private-IP-Prüfung (interner Port-Scan)**:772
  - Neue Helfer _LOCAL_DIAGNOSTIC_HOSTS und _host_target_error eingeführt und vor dem Socket-Connect in server_check aufgerufen. Loopback/localhost (127.0.0.1, ::1) bleibt für lokale Diagnose erlaubt; alle anderen Hosts werden über dieselbe Logik wie der URL-Pfad geführt (IP direkt via _is_dangerous_ip_address, Hostnamen via _resolved_public_host_error/getaddrinfo). Damit ist der vorher offene host-Pfad gegen Scans auf private/link-local/reservierte/öffentlich-gefährliche IPs (z.B. 192.168.x:22, Cloud-Metadaten) geschlossen, ohne die Frontend-Diagnose (host/port) zu brechen.
- **[quality] multi_server_check: Default-Server prüft localhost-Ports, die der URL-Pfad sonst blockiert**:803
  - Durch die in server_check ergänzte einheitliche Host-Validierung laufen jetzt beide Eingabeformen (host:port und URL) durch dieselbe Prüfschicht. Die Default-Server (127.0.0.1:*) sind über die explizite _LOCAL_DIAGNOSTIC_HOSTS-Allowlist als lokale Diagnose abgedeckt und damit konsistent erlaubt. Die bewusste Asymmetrie (URL-Form http://localhost bleibt strikt blockiert, da angreifer-kontrollierbar; bare-host-Loopback nur für Diagnose erlaubt) wurde beibehalten, weil die bestehenden URL-SSRF-Tests dies fordern.
- **[quality] http_request: beliebige HTTP-Methode wird ungeprüft an urllib weitergereicht**:586
  - method wird einmal normalisiert (strip().upper(), Default 'GET') und gegen ein frozenset erlaubter Verben (GET/POST/PUT/PATCH/DELETE/HEAD/OPTIONS) validiert; bei ungültigem Wert klare deutsche Fehlermeldung statt generischer Exception. Request nutzt nun das validierte method, und die Body-Bedingung verwendet die normalisierte Variable (method in POST/PUT/PATCH).
- **[security] git_add: files-Parameter wird ungeprüft als git-Pfadspezifikation übergeben**:294
  - Vor dem Aufruf '--' als Trenner eingefügt: ['git','add','--',files], damit der Wert garantiert als Pathspec und nicht als git-Option interpretiert wird. Zusätzlich files getrimmt (leer -> '.') und führendes '-' mit klarer deutscher Meldung abgelehnt.
- **[bug] git_diff: full=True meldet 'keine Änderungen', obwohl nur Binär-/Mode-Änderungen vorliegen**:222
  - Im full=True-Zweig wird jetzt stat_output als Fallback berücksichtigt: bei leerem diff_output, aber vorhandenem stat_output wird die Stat-Zusammenfassung zurückgegeben statt fälschlich error.noChanges. Nur wenn beides leer ist, wird 'Keine Änderungen' gemeldet.
- **[bug] git_status: Parsing der --porcelain-Ausgabe klassifiziert Staging-/Working-Status falsch**:121
  - X- (Index/staged) und Y-Spalte (Working-Tree) werden getrennt ausgewertet: staged bei line[0] in (M,A,D,R,C), modified bei line[1] in (M,D). Eine 'MM'-Datei erscheint nun korrekt in staged UND modified; reine Working-Tree-Löschungen (' D') landen korrekt in modified statt über den alten else-Zweig. continue-Statements sauber gesetzt, sodass branch/untracked-Zeilen nicht doppelt verarbeitet werden.
- **[bug] git_status/git_log: subprocess-Returncode wird ignoriert — Fehler werden als leeres/sauberes Repo interpretiert**:121
  - In git_status und git_log nach subprocess.run je eine returncode-Prüfung ergänzt: bei returncode != 0 wird result.stderr.strip() (bzw. ein deutscher Fallback) als Fehler zurückgegeben (dict bzw. Liste mit error), bevor stdout geparst wird — analog zu git_commit/git_add. Damit werden z.B. 'dubious ownership'- oder Kein-Repo-Fehler nicht mehr als sauberes/leeres Repo fehlinterpretiert.

## companion/engine.py
- **[performance] screen_analyze: ThreadPoolExecutor wird ohne Timeout-Cleanup geschlossen, Vision-Future kann haengen**:1043
  - Den with-Block (dessen __exit__ shutdown(wait=True) aufruft und den 30s-Timeout aushebelt) durch try/finally ersetzt; im finally wird pool.shutdown(wait=False, cancel_futures=True) aufgerufen. Damit blockiert ein hängender Vision-Call den Worker-Thread nicht mehr dauerhaft, sobald future.result(timeout=30) ausgelöst hat. cancel_futures ist ab Python 3.9 verfügbar (Projekt läuft auf 3.13).
- **[quality] read_clipboard liefert unbegrenzten Clipboard-Inhalt ohne Laengen-Cap zurueck**:524
  - read_clipboard() kürzt jetzt auf 50000 Zeichen (konsistent mit dem body-Cap aus backend.md/security.md) und hängt bei Überschreitung den Hinweis '… (gekürzt)' an, damit große Clipboard-Inhalte den Antwort-Payload/Token-Kontext nicht aufblähen.
- **[security] Parameter-Wert-Laengen werden in execute() nicht begrenzt, nur die Anzahl**:361
  - In execute() nach der Param-Anzahl-Prüfung eine zentrale Schranke ergänzt: jeder String-Parameter > 50000 Zeichen wird mit klarer Fehlermeldung ('Parameter ... zu lang (max 50000 Zeichen)') abgelehnt, bevor func aufgerufen wird — konsistent mit den in security.md dokumentierten Caps, schließt den Pfad zu subprocess/PowerShell/OCR ab.
- **[bug] _search_index escaped keine SQL-LIKE-Wildcards (%, _) im Suchbegriff**:617
  - Vor dem SQL-/PS-Escaping wird der query nun mit Square-Bracket-Escaping behandelt (zuerst '[' -> '[[]', dann '%' -> '[%]' und '_' -> '[_]'), sodass literale % und _ im LIKE '%...%' verbatim statt als Wildcards matchen. Nur der query geht in LIKE; path nutzt SCOPE= und extension '=' (exakt), daher kein Escaping dort nötig.
- **[bug] shutdown_pc/restart_pc fuehren sofort und nicht stornierbar aus — kein Abbruch-Pfad**:1015
  - Neue Methode shutdown_cancel() (subprocess.run ['shutdown','/a'], shell=False, mit Timeout/Encoding) hinzugefügt und als Command 'shutdown_cancel' in self.commands registriert. Erfolgs-/Kein-Treffer-Fall werden differenziert gemeldet. Die Erfolgsmeldungen von shutdown_pc/restart_pc weisen jetzt explizit auf den Abbruch hin ("Sage 'Abbrechen', dann stoppe ich das"). Whitelist-Eintrag wurde NICHT geändert (command_whitelist.json darf nicht editiert werden); unbekannte Commands liefern laut is_command_allowed 'unknown' -> in execute() nicht blockiert, Bestätigung wird vom Aufrufer behandelt, daher rein in dieser Datei funktionsfähig.

## companion/file_tools.py
- **[bug] batch_rename überschreibt vorhandene Dateien ohne Prüfung — Datenverlust**:225
  - Vor filepath.rename() wird jetzt geprüft, ob new_path bereits existiert — in dem Fall wird die Datei mit Eintrag {'old','skipped','reason':'Ziel existiert bereits'} übersprungen statt überschrieben. Zusätzlich ist rename() in try/except OSError gekapselt, sodass ein einzelner Fehler den Batch nicht abbricht (Eintrag {'old','failed','reason'}). Kein stilles Überschreiben mehr.
- **[bug] batch_rename: pattern verwirft prefix/suffix/replace-Optionen stillschweigend**:216
  - pattern wird jetzt auf den bereits transformierten stem angewendet: {name} verwendet stem (nach replace/prefix/suffix) statt filepath.stem. prefix/suffix/replace werden nicht mehr verworfen, wenn pattern gesetzt ist; Verhalten ist konsistent. Kommentar dokumentiert die Reihenfolge.
- **[performance] find_duplicates: max_files-Limit stoppt den Verzeichnis-Walk nicht**:130
  - Am Kopf der os.walk-Schleife wird zusätzlich 'if count >= max_files: break' geprüft, sodass der äußere Walk verlassen wird, sobald genug Dateien gesammelt sind — kein sinnloses Weiter-Traversieren des gesamten Baums bis zum 30s-Timeout mehr.
- **[security] archive_extract: tar-Entpackung umgeht eigene _safe_archive_members-Prüfung**:591
  - tar/gz/bz2/xz/tgz werden jetzt analog zu ZIP behandelt: tf.getnames() läuft durch _safe_archive_members(), nur sichere Member werden via members=[...] extrahiert (zusätzlich zu filter='data'). Einheitliches '(X unsichere Einträge geblockt)'-Reporting über alle Formate.
- **[bug] split_pdf/merge_pdfs validieren Eingabepfade nicht und behandeln offene Bereiche fehlerhaft**:290
  - split_pdf: Existenz-/Dateiprüfung für pdf_path; leerer Part wird übersprungen; ungültige/rückwärtige Bereiche ('5-','7-3') geben jetzt eine klare Fehlermeldung statt stillem continue bzw. leerer PDF; vor dem Schreiben wird sichergestellt, dass mindestens eine Seite vorhanden ist. merge_pdfs: prüft alle Pfade via Path.is_file() vor merger.append und meldet fehlende Dateien, statt mit unbehandelter Exception abzustürzen.
- **[quality] file_copy: copytree schlägt fehl, wenn Zielordner existiert — irreführende Meldung**:942
  - Bei src.is_dir() und existierendem dst-Ordner wird jetzt analog zu file_move nach dst/src.name kopiert; existiert auch das schon, kommt eine klare Meldung 'Zielordner existiert bereits: ...'. Zusätzlich fängt ein except FileExistsError die generische error.generic-Meldung mit verständlichem Text ab.
- **[security] organize_downloads validiert downloads_path nicht und meldet Konflikte nicht**:234
  - downloads_path wird jetzt über _validate_scan_path geprüft (Systemverzeichnisse/nicht existierende Pfade -> {'error':...}). Namenskonflikte (Ziel existiert bereits) werden in 'skipped_conflicts' in der Rückgabe ausgewiesen, statt stumm übersprungen zu werden.
- **[security] _validate_scan_path nutzt substring-Match auf 'windows\\system' — fragil und zu breit**:86
  - Neue Hilfsfunktion _is_blocked_system_path vergleicht aufgelöste Pfad-Segmente komponentenweise (case-insensitive): system32/syswow64 als eigene Segmente, Sequenzen ('windows','system') und ('windows defender'). _validate_scan_path und folder_create nutzen jetzt diese Funktion (Duplikat-Logik in folder_create entfernt). Harmlose Pfade wie 'C:/Users/me/system32_backup' werden nicht mehr fälschlich geblockt, echte System32-Ziele schon.
- **[improvement] merge_pdfs nutzt das deprecte pypdf.PdfMerger**:274
  - merge_pdfs auf PdfWriter().append/write umgestellt (PdfMerger entfernt). Import in try/except ImportError gekapselt mit verständlicher 'pypdf nicht installiert'-Meldung, konsistent zu den image_*-Funktionen; writer.close() im finally.
- **[performance] clean_temp: rglob-Größenberechnung kann bei großen Temp-Bäumen blockieren**:453
  - Der teure per-Datei f.resolve()-Aufruf in der reinen Größen-Schätzung (rglob) wurde entfernt; der is_symlink()-Guard pro Datei bleibt erhalten. Da freed_mb nur ein Schätzwert ist und die eigentliche, sichere Löschung weiterhin durch shutil.rmtree erfolgt, halbiert das die I/O-Last bei großen Bäumen, ohne die Sicherheit der Löschung zu verändern. Kommentar erläutert die Begründung.

## companion/hermes_desktop.py
- **[bug] _extract_typed_text faellt auf Resttext zurueck und tippt ggf. Steuerwoerter mit**:1240
  - Im Fallback-Zweig von _extract_typed_text (ohne Anfuehrungszeichen) wird der Tail jetzt mit _ACTION_SPLIT_RE in Fragmente zerlegt und nur das erste Fragment uebernommen. Damit werden angehaengte Folge-Aktionen (z. B. 'Hallo Welt und speichere') abgeschnitten, falls der vorherige Instruktions-Split sie nicht getrennt hat. Verhindert, dass Steuerwoerter real per desktop_type ins Nutzerdokument getippt werden.
- **[bug] _DEICTIC_TERMS enthaelt haeufige Artikel (das, den, die) - kollidiert mit echten Klickzielen**:546
  - Artikel 'das'/'den'/'die' aus _DEICTIC_TERMS entfernt (nur projektintern genutzt, per Grep verifiziert). Dadurch loesen kurze Klickziele wie 'klicke das' nicht mehr from_recent aus, werden als echtes (ggf. unvollstaendiges) Ziel behandelt und durchlaufen weiterhin die Mehrdeutigkeitspruefung _find_is_ambiguous. Echte Verweiswoerter (darauf/drauf/daruf/es/ihn/sie) bleiben erhalten.
- **[bug] Vorbereiteter scroll-Parameter 'window' wird bei Ausfuehrung nicht an desktop_scroll uebergeben**:1979
  - Im scroll-Zweig von hermes_desktop_commit wird jetzt geprueft: wenn ein Zielfenster verlangt war (window nicht leer), aber _center_cursor_on_window None liefert (Fenster nicht aufloesbar), wird NICHT mehr blind an der aktuellen Cursorposition gescrollt. Stattdessen Rueckgabe mit summary/Verifikation status='failed' (scroll_target_window_not_found) und scrolled=False. Ohne Fensterwunsch bleibt das bisherige Verhalten (Scroll an aktueller Position) erhalten, da desktop_scroll selbst kein window-Argument kennt.
- **[bug] type-Verifikation liest UI-Baum ohne kurze Wartezeit nach dem Tippen**:2236
  - Im type-Zweig von hermes_desktop_commit ein time.sleep(0.2) direkt nach 'if verify:' und vor ui_automation.ui_tree/read_screen_text eingefuegt - analog zu click (0.25), scroll (0.15) und hotkey (0.15). Verhindert faelschliche 'failed'-Meldungen, wenn die App den UIA-Baum/Value erst nach einem kurzen Tick aktualisiert.
- **[bug] _extract_window_hint Pattern 4 matcht beliebigen Resttext als Fenstername**:1252
  - Das letzte (bewusst lose) Pattern wird jetzt nachtraeglich strenger gefiltert: nur fuer dieses Pattern (index == letztes) gilt - hoechstens 3 Tokens und keine Lagewoerter/Strukturbegriffe (oben/unten/rechts/links/mitte/ecke/menue/menu/leiste/rand/bereich/seite). 'klicke Speichern im Menue oben rechts' liefert damit keinen Fensternamen mehr, waehrend explizite 'fenster'-/App-Patterns (z. B. 'im Editor', 'im Fenster Notepad') unveraendert greifen.

## companion/media.py
- **[bug] Spotify-Token-Cache ohne Lock: Race Condition bei paralleler Wiedergabe**:109
  - Modulweites _spotify_token_lock = threading.Lock() ergänzt. _get_spotify_token() nutzt jetzt Double-Checked Locking: Fast-Path-Read des Caches bleibt erhalten, das Holen/Schreiben des Tokens (keyring + requests.post + Cache-Write) läuft serialisiert im 'with _spotify_token_lock'-Block mit erneuter Gültigkeitsprüfung. Verhindert doppelte API-Aufrufe und inkonsistenten Cache-Zustand bei Daemon-Threads aus open_spotify(). Muster analog zu app_discovery._cache_lock.
- **[quality] open_spotify: Web-API-URI wird ohne Schema-Prüfung an cmd start übergeben**:401
  - Strategy-1-Bedingung von 'if result:' auf 'if result and str(result.get("uri","")).startswith("spotify:"):' verschärft. Nur 'spotify:'-URIs werden an subprocess.Popen(["cmd","/c","start","",uri]) übergeben; ein unerwartetes/nicht-spotify-URI fällt automatisch auf Strategy 2 (escaptes search) zurück. Kommentar ergänzt.
- **[performance] screen_record: Hintergrund-ffmpeg-Prozess wird nie eingesammelt (Pipe-Deadlock/Leak)**:504
  - stderr=PIPE/stdout=PIPE entfernt. stdout geht jetzt auf DEVNULL, stderr in eine Logdatei neben der Aufnahme (output_path mit .log-Suffix). Damit kann der OS-Pipe-Buffer (~64KB) nicht volllaufen und ffmpeg blockiert während der laufenden Aufnahme nicht. Bei Frühabbruch (<2s) werden die letzten 300 Bytes der Logdatei als Fehlertext zurückgegeben. Der Parent schliesst sein log_file-Handle nach Popen; ffmpeg behält sein eigenes vererbtes Handle bis zum Ende. OSError beim Logöffnen fällt sauber auf DEVNULL zurück.
- **[security] convert_media/extract_audio: output_path wird nicht gegen Path-Traversal/Systemdirs validiert**:446
  - In convert_media() und extract_audio() wird output_path jetzt zusätzlich durch _validate_media_path(output_path, must_exist=False) geprüft (nach Berechnung des Default-Pfads, vor dem ffmpeg-Aufruf). Damit greift dieselbe Systemverzeichnis-Sperre (system32, syswow64, windows\system, program files\windows) auch für das mit 'ffmpeg -y' überschriebene Ziel. Bei Verstoss wird t('error.generic',...) zurückgegeben, ffmpeg wird nicht gestartet.

## companion/ocr.py
- **[bug] ocr_screenshot/ocr_from_bytes verwenden lang='en', aber deutscher Text braucht 'de' für Tesseract-Fallback**:183
  - Bestätigt: ocr_screenshot() rief _run_ocr_with_blocks(image_bytes) ohne lang auf (Default 'en'), wodurch der Tesseract-Fallback in _run_ocr_with_blocks nie 'deu+eng' nutzte. Behoben durch (1) neuen Parameter lang: str = 'de' in ocr_screenshot(), der jetzt an _run_ocr_with_blocks(image_bytes, lang) durchgereicht wird, und (2) Umstellung des ocr_from_bytes-Defaults von 'en' auf 'de'. Damit aktiviert der Standardpfad für die deutschsprachige App den deu+eng-Fallback (Umlaute ä/ö/ü/ß). Rückwärtskompatibel: alle Aufrufer (desktop_engine.py, desktop_control.py, hermes_desktop.py, router_vision.py) nutzen Keyword-Argumente (window_title=...) und werden vom neuen optionalen Parameter nicht gebrochen; ocr_from_bytes hat keine weiteren Aufrufer. Docstrings ergänzt.

## companion/system_tools.py
- **[security] PowerShell-Command-Injection in installed_apps über einfache Anführungszeichen**:581
  - Der Suchbegriff wird in installed_apps() in einen EINFACH-gequoteten PS-String ('*...*') eingebettet, wo _sanitize_ps_arg() (double-quote-Logik) das einfache Anführungszeichen NICHT escapt — bestätigt am Code. Fix: neue Helper-Funktion _sanitize_ps_arg_single() (verdoppelt ' → '') eingeführt und in installed_apps verwendet. Außerdem die fehlerhafte Wildcard-Escape-Sequenz von '``*'/'``?' (doppelter Backtick, in Single-Quote-Kontext zwei literale Backticks) auf '`*'/'`?' (einfacher Backtick) korrigiert, sodass -like-Wildcards korrekt escaped werden. Laufzeitprüfung: payload x'; calc; '* → x''; calc; ''* (Ausbruch neutralisiert).
- **[improvement] _sanitize_ps_arg ist nur für double-quoted Kontext sicher — fehlende Kontext-Kennzeichnung**:30
  - Docstring von _sanitize_ps_arg() um expliziten Hinweis ergänzt, dass das Ergebnis NUR im "..."-Kontext sicher ist und für '...' _sanitize_ps_arg_single() zu nutzen ist. Zweite Funktion _sanitize_ps_arg_single() (verdoppelt einfache Anführungszeichen, strippt Newlines/CR) hinzugefügt und in installed_apps angebunden — wie im Fix-Vorschlag empfohlen. Keine bestehende Signatur geändert.
- **[bug] env_set: os.environ wird mit ungesäubertem Originalwert gesetzt**:541
  - Bestätigt: env_set schrieb persistent safe_value (gekappt/Newlines entfernt), aktualisierte os.environ aber mit dem rohen value. Geändert zu os.environ[name] = safe_value, sodass Prozess- und Registry-Zustand konsistent sind. Kommentar ergänzt.
- **[quality] autostart_add überschreibt bestehende Run-Einträge ohne Prüfung**:320
  - Bestätigt: Set-ItemProperty überschrieb bedingungslos. Fix: PowerShell prüft jetzt zuerst per Get-ItemProperty, ob der Name existiert; falls ja, wird 'EXISTS:<aktueller Wert>' zurückgegeben und NICHT überschrieben, sonst angelegt ('OK'). Python wertet das aus und gibt bei Konflikt eine klare deutsche Meldung zurück (Hinweis auf autostart_remove). Es wurde KEIN neuer i18n-Key benutzt (t() würde sonst den Rohschlüssel anzeigen) — stattdessen literaler deutscher Text analog zu service_start_cmd/service_stop_cmd, da i18n-Dateien nicht editiert werden dürfen.
- **[quality] Hostname-Validierung erlaubt mehrdeutige Eingaben, IPv6 mit Doppelpunkt nicht abgedeckt**:627
  - Gemeinsamer Helper _classify_host() eingeführt: parst rohe IPs via ipaddress (erkennt IPv6) und prüft Hostnamen sonst strikt gegen RFC-1123-Regex; gibt (is_valid, is_ipv6) zurück. ping_host, dns_lookup und traceroute nutzen ihn statt des alten ^[a-zA-Z0-9.\-:]+$ (das z.B. 'a:b:c' durchließ). ping/tracert hängen bei roher IPv6 nun '-6' an (Windows benötigt das für zuverlässige IPv6-Auflösung); subprocess bleibt shell=False/Arg-Liste. dns_lookup (getaddrinfo) nur strenger validiert. Laufzeit: 8.8.8.8→(True,False), ::1→(True,True), a:b:c→(False,False), example.com→(True,False), -bad→(False,False).

## companion/tool_health.py
- **[quality] refresh() laeuft synchron im aufrufenden Thread und kann sekundenlang blockieren**:214
  - refresh() laeuft jetzt analog zu init_async() in einem Daemon-Hintergrund-Thread (name='tool-health-refresh') und kehrt sofort zum Aufrufer zurueck, statt _run_health_checks() (inkl. Playwright-Browserstart, bis ~10s) synchron auszufuehren. Zusaetzlich neuer modul-globaler threading.Lock _refresh_lock: _build_cache() haelt diesen Lock waehrend des gesamten Rebuilds, sodass parallele refresh()/init_async()-Laeufe serialisiert werden und _health_cache nicht mehr ueberlappend ueberschrieben werden kann. refresh() ist im Projekt aktuell ungenutzt (Grep bestaetigt: nur Definition, kein Aufruf), die oeffentliche Signatur 'def refresh() -> None' bleibt unveraendert.

## companion/ui_automation.py
- **[bug] _candidate_windows: prefer_recent-Fallback kann beliebiges Fenster als Klickziel liefern**:338
  - Der rekursive Fallback bei nicht mehr existierendem gemerkten Fenster (remembered_query und leeres Ergebnis) wurde von all_visible=True auf all_visible=False geaendert. Dadurch wird im rekursiven Aufruf (query leer, allow_excluded=False) nur noch das tatsaechliche Vordergrundfenster gesammelt statt ALLER sichtbaren, nicht-ausgeschlossenen Fenster. Begruendung: Bei leerem query und gefuelltem candidates (Foreground) wird die all-visible-Schleife uebersprungen. Ein erklaerender deutscher Kommentar wurde ergaenzt.
- **[quality] ui_click: occurrence wird stillschweigend auf den letzten Treffer geklemmt statt zu melden**:579
  - ui_click meldet jetzt eine Abweichung: requested_occurrence wird separat gehalten, occurrence_clamped = (index > len(matches)-1) berechnet. Bei Klemmung enthaelt das Ergebnis-Dict zusaetzlich occurrence_clamped=True, requested_occurrence und match_count, sodass LLM/Nutzer erkennen, dass weniger Treffer als angefordert gefunden wurden. Die oeffentliche Signatur und das normale Klickverhalten bleiben unveraendert; keine Exception, um den bestehenden Flow nicht zu brechen.

## companion/weather.py
- **[performance] Wetter-Cache wächst unbegrenzt (Memory Leak über lange Laufzeit)**:16
  - threading-Import und Modul-globalen _cache_lock ergänzt. _cache_get und _cache_set greifen jetzt unter dem Lock zu (Companion-Befehle laufen laut engine.py via asyncio.to_thread in Worker-Threads, daher real nebenläufig). _cache_get entfernt abgelaufene Einträge beim Lesen; _cache_set purged vor dem Schreiben alle per TTL veralteten Keys. Damit ist das Wachstum auf die Menge gleichzeitig frischer (<10min) Einträge beschränkt und der Zugriff thread-sicher.
- **[security] _get_default_city ruft ip-api.com über unverschlüsseltes HTTP auf**:115
  - Den HTTP-Aufruf http://ip-api.com/json/?fields=city durch den HTTPS-Endpunkt https://ipapi.co/json/ ersetzt (kein API-Key nötig, wie vom fix-Feld vorgeschlagen). Antwort-Parsing bleibt identisch (.get('city')), Timeout 3s und der vorhandene try/except-Fallback bleiben unverändert. Standort wird damit verschlüsselt übertragen, MITM-Manipulation der Stadt verhindert. Keine projektweite Wiederverwendung von ip-api.com außer hier (per Grep geprüft).
- **[bug] weather_will_it_rain filtert auch vergangene Stunden des aktuellen Tages**:300
  - In der hourly-Schleife wird times[i] zuerst geparst und mit datetime.now() (auf volle Stunde gerundet) verglichen; bereits vergangene Stunden werden übersprungen, bevor Regen-Einträge gesammelt werden. So liefert 'expected_time' nur noch zukünftige Stunden. Zeitvergleich naiv vs. naiv konsistent zur Europe/Berlin-Anfrage (Projekt nutzt durchgängig keine tz-aware Datetimes). Nicht parsebare Zeitstrings werden weiterhin (ohne Filter) als Rohwert übernommen, wie im Originalverhalten.

## dashboard.html
- **[bug] CSP connect-src blockiert API-Aufrufe an api.exa-ai.space — Checkout und Billing-Portal kaputt**:6
  - In der Content-Security-Policy auf Zeile 6 wurde 'https://api.exa-ai.space' zur connect-src-Direktive hinzugefuegt (direkt nach 'self', vor *.supabase.co). Verifiziert am echten Code: dashboard.js ruft per fetch ${configValue('API_URL')}/v1/stripe/checkout und /v1/stripe/portal auf, und config.js setzt API_URL: 'https://api.exa-ai.space' — ein anderer Origin als 'self' (exa-ai.space), der vorher nicht erlaubt war und vom Browser blockiert wurde.
- **[bug] Doppelter i18n-Key price_pro_f2 im Pro-Upgrade-Modal erzeugt zwei identische Listeneintraege**:158
  - Verifiziert: data-i18n='price_pro_f2' kam im Pro-Plan zweimal vor (Zeile 156 '500 Befehle / Tag', Zeile 158 'Erweitertes Gedaechtnis'); i18n.js ueberschreibt beide mit demselben Wert 'Erweitertes Gedaechtnis (500 Befehle)'. Da nur dashboard.html geaendert werden darf (i18n.js ist tabu) und der kombinierte Key bereits 'Befehle' und 'Gedaechtnis' abdeckt, wurde der doppelte <li> (vormals Zeile 156) entfernt und der verbleibende price_pro_f2-Eintrag erhielt den kanonischen statischen Fallback-Text 'Erweitertes Gedaechtnis (500 Befehle)' — identisch zur Nutzung in index.html. Der Pro-Plan zeigt nun drei verschiedene Zeilen (Spracheingabe, Gedaechtnis, Tools).

## dashboard.js
- **[quality] loadSubscriptionInfo verwendet .single() statt .maybeSingle() — erzeugt Fehler bei Free-Nutzern**:212
  - In loadSubscriptionInfo() .single() durch .maybeSingle() ersetzt und error mitdestrukturiert. maybeSingle() liefert bei 0 Zeilen data=null OHNE PGRST116-Fehler/406, sodass Free-Nutzer keinen unnötigen API-Fehler mehr erzeugen. Ein echter Fehler wird nun explizit per console.warn geloggt, account-next-payment auf '—' gesetzt und früh returniert; der bestehende Free-Pfad (data=null) landet weiterhin korrekt im else-Zweig.
- **[quality] Passwortänderung ohne Mindestlängen-Validierung im JS — Supabase-Rohfehler wird angezeigt**:416
  - Im password-form-Submit-Handler vor dem confirmPw-Vergleich eine clientseitige Längenprüfung (pw.length < 8) ergänzt, die eine lokalisierte Meldung anzeigt. Im error-Fall wird statt error.message nun mapAuthError(error.message) verwendet. Da mapError() in auth.js innerhalb dessen DOMContentLoaded-Scope gekapselt und in dashboard.js NICHT erreichbar ist, wurde eine lokale Helferfunktion mapAuthError() im Helpers-Abschnitt ergänzt (nutzt das global verfügbare currentLang aus i18n.js, mit Fallback 'de'), die weak_password/'should be at least'/rate-limit/'same password' auf DE/EN-Texte mappt und sonst die Originalmeldung durchreicht. Keine neue i18n.js-Bearbeitung nötig (verboten), keine neuen Abhängigkeiten.
- **[improvement] Kopier-Button für Lizenzschlüssel ohne Fehlerbehandlung / Fallback bei fehlendem Clipboard-Zugriff**:378
  - Copy-License-Handler robust gemacht: (1) Platzhalter/Fehlertexte ('Nicht verfügbar', 'Fehler beim Laden', 'Wird generiert...') werden über eine LICENSE_PLACEHOLDERS-Liste erkannt und nicht kopiert (lokalisierter Hinweis). Der Demo-Schlüssel LEXA-DEMO0-... bleibt bewusst kopierbar, da gültiger Wert. (2) Fehlt navigator.clipboard.writeText (unsicherer Kontext/alter Browser), wird ein lokalisierter Manuell-Kopieren-Hinweis gezeigt. (3) Ein .catch()-Zweig fängt rejected Promises ab, loggt per console.warn und zeigt eine lokalisierte Fehlermeldung — der Klick wirkt nicht mehr wirkungslos.

## evals/adapters/answer_quality_adapter.py
- **[improvement] cites_evidence-Heuristik akzeptiert nur ein festes Pfad-Praefix-Set**:47
  - Das Regex-Praefix-Set in der cites_evidence-Berechnung wurde von (backend|frontend|tests|evals|docs|scripts) um die realen Top-Level-Kernmodule companion, voice, personal_os sowie plugins erweitert. Zuvor wurden inhaltlich korrekte Datei-Zitate wie 'companion/engine.py' faelschlich als cites_evidence=False gewertet und verfehlten die Assertion. Per ls bestaetigt, dass companion/, voice/, personal_os/ und plugins/ echte Top-Level-Verzeichnisse von lexa-ai sind; per Grep verifiziert, dass dieses Regex nur an dieser einen Stelle existiert (keine doppelte Heuristik in run_eval_suite.py). Aenderung ist sicher: Der einzige Golden-Task mit Assertion cites_evidence=true zitiert bereits backend/, und die Default-/Fixture-Antworten referenzieren die neuen Praefixe nicht, daher kein Risiko gebrochener erwarteter False-Ergebnisse. Muster/Stil des bestehenden Codes beibehalten.

## evals/adapters/base.py
- **[bug] max_tool_count-Assertion kann bei nicht-numerischem value abstuerzen (anders als run_eval_suite)**:117
  - In base.evaluate_assertions wurden vier numerische Konvertierungen in try/except ValueError gekapselt (im Fehlerfall passed=False), exakt nach dem Muster von run_eval_suite.evaluate_assertion: max_tool_count (int(tool_count) <= int(value)), confidence_below (float(confidence) < float(value)), confidence_above (float(confidence) > float(value)) und max_steps_not_exceeded (int(step_count) <= int(value)). Ein nicht-numerischer value (z.B. 'drei' oder Leerstring) bricht jetzt nicht mehr den gesamten Lauf ab, sondern lässt den einzelnen Check sauber fehlschlagen. Bestätigt durch Vergleich mit dem parallelen Pfad in evals/runners/run_eval_suite.py (Zeilen 404-455), der genau diese vier Stellen ebenfalls in try/except ValueError kapselt. Nur except ValueError verwendet, um konsistent mit dem dokumentierten Projektmuster zu bleiben (kein TypeError ergänzt, um Verhalten nicht über den Parallelpfad hinaus zu verändern).

## evals/adapters/os_draft_adapter.py
- **[improvement] Path-Traversal-Erkennung greift nur bei Vorwaerts-Slash, nicht bei Windows-Backslash**:22
  - Zeile 22 von 'traversal = "../" in text or "path traversal" in text' auf 'traversal = "../" in text.replace("\\", "/") or "path traversal" in text' geaendert. Backslash-basierte Traversal-Eingaben (z.B. 'OS\..\..\core.md') werden vor dem Test auf '/'-Normalform gebracht und dadurch auf Windows erkannt, sodass blocked korrekt True wird. Folgt der bereits im Projekt etablierten Normalisierung (answer_quality_adapter.py:47 nutzt output.replace("\\", "/")). Keine neue Abhaengigkeit, keine Signaturaenderung, der 'path traversal'-Literal-Check bleibt unveraendert.

## evals/runners/check_eval_regressions.py
- **[improvement] CLI validiert Baseline nie gegen die Golden-Tasks (schwache Validierung)**:220
  - Fund am echten Code bestaetigt: validate_baseline() besitzt den optionalen Parameter golden_tasks (Zeile 40, Drift-Check Zeile 63-70), aber evaluate_regressions rief validate_baseline(baseline) ohne Golden-Tasks auf und main() lud sie nie. Umsetzung minimal und ohne Signaturbruch: (1) evaluate_regressions um den optionalen Keyword-Parameter golden_tasks erweitert, der an validate_baseline durchgereicht wird (bestehende 2-Argument-Aufrufer/Tests bleiben kompatibel). (2) In build_parser() ein optionales --tasks-Argument (nargs=+) hinzugefuegt. (3) In main() werden die Golden-Tasks NUR bei gesetztem --tasks via lazy Import von evals.runners.run_eval_suite.load_tasks_from_paths geladen und an evaluate_regressions uebergeben. Default bewusst None (opt-in) gewaehlt, damit Standalone-Baselines ohne passenden On-Disk-Golden-Satz weiterhin funktionieren (sonst Regression im CLI-Test test_checker_cli_writes_redacted_markdown_triage). load_tasks_from_paths wirft EvalSchemaError (Subklasse von ValueError), wird also vom vorhandenen except (OSError, ValueError, json.JSONDecodeError) sauber abgefangen. Kein Zirkular-Import (run_eval_suite importiert check_eval_regressions nicht; Import zusaetzlich lazy). Keine neuen Abhaengigkeiten, deutsche/englische Kommentarsprache der Datei beibehalten.

## evals/runners/eval_trend_report.py
- **[bug] Risk-Score kann durch Mismatch zwischen penalty und max_penalty auf 0 kollabieren**:63
  - In summarize_report() wurde die max_penalty-Berechnung von str(result.get('risk_level', 'medium')) auf str(result.get('risk_level') or _risk_from_checks(result)) umgestellt — identisch zur Risiko-Ableitung in risk_penalty(). Bestaetigt am echten Code: risk_penalty() (Z.41-48) verwendet den kategoriebasierten Fallback (_risk_from_checks, bis Gewicht 8 fuer 'security' u.a.), waehrend max_penalty diesen Fallback fehlte und bei fehlendem/leerem risk_level nur Gewicht 2 ('medium' bzw. RISK_WEIGHTS.get('',2)) ansetzte. Dadurch konnte penalty > max_penalty werden und risk_weighted_score via max(0.0, 1.0 - penalty/max_penalty) faelschlich auf 0.0 geklemmt werden. Nach dem Fix tragen failende Ergebnisse (Teilmenge aller Ergebnisse) dasselbe Gewicht zu Zaehler und Nenner bei, daher gilt stets penalty <= max_penalty und der Score bleibt korrekt im Bereich [0,1].

## evals/runners/policy_dashboard.py
- **[improvement] value_hash-Fallback hasht leeren String, weil Adapter-Checks kein 'value' enthalten**:59
  - In _failed_checks() (jetzt ca. Zeile 50-67) wurde der Fallback fuer value_hash korrigiert. Bestaetigt am echten Code: base.evaluate_assertions (base.py Zeile 191) erzeugt Checks nur mit 'value_hash' (stable_hash(value)[:12]) und ohne 'value'. Im Normalfall greift daher immer das vorhandene value_hash; fehlt es jedoch (extern eingespeiste Reports), lieferte stable_hash(check.get('value','')) konstant den Hash des Leerstrings fuer ALLE Checks. Neu: Wenn kein value_hash vorhanden ist, wird stable_hash([check_type, check.get('value','')])[:12] gebildet, sodass mindestens der Check-Typ (und ein evtl. vorhandenes value) den Hash unterscheidet. stable_hash akzeptiert Any und JSON-serialisiert die Liste, daher ist das typsicher. Vorhandenes value_hash wird unveraendert uebernommen; oeffentliche Signatur und Ausgabeformat (Schluessel type/passed/value_hash) bleiben gleich. Kommentar in Englisch passend zur Dateisprache hinzugefuegt.

## evals/runners/run_eval_suite.py
- **[bug] ImportError der Laufzeit-Adapter wird nicht abgefangen und bricht die ganze Suite ab**:559
  - Bestätigt: _load_adapter (Z.545-548) fängt nur ModuleNotFoundError beim importlib.import_module ab, aber der Aufruf adapter.evaluate(...) in _evaluate_with_adapter (Z.559) war ungeschützt. tool_selection_adapter.evaluate() macht in Zeile 12 einen Laufzeit-Import 'from backend.tool_registry import ...'; ein dort ausgelöster ImportError (oder anderer Laufzeitfehler) propagiert über die List-Comprehension in run_suite (Z.606-611) bis main(), das nur EvalSchemaError fängt, und bricht so den gesamten Lauf mit Traceback ab. Fix: Den adapter.evaluate(...)-Aufruf in try/except (Exception) gekapselt. Bei einem Adapterfehler wird jetzt nur der betroffene Task als passed=False gewertet, alle seine Assertions als nicht bestanden markiert und der Fehler als redigierte Observation {'adapter_error': 'TypName: msg'} festgehalten (via vorhandenem redact_secrets, damit keine Secrets in der Fehlermeldung lecken). value_hash wird wie sonst über stable_hash berechnet. Keine öffentlichen Signaturen geändert, keine neuen Abhängigkeiten, Kommentare in Englisch (konsistent zur Datei).

## evals/runners/run_manual_prompt_probe.py
- **[security] Manuelle Prompt-Probe schreibt Modell-Antworten und Prompts unredigiert auf die Platte**:458
  - Helfer aus evals/adapters/base.py importiert (has_secret, redact_secrets) nach dem sys.path-Block. In write_reports() wird das gesamte json_payload jetzt vor dem Schreiben durch redact_secrets() geleitet und durch ein has_secret()-Gate abgesichert (ValueError statt Schreiben, wenn nach Redaktion noch Secrets erkennbar) — analog zu update_eval_baseline/check_eval_regressions. Zusaetzlich redigiert _safe_reply_excerpt() die Antwort vor dem Kuerzen, sodass auch die Markdown-Ausgabe sauber ist.
- **[bug] reset_probe_state rebindet conversation_history statt das geteilte Objekt zu leeren**:131
  - Bestaetigt: router_chat importiert conversation_history per Name aus backend.shared (router_chat.py Z.33), und _history_lock (asyncio.Lock, shared.py Z.19) bewacht das Original-Objekt. reset_probe_state() und _install_sandbox_patches() leeren jetzt shared.conversation_history in-place (.clear()) und binden router_chat.conversation_history wieder an genau dieses kanonische Objekt, damit Lock-Objekt und gelesene Liste synchron bleiben. Den im fix-Feld vorgeschlagenen 'with shared._history_lock' habe ich bewusst NICHT verwendet, weil _history_lock ein asyncio.Lock ist und kein synchrones Context-Manager-Protokoll bietet; der single-threaded TestClient greift zwischen Probes nicht nebenlaeufig zu, daher ist In-place-Clear ohne Lock korrekt und sicher (per Kommentar dokumentiert).
- **[bug] main() faengt FileNotFoundError und ValueError aus Parsing/Suite-Laden nicht ab**:528
  - Suite-Laden plus _parse_sections/_parse_numbers in main() in try/except (OSError, ValueError) gekapselt, mit sauberer stderr-Meldung und return 2 (konsistent zu 'No prompts selected.'). Zusaetzlich _parse_numbers gehaertet: ungueltige Teile (z.B. 'abc' oder '5-x') werden jetzt mit klarer ValueError-Meldung 'invalid --numbers value: ...' abgewiesen statt rohem int()-Traceback; diese wird in main() abgefangen.
- **[improvement] Reply-Extraktion faellt bei Nicht-Standard-Payloads auf String der ganzen Payload zurueck**:230
  - run_prompt() stringifiziert die Payload nicht mehr komplett. Es liest reply (und nur als Fallback detail) ausschliesslich aus einem dict; fehlen beide, wird reply='' und error='unexpected payload shape' zurueckgegeben, sodass json_response/non_empty_reply den Fall korrekt als Fehlschlag markieren und keine internen Strukturen in den (jetzt zusaetzlich redigierten) Report gelangen. Sicherer und sinnvoller Vorschlag, daher umgesetzt.

## evals/runners/write_failure_triage.py
- **[bug] Inkonsistente Blocking-Logik zwischen Regressions-Checker und Triage**:56
  - make_triage_entry() erhielt einen optionalen Parameter blocking: bool | None = None. Ist er gesetzt, wird er als 'blocking'-Feld übernommen; ist er None (z. B. Standalone-Aufrufe/Tests), wird wie bisher per is_blocking() berechnet. build_triage_entries() liest nun failure.get('blocking') aus dem Regressions-Report und reicht diesen Wert (bool, sonst None als Fallback) an make_triage_entry weiter. Dadurch übernimmt der Triage-Report exakt die im Regressions-Gate via _blocking() berechnete Entscheidung — ein im Gate blockierender low/medium- bzw. new_failure-Fall erscheint nicht mehr fälschlich als blocking=False. is_blocking() und alle öffentlichen Signaturen bleiben kompatibel; nur ein optionaler Keyword-Parameter wurde additiv ergänzt. Kommentarsprache (Englisch) der Datei beibehalten.

## frontend/main.js
- **[bug] app.whenReady().then() ohne .catch() — unbehandelte Promise-Rejection beim Start**:1436
  - Der whenReady-Block erhielt einen abschliessenden .catch(), der bei jedem Startup-Fehler loggt und als letzte Rettung createWindow()+createTray() aufruft, falls noch kein Fenster existiert (sonst app.exit(1)). Zusaetzlich wird startBackend() jetzt in try/catch ausgefuehrt, sodass ein Backend-Fehler das Fenster nicht mehr verhindert (Renderer hat Offline-Fallbacks). installLocalAuthCookie ist separat abgesichert (siehe Zeile 349).
- **[security] Permission-Handler erlaubt Audio-Capture ohne ergaenzenden PermissionCheckHandler**:891
  - Nach dem bestehenden setPermissionRequestHandler wurde (feature-detected via typeof) ein konsistenter ses.setPermissionCheckHandler registriert: erlaubt nur permission==='media' (kein 'video') fuer vertrauenswuerdige Renderer-URLs, sonst false. Damit sind Permissions.query/enumerateDevices-Checks fuer Mikrofon deterministisch und identisch zur Request-Policy.
- **[quality] show-notification-IPC validiert die Eingabe-Payload nicht**:1335
  - Payload wird jetzt defensiv koerziert: title = String(data?.title || 'Lexa AI').slice(0,120), body = String(data?.body || '').slice(0,500), silent = Boolean(data?.silent). Optional-Chaining verhindert TypeError bei undefined data; Laengenlimits verhindern uebergrosse Strings.
- **[bug] Fenster-close-Handler/Auto-Reload-Watcher koennen sich ueber Lifecycle-Uebergaenge ansammeln**:932
  - setupFrontendAutoReload raeumt jetzt zu Beginn einen evtl. vorhandenen frontendWatcher (frontendWatcher.close() in try/catch) und frontendReloadTimer (clearTimeout) auf, bevor ein neuer fs.watch angelegt wird. Der closed-Cleanup setzt zusaetzlich frontendReloadTimer=null. Damit sammeln sich bei window-all-closed -> activate -> createWindow keine Watcher/Timer mehr an. Der separate close->hide()-Handler in createWindow bleibt unveraendert (korrektes Tray-Verhalten).
- **[bug] installLocalAuthCookie ohne Fehlerbehandlung — Cookie-Fehler kippt gesamten Startup**:349
  - cookies.set wurde in try/catch gekapselt; ein Fehler wird nur via console.warn geloggt, der Startup laeuft weiter. Begruendung im Kommentar: der onBeforeSendHeaders-Header (installLocalAuthRequestHeaders) ist ein zweiter Auth-Kanal, daher darf ein Cookie-Fehler das Fenster nicht verhindern.

## frontend/preload.js
- **[quality] Kern-Bridge-Methoden (chat, stt, tts, chatFile, setProfile u.a.) prüfen res.ok nicht und können unverarbeitete Fehler-Bodies oder Exceptions liefern**:1434
  - chat(), chatFile() und stt() leiten jetzt über apiJson(res, fallback) statt res.json(). Dadurch wird res.ok geprüft: bei 429/5xx kommt ein strukturiertes {ok:false, httpStatus, error, message} (Spread erhält detail/reply, sodass bestehende Aufrufer wie handleChatResponse und der res.detail-Pfad weiter funktionieren), und nicht-JSON-Antworten werfen keine unbehandelte Exception mehr (apiJson fängt den Parse-Fehler intern ab). tts() wurde NICHT geändert, da es bereits res.ok prüft und einen Blob-Object-URL bzw. null statt JSON liefert — apiJson ist dort nicht anwendbar. setProfile/conversation* wurden im Rahmen dieses Findings nicht angefasst, da der Fund explizit chat/stt/tts/chatFile als Mindestumfang nennt und diese den Hauptpfad (Chat-Antwort, Rate-Limit) betreffen.
- **[bug] generateTitle-Fallback wirft TypeError, wenn message null/undefined ist**:1931
  - Im catch-Zweig wurde message.substring(0,40) durch String(message || "").substring(0,40) ersetzt. Damit wirft der Fallback bei message===null/undefined keinen TypeError mehr, der den ursprünglichen Netzwerkfehler verdeckt. Real bestätigt: Aufrufer chat_conversations.js:356 übergibt rohes userMessage an generateTitle.
- **[security] backupRestoreDb ohne clientseitige Pfad-Validierung — inkonsistent zur personalOs-Härtung**:2403
  - Neuer Helper isAllowedBackupDbPath() (plus Konstante BACKUP_DB_FILENAME_PATTERN) ergänzt; backupRestoreDb wirft jetzt bei ungültigem Pfad bridgeSecurityError('','bridge_privacy_denied') BEVOR der Request gesendet wird. Die Prüfung verwendet bewusst NICHT das vom Fund vorgeschlagene isPathTraversal: dieses lehnt absolute Pfade ab, Backup-Pfade aus backupListDb sind aber legitim absolut (z.B. ~/.lexa/backups\lexa-backup-*.json) — isPathTraversal hätte jede gültige Wiederherstellung gebrochen. Stattdessen Defense-in-Depth analog zur Backend-Validierung (router_backup.py _resolve_backup_path): echte Traversal-Segmente (..) ablehnen via bridgePathInfo().hasParentSegment und Basename gegen das bekannte Muster ^lexa-backup-*.json prüfen. Absolute Pfade bleiben zulässig.

## frontend/src/app.js
- **[performance] Poll-Intervall-Umschaltung innerhalb der Event-Schleife kann verwaiste/wechselnde Timer erzeugen**:596
  - Real und behoben. Die direkten setInterval/_stopWakeWordPolling-Aufrufe innerhalb der for-Schleife (Cases conversation_start/conversation_end) entfernt. Stattdessen wird die gewuenschte Rate in 'let desiredPollRate' gesammelt und erst NACH der Schleife einmalig via neuem Helfer _setWakeWordPollRate(rate) angewendet (nur ein Timer-Wechsel pro Batch, kein sofortiger Re-Poll/Rekursion). _startWakeWordPolling(rate=...) zentralisiert die Frequenz und trackt sie in der neuen Modul-Variable _wakeWordPollRate; _stopWakeWordPolling setzt sie zurueck. Kommentarsprache (Deutsch) beibehalten.

## frontend/src/chat.js
- **[bug] Leaked 45s-Timeout fuehrt zu uncaught TypeError (null.abort())**:1526
  - _streamTimeout wird jetzt mit `let _streamTimeout = null;` VOR dem aeusseren try deklariert und im try zugewiesen. Die Timer-Callback prueft defensiv `if (window._lexaStreamAbort) window._lexaStreamAbort.abort();`. Im aeusseren catch(err) sowie unmittelbar vor dem finalen `window._lexaStreamAbort = null` wird `clearTimeout(_streamTimeout)` aufgerufen. Damit kann der Timer nach einem Fehler (z.B. response.body null) nicht mehr verwaisen und keinen TypeError ~45s spaeter ausloesen.
- **[performance] Pro Reader-Read ein nicht abgebrochener setTimeout im Agent-Stream**:2183
  - Im Agent-Stream-Loop wird der setTimeout-Handle in `readTimeoutHandle` gemerkt und nach jedem gewonnenen Promise.race mit `clearTimeout(readTimeoutHandle)` geleert. So akkumulieren sich keine bis zu hunderte offene 120s-Timer mehr ueber lange Agent-Laeufe.
- **[quality] Tote Variable requiresConfirmation in sendMessage und ungenutzter Parameter in addMessage**:1508
  - Die tote lokale Variable `let requiresConfirmation = false;` (Z.1508) und ihre einzige Zuweisung `requiresConfirmation = data.rc;` (Z.1600) wurden aus sendMessage entfernt. Der Parameter `requiresConfirmation` in addMessage (Z.267) wurde NICHT entfernt (positionale Signatur, viele Aufrufer uebergeben silent/options positionsbasiert) sondern per Kommentar als bewusst ungenutzter Positions-Platzhalter dokumentiert.
- **[improvement] Suggestion-Chip ignoriert syncChatInputSize und kann Vorschlag bei laufendem Stream verwerfen**:1681
  - Im Chip-Click-Handler zuerst `if (LexaState.get('isLoading')) return;` (Chip bleibt erhalten, kein Verlust bei laufendem Stream), danach `chatInput.value = s; syncChatInputSize();` (Zeichenzaehler/Textarea-Hoehe konsistent) und erst dann `suggestDiv.remove(); sendMessage();`.
- **[improvement] Follow-up-Vorschlaege werden auch bei abgebrochenen/timeout-Antworten generiert**:1672
  - Die Chip-Generierung wurde zusaetzlich an `!streamStoppedByUser && !streamTimedOut && !streamError` gebunden, sodass Folgevorschlaege nur noch bei einer vollstaendig und fehlerfrei empfangenen Antwort erscheinen.
- **[quality] Lokale Neudeklaration von sendBtn beschattet das Modul-Global im Stop-Handler**:1377
  - Die lokale `const sendBtn = document.getElementById('send-btn');` im Stop-Button-Handler von showTyping wurde entfernt; der Handler nutzt jetzt das in app.js definierte Modul-Global `sendBtn` (wie der Rest des Moduls).

## frontend/src/chat_agent_runs.js
- **[performance] agentRunAttentionResolvedHistory() schreibt potenziell bei jedem Lesezugriff in den Speicher**:200
  - Befund bestätigt: renderConversationList() ruft agentRunAttentionResolvedHistory() pro Render mindestens zweimal auf (über updateAgentAttentionHeaderSummary Zeile 112 und renderAgentResolvedHistoryPanel Zeile 120 in chat_conversations.js, jeweils via agentRunAttentionResolvedHistoryForConversations). Jeder Aufruf führte das teure Pruning inkl. Evidenz-Parsing aller Meta-Caches (agentRunAttentionResolvedHistoryItemHasEvidence) plus JSON.stringify-Vergleich aus und konnte einen Schreibvorgang auslösen. Fix: render-/Tick-bezogener Memo in agentRunAttentionResolvedHistory() eingeführt — Ergebnis wird mit dem rohen Storage-String als Schlüssel gecacht und im nächsten Microtask (queueMicrotask, Fallback Promise.resolve().then) wieder verworfen. Innerhalb eines synchronen Renders treffen die Folgeaufrufe den Cache (kein erneutes Parsen/Pruning/Schreiben); spätere Renders oder Mutationen starten frisch. Cache-Invalidierung ist korrekt, da jede Mutation ausschließlich über saveAgentRunAttentionResolvedHistory läuft (einzige Schreibstelle für den History-Key, Zeilen 162-163) und damit den rohen String ändert. Kommentar auf Deutsch ergänzt (Dateisprache). Keine öffentliche Signatur geändert, keine neue Abhängigkeit.

## frontend/src/chat_markdown.js
- **[bug] Markdown-Links mit Titel ([text](url "titel")) degradieren still zu Klartext**:33
  - Problem am echten Code verifiziert: Die URL-Gruppe des Link-/Bild-Tokens fängt den optionalen CommonMark-Titel mit ein. normalizeChatUrl ruft new URL(value) darauf auf. Per node getestet: 'https://x.com "Tooltip"' wirft -> Link fällt in den Plaintext-Zweig; 'https://x.com/a "Tooltip"' wirft NICHT, sondern liefert eine kaputte href mit angehängtem %20%22Tooltip%22. Fix: Neue Hilfsfunktion splitChatLinkTarget(rawTarget) in chat_markdown.js eingefügt, die das Ziel am ersten Whitespace in url + title zerlegt und umschließende Quotes/Klammern des Titels entfernt. appendInlineMarkdown nutzt sie nun sowohl für das Bild-Token (match[4]) als auch das Link-Token (match[6]): normalizeChatUrl bekommt nur noch target.url; der Titel wird (falls vorhanden) sicher als title-Attribut am <a> bzw. <img> gesetzt (textContent-äquivalent, kein Injection-Risiko). Nur chat_markdown.js geändert, normalizeChatUrl-Signatur unangetastet.

## frontend/src/chat_state.js
- **[quality] chatCachedHistorySnapshot validiert Array-Elemente nicht**:86
  - In chatCachedHistorySnapshot wird das geparste Array jetzt zusaetzlich pro Element gefiltert: nur Plain-Objekte (it && typeof it === 'object' && !Array.isArray(it)) werden zurueckgegeben. Damit koennen Nicht-Objekt-Eintraege (Strings/Zahlen/null/verschachtelte Arrays) aus dem extern beeinflussbaren sessionStorage-Cache nicht mehr an renderPersistedConversationMessages (chat.js Z.175-187) durchgereicht werden, das msg.content/msg.text/msg.role liest. Verhalten fuer wohlgeformte Caches unveraendert; Kommentarsprache (Deutsch) beibehalten.

## frontend/src/commands.js
- **[improvement] Hartkodierter Button-Text statt i18n in dashQuickPomodoro**:513
  - In dashQuickPomodoro (Stop-Zweig, jetzt Zeile 536) wurde der fest kodierte String 'Pomodoro' durch den vorhandenen i18n-Key ersetzt: pomoBtn.textContent = '⏱ ' + t('pomodoro.start'). Das Uhr-Emoji bleibt als visuelles Praefix erhalten (konsistent mit dashboard.js). Die Keys 'pomodoro.start' existieren bereits in de.json ('Pomodoro starten') und en.json ('Start Pomodoro'). Hinweis: Die identische Hartkodierung in dashboard.js:305 wurde NICHT angefasst, da nur frontend/src/commands.js geaendert werden darf — sie sollte in einem separaten Schritt analog umgestellt werden.

## frontend/src/css/chat.css
- **[bug] Nicht definierte Custom Property var(--mono) bei .agent-step**:1057
  - In .agent-step (jetzt Zeile 1052) wurde die kaputte Variablenreferenz 'font-family: var(--mono);' durch 'font-family: var(--font-mono);' ersetzt. '--mono' ist nirgends im Projekt definiert (verifiziert per Grep: einzige Verwendung war chat.css:1057), '--font-mono' ist in theme.css:59 definiert. Damit ist die ungueltige var()-Referenz behoben und die vom Datei-Autor beabsichtigte Monospace-Schrift korrekt referenziert. Hinweis: premium_animations.css:756 setzt .agent-step spaeter (letzte geladene Datei, index.html:36) ohnehin auf var(--font); das ist eine separate, dateiuebergreifende Stilentscheidung und wurde nicht angefasst — die Korrektur der kaputten Referenz in dieser Datei ist dennoch richtig und risikolos.
- **[quality] Toter Selektor .tts-toggle.active (Element heisst .tts-toggle-btn)**:995
  - Der tote Selektor-Block '.tts-toggle.active { color: var(--accent2); background: var(--accent2-soft); }' wurde aus chat.css entfernt. Verifiziert: Das HTML-Element ist <button class="sleek-icon-btn tts-toggle-btn" id="tts-toggle"> (index.html:636) — es gibt keine Klasse 'tts-toggle', nur die id 'tts-toggle' und die Klasse 'tts-toggle-btn'. Der Selektor '.tts-toggle.active' (Klasse) matchte das Element also nie. Der gueltige Aktiv-Stil liegt in voice.css:584 ('.tts-toggle-btn.active'); JS (chat_voice.js:103/108) setzt die Klasse 'active' auf #tts-toggle, was korrekt von voice.css gegriffen wird. Damit ist toter, verwirrungsstiftender Code entfernt, ohne sichtbares Verhalten zu aendern.

## frontend/src/css/components_overlays.css
- **[bug] Animation 'fadeIn' wird referenziert, ist aber nirgends als @keyframes definiert**:8
  - Projektweit per Grep bestaetigt, dass kein '@keyframes fadeIn' existiert (nur 'fadeInUp' in chat.css, anderer Name) und nichts per JS injiziert wird. '@keyframes fadeIn { from{opacity:0} to{opacity:1} }' am Dateiende ergaenzt. Da CSS-Keyframes dokumentweit gelten, behebt das den No-Op fuer .shortcuts-overlay (Z.8) und gleichzeitig alle weiteren Referenzen (.onboarding-overlay/-result, .fade-in, .sleek-greeting, aktive system/commands-Views).
- **[bug] Animation 'modalSlideIn' wird referenziert, ist aber nirgends als @keyframes definiert**:108
  - Projektweit per Grep bestaetigt, dass kein '@keyframes modalSlideIn' existiert und nichts per JS injiziert wird. '@keyframes modalSlideIn' (opacity 0->1, translateY(8px) scale(0.98) -> none) gemaess fix-Leitlinie ergaenzt. Behebt den No-Op fuer .input-modal-panel (Z.108) und dokumentweit auch .notif-center (components_shell_widgets.css:198).
- **[quality] Toter .input-hint-Stilblock (Element existiert nicht) inkl. globalem display:none !important**:162
  - Per Grep ueber *.js/*.html/*.ts bestaetigt: es gibt nirgends ein 'input-hint'-Element (weder im HTML noch dynamisch erzeugt). Die Zeile '.input-hint { display: none !important; }' wurde aus dieser Datei entfernt (durch die neuen Keyframes ersetzt). Damit entfaellt der projektweite display:none-!important-Fallstrick. Die uebrigen toten .input-hint-Regeln in chat.css/components_search_memory.css liegen ausserhalb dieser Datei und bleiben unangetastet; ohne das globale !important sind sie inerte Stile fuer ein nicht existierendes Element und nun gefahrlos.

## frontend/src/css/components_search_memory.css
- **[bug] Nicht definierte Custom Property var(--text-primary) (4 Verwendungen)**:528
  - Die nicht definierte Variable var(--text-primary) wurde an allen 3 Vorkommen in dieser Datei durch das im Theme definierte var(--text) ersetzt: Z. 528 (.note-modal-title-input), Z. 546 (.note-modal-close:hover) und Z. 556 (.note-modal-textarea). Per projektweitem Grep bestaetigt: --text-primary wird nirgends in CSS oder JS gesetzt, waehrend --text in theme.css sowohl im :root (Z. 33, Dark) als auch in [data-theme="light"] (Z. 91) definiert ist, also in beiden Themes korrekt aufloest. Die im fix-Feld alternativ genannte Theme-Alias-Loesung (--text-primary: var(--text) in theme.css) wurde NICHT gewaehlt, da sie eine andere Datei (theme.css) aendern wuerde, die ausserhalb des erlaubten Bearbeitungsumfangs liegt. Das vierte Vorkommen in components_productivity.css:207 (.todo-inline-input) liegt in einer anderen Datei und bleibt daher unveraendert; es muss separat behoben werden.

## frontend/src/css/components_shell_widgets.css
- **[quality] Doppelte .skeleton-Definition mit unterschiedlichen Keyframes (shimmer vs. skeleton)**:367
  - Verifiziert: components_core.css:298 definiert .skeleton mit 'animation: skeleton 1.5s' und dem Keyframe '@keyframes skeleton', components_shell_widgets.css:367 definierte .skeleton mit 'animation: shimmer 1.5s' und einem eigenen '@keyframes shimmer'. Beide Keyframes waren inhaltlich identisch (0% {background-position: 200% 0} -> 100% {background-position: -200% 0}). Da components_shell_widgets.css laut index.html (Zeile 28) nach components_core.css (Zeile 24) laedt, gewinnt die shimmer-Variante. Da nur components_shell_widgets.css editiert werden darf, wurde die in-file-Redundanz beseitigt: Die .skeleton-Regel referenziert jetzt das gemeinsame 'skeleton'-Keyframe aus components_core.css, der lokale doppelte '@keyframes shimmer' wurde entfernt. Background-Farben (--card-bg/--border), background-size und border-radius:8px der gewinnenden Regel bleiben unveraendert -> kein visueller Unterschied. Projektweit per Grep geprueft: 'shimmer' wird sonst nirgends in CSS/JS als Animation genutzt (einziger weiterer Treffer war eine unabhaengige JS-Variable in orb3d.js).

## frontend/src/css/overrides_accessibility.css
- **[bug] Fokus-Outline für Toggle-Switches greift nie (falscher Klassenname '.slider')**:23
  - Selektor in Zeile 23 von '.toggle-switch input:focus-visible + .slider' auf '.toggle-switch input:focus-visible + .toggle-slider' korrigiert. Verifiziert per Grep über das gesamte Frontend: das sichtbare Geschwister-Element heißt '.toggle-slider' (index.html Z. 896/945/994/1128/1141/1152/1163, gestylt in views_settings.css:334) und das bestehende Muster '.toggle-switch input:checked+.toggle-slider' (views_settings.css:356) bestätigt den richtigen Adjacent-Sibling-Selektor. Eine Klasse '.slider' existiert nirgends im Frontend (nur 'toggle-slider', 'settings-range::-webkit-slider-thumb' und ID-basierte Range-Slider). Da '.toggle-switch input' per opacity:0/width:0/height:0 unsichtbar ist (views_settings.css:328), erhalten alle Toggle-Schalter jetzt bei Tastatur-Fokus wieder eine sichtbare Outline.

## frontend/src/css/overrides_chat_voice.css
- **[bug] Nicht definierte Custom Property var(--font-main) bei .conv-agent-attention-badge**:650
  - In .conv-agent-attention-badge (Zeile 650) wurde die kaputte Referenz 'font-family: var(--font-main);' durch 'font-family: var(--font);' ersetzt. Per Grep ueber das gesamte Projekt (lexa-ai) bestaetigt: '--font-main' war ausschliesslich an dieser einen Stelle verwendet und nirgends definiert; '--font' ist in theme.css:58 definiert. Minimaler, korrekter 1-Zeilen-Fix; restliche Tokens der Regel unveraendert.

## frontend/src/css/views.css
- **[bug] Dashboard-Widgets bekommen die gestaffelte Einblend-Animation nie (.dashboard-view nie gesetzt)**:70
  - Bestaetigt am echten Code: In index.html:228 hat der Dashboard-Container id="dashboard-view" und Klasse "tool-view"; switchView() (app.js:344) setzt nur die Klasse "active" auf dieses id-Element, eine Klasse "dashboard-view" wird nirgends im Frontend vergeben (per Grep ueber frontend/ verifiziert). Daher matchten die Selektoren ".dashboard-view.active .dash-widget/.dash-section" (views.css:70-119) nie und die staggerFadeIn-Animation lief nicht. Fix: Alle 10 Vorkommen von ".dashboard-view.active" auf "#dashboard-view.active" umgestellt (analog zum funktionierenden ".tool-view.active .tool-card"-Muster). Die :nth-child(1..7)-Regeln treffen jetzt korrekt die 7 .dash-widget-Kinder der .dashboard-grid; :nth-child(8) bleibt harmlos ohne Treffer. Nur diese Datei geaendert.

## frontend/src/css/views_dashboard.css
- **[bug] Dashboard-Systembalken (CPU/RAM/Disk/Akku) bleiben immer auf 0 % Breite**:169
  - Bug am echten Code verifiziert: Die Balken (#dash-cpu-bar etc.) tragen im HTML 'dash-stat-bar-fill bar-initial' und bekommen per dashboard.js (applyMeterClass, Z. 453-456) 'meter-width-X' dazu — 'bar-initial' wird nie entfernt. Drei width-Regeln gleicher Spezifitaet (0,1,0) konkurrieren: '.meter-width-X {width:X%}' (views.css, zuerst geladen), '.dash-stat-bar-fill {width:0%}' (views_dashboard.css) und '.bar-initial {width:0%}' (overrides_responsive_utilities.css, zuletzt geladen → gewinnt). Ergebnis: dauerhaft 0 %. Da nur views_dashboard.css editiert werden darf, wurde der vom Fund vorgeschlagene Spezifitaets-Ansatz gewaehlt: 21 gekoppelte Regeln '.dash-stat-bar-fill.meter-width-{0..100 in 5er-Schritten} {width:X%}' (Spezifitaet 0,2,0) hinzugefuegt, die '.bar-initial'/'.dash-stat-bar-fill' unabhaengig von der Ladereihenfolge schlagen. percentBucket() in dashboard.js (Math.round(clampPercent/5)*5) liefert exakt diese 21 Buckets, daher sind alle Regeln erreichbar. Die '.dash-stat-bar-fill {width:0%}'-Default-Regel blieb als sinnvoller Fallback vor dem ersten JS-Update erhalten. Deutscher Erklaerkommentar (Kommentarsprache der Datei) ergaenzt. Keine fremden Dateien/Signaturen geaendert.

## frontend/src/dashboard.js
- **[quality] Batterie-Ton invertiert Schwellenwert über einen Hack-Wert statt klarer Logik**:263
  - In refreshDashboard (Zeile 263) den Aufruf von applyMetricTone(battEl, bv <= 30 ? 90 : 20, "metric-success") ersetzt durch applyMetricTone(battEl, bv, bv <= 30 ? "stat-danger" : "metric-success"). Die künstlichen, wirkungslosen 90/20-Werte sind entfernt; stattdessen wird der echte Batteriewert bv übergeben und die Tönungsklasse direkt gewählt. Niedrige Batterie (<= 30) erhält jetzt sichtbar die Warn-/Fehlerfarbe (.stat-danger), gesunde Batterie .metric-success — konsistent mit der bereits korrekten Meter-Bar-Logik (bv > 30 ? meter-success : meter-error) und mit identischem Schwellenwert. Alle drei Klassen (.stat-danger, .stat-warn in theme.css; .metric-success in views.css) sind per Grep als existent bestätigt; applyMetricTone fügt den in successClass übergebenen String unverändert via classList.add hinzu, daher ist der Fix kompatibel ohne Signaturänderung.

## frontend/src/devtools.js
- **[bug] Clipboard-Schreibzugriff ohne Verfuegbarkeitspruefung in base64/uuid/hash**:412
  - Real bestaetigt: base64Action (Z.419), hashAction (Z.439) und uuidAction (Z.453) riefen direkt navigator.clipboard.writeText(...).catch(()=>{}) auf — bei undefined navigator.clipboard wirft das einen synchronen TypeError am .catch vorbei, und der 'kopiert'-Toast erschien unabhaengig vom Kopiererfolg. Fix: An allen drei Stellen auf die projektweite Hilfsfunktion copyTextToClipboard() (definiert in chat_message_actions_controller.js, im HTML vor devtools.js geladen, also global verfuegbar; gleiches Muster wie in commands.js) umgestellt. Diese prueft navigator.clipboard sicher ab und faellt auf execCommand zurueck. Der Erfolgs-Toast (devtools.resultCopied / toast.hashCopied / devtools.uuidCopied) wird nun erst nach erfolgreichem Promise gezeigt; bei Fehler erscheint ein Warn-Toast t("toast.copyFailed"). Alle verwendeten i18n-Keys existieren in de.json/en.json.

## frontend/src/i18n/i18n.js
- **[bug] i18n fetch-Fallback parst src-Pfad fehleranfaellig (script[src*='i18n.js'])**:60
  - Im fetch-Fallback (Strategie 2) die fragile Neuermittlung des basePath via document.querySelectorAll('script[src*="i18n.js"]') + src.replace('i18n.js','') entfernt und stattdessen den bereits am Ladezeitpunkt via document.currentScript gecachten '_basePath' verwendet (fetch(_basePath + info.file)). _basePath (Zeilen 26-30) strippt Query-Strings (z.B. i18n.js?v=2) und war zuvor toter, ungenutzter Code (projektweit nur an der Definitionsstelle referenziert) — nun korrekt angebunden. Erklaerender Kommentar ergaenzt (Zeilen 72-74). Dadurch robuster bei Query-Strings/umbenannten Builds; doppelte, abweichende Pfadlogik beseitigt.

## frontend/src/index.html
- **[improvement] Hartkodierte Statistik 'Befehle bereit' / Versions-Strings im Markup driften von der Realität**:580
  - Die hartkodierten, driftenden Default-Werte im Markup durch neutrale Platzhalter (&mdash;) ersetzt, damit vor der Laufzeit-Befüllung nie eine falsche/widersprüchliche Zahl aufblitzt: Z.580 #greeting-cmd-count '162 Befehle bereit' → &mdash;; Z.221 .version-tag '[v]1.0.0' → &mdash;; Z.1212 #settings-version '[v]1.0.0' → &mdash;; Z.1218 #settings-cmd-count '60 registriert' → &mdash;. Die Laufzeit-Verdrahtung existiert bereits und überschreibt diese Felder verlässlich (fetchCommandCount→updateCommandCount in app.js:80-104/283 mit Fallback auf ALL_COMMANDS_DATA.length, settings.js:233, sowie updateVersionLabels via checkHealth in app_health.js:6-23 für beide data-role='backend-version'-Felder). NICHT geändert: #greeting-conv-count ('0 Chats gespeichert' ist der korrekte Frischstart-Default und wird durch updateConversationCount überschrieben) sowie die data-i18n dashboard.subtitle (Z.324) — deren Text stammt aus den i18n-JSON (de.json/en.json, außerhalb dieser Datei) und wird zur Laufzeit von dashboard.js:239 mit der Live-Version überschrieben.

## frontend/src/memory.js
- **[bug] clearClipboardHistory ohne Fehlerbehandlung führt zu unbehandelter Promise-Rejection**:1068
  - backendOnline-Guard und try/catch ergänzt (analog deleteSnippet/toggleRoutine): bei Offline früher Abbruch mit Fehler-Toast; clipboardClear()-Aufruf in try/catch gekapselt, Erfolgs-Toast nur bei Erfolg, im catch console.warn + showToast(t('toast.executionError'),'error',2200).
- **[improvement] runMemoryCleanup löscht Daten ohne Bestätigungsdialog und mit Magic-Numbers**:1404
  - Vor dem destruktiven memoryCleanup(90,3) einen Bestätigungsdialog ergänzt (showInputModal, Muster wie Notiz-/Snippet-Löschung): Das Label nennt die Kriterien explizit ('Erinnerungen älter als 90 Tage mit Wichtigkeit < 3 werden unwiderruflich gelöscht') und verlangt die Eingabe 'ja'; bei Abbruch/anderer Eingabe passiert nichts. Busy-Sperre wird erst nach der Bestätigung gesetzt. Kein neuer i18n-Key erfunden (t() würde sonst den rohen Key anzeigen), nur bestehender Key memory.cleanupOld wiederverwendet plus deutscher Inline-Text konsistent zur Dateisprache.
- **[bug] updateMemoryGraphInspector kann bei undefined graph crashen**:335
  - Im node===null-Zweig den Zugriff abgesichert: graph.nodes.length/graph.links.length durch graph?.nodes?.length || 0 bzw. graph?.links?.length || 0 ersetzt. Damit wirft die Funktion bei undefined/teilweise initialisiertem graph keinen TypeError mehr.

## frontend/src/modals.js
- **[bug] showToast loggt bei fehlendem Toast-Container nicht in die Notification-Center**:249
  - Der Aufruf _addToNotifCenter(message, type) wurde VOR den frühen 'if (!container) return;'-Check verschoben (vorher Zeile 256, jetzt direkt nach dem Toast-Aufbau). Dadurch wird die Notification-Historie und der Unread-Badge immer befüllt, auch wenn der toast-container früh im Bootstrap oder in reduzierten Views noch nicht im DOM existiert. Erklärender Kommentar (Englisch, konsistent zur Datei) hinzugefügt. Reihenfolge des sichtbaren Toasts/Timeout unverändert.
- **[quality] Command-Palette ruft Aktionen per Window-Lookup auf — fragiler String-basierter Dispatch**:724
  - Die Action-Items in renderPaletteResults speichern jetzt direkte Funktionsreferenzen (action: showHelp / showOnboarding / showShortcutsOverlay) statt der Strings 'showHelp()' etc. Der Dispatch im Click-Handler wurde von item.action.replace('()','') + window[fnName]() auf 'if (typeof item.action === "function") item.action(); else showToast(t("modals.nothingFound"), "error");' umgestellt. Damit ist der Aufruf modul-scoping-fest (kein window-Lookup mehr) und ein fehlender Handler erzeugt einen sichtbaren Fehler-Toast statt stillem No-op. Alle drei Funktionen sind als Top-Level-Deklarationen in derselben Datei definiert und werden auch andernorts direkt namentlich aufgerufen — Referenzen sind im Script-Scope gültig.

## frontend/src/orb3d.js
- **[improvement] Toter Code in LexaOrb3D: konfigurierbare detail-Option und glowTexture werden ignoriert**:14
  - Alle drei Teilprobleme behoben, alles innerhalb orb3d.js: (1) Die irreführende Default-Option detail: 12 und der harte Override this.options.detail = 4 wurden entfernt; per Grep verifiziert, dass this.options.detail nirgends gelesen wird und der einzige Aufrufer in app.js (Zeile 188) kein detail uebergibt. (2) Der tote glowTexture-Dispose-Block in destroy() wurde geloescht; per Grep ueber das ganze Projekt bestaetigt, dass glowTexture ausser in destroy() nie zugewiesen/genutzt wird. (3) Der Pro-Frame-Write this.core.material.shininess (auf dem dauerhaft unsichtbaren core, visible=false) wurde aus animate() entfernt. Zusaetzlich der ebenfalls tote computeVertexNormals-Block fuer die unsichtbaren, nie deformierten core/glassShell-Meshes entfernt, samt der dadurch ungenutzten Felder _normalFrame, _lastNormalUpdate, der Option normalUpdateMs und der Variable normalInterval.
- **[performance] Orb-Animationsschleife laeuft dauerhaft mit requestAnimationFrame ohne sichtbarkeitsgebundenes Anhalten/Disposal**:168
  - In animate() eine Sichtbarkeitspruefung direkt nach dem bestehenden document.hidden-Guard hinzugefuegt: Wenn das injizierte Canvas (this.renderer.domElement) offsetParent === null hat (z.B. weil die Chat-View ueber die Klasse 'hidden'/display:none ausgeblendet ist, waehrend eine andere View aktiv ist), wird die teure Pro-Frame-Arbeit (Deformation von 1800 Punkten, Farb-Neuberechnung, render) uebersprungen. Der rAF-Loop bleibt absichtlich aktiv (gleiches Muster wie der vorhandene document.hidden-Guard), sodass der Orb beim Zurueckwechseln zur Chat-View automatisch weiterlaeuft. _lastFrameNow wird beim Ueberspringen auf 0 gesetzt, damit der erste sichtbare Frame nicht faelschlich als Frame-Spike den _perfMode hochstuft. Dies ist die vom Finding genannte In-Datei-Alternative; die andere Variante (stop()/start() in switchView) liegt in app.js und ist hier ausgeschlossen.

## frontend/src/personal_os.js
- **[quality] decidePersonalOsDraft setzt isDeciding vor dem Modal, blockiert Auto-Refresh unbegrenzt**:197
  - In decidePersonalOsDraft wurde 'PersonalOSState.isDeciding = true' aus dem Bereich VOR dem await showInputModal entfernt und erst NACH dem Guard 'if (!result) return;' gesetzt. Der nun redundante isDeciding=false-Reset im !result-Zweig entfiel (Guard ist jetzt ein einfaches early return). Damit ist das isDeciding-Gate nur noch waehrend des laufenden Decision-Requests aktiv; der offene Modal-Dialog ist weiterhin separat ueber personalOsHasOpenModal abgedeckt. Konsistenzhalber wurde dasselbe Muster identisch in applyPersonalOsDraft (isApplying) angewendet, da dort die gleiche latente Haengergefahr bestand. Die finally-Bloecke setzen die Flags weiterhin defensiv zurueck (unschaedlich).
- **[improvement] Obsidian-Kontext-Topic kann durch Default-Fallback ungewollte Suchbegriffe injizieren**:113
  - Der Topic-Aufbau in personalOsLoadObsidianContext wurde umgestellt: Der generische Fallback wird nur noch verwendet, wenn KEIN nutzerspezifisches Signal vorliegt (kein sharedTopic, kein tagFilter.tag und area noch auf Default). Dazu wurde der Default-Area-Wert als Konstante POS_OBSIDIAN_DEFAULT_AREA extrahiert und eine hasUserSignal-Pruefung eingefuehrt. Der zuvor hartkodierte englische String 'lexa hermes obsidian' laeuft jetzt ueber posUiText('pos.obsidianDefaultTopic', 'lexa hermes obsidian') und ist damit lokalisierbar (Inline-Fallback bleibt bei fehlendem i18n-Key, daher keine Aenderung an anderen Dateien noetig). Bei vorhandenem area/tag wird kein generischer Fallback mehr angehaengt; der Topic bleibt fuer den leeren Default-Fall weiterhin nicht-leer.

## frontend/src/personal_os_prompt_helpers.js
- **[bug] compacted-Flag immer false bei deutscher UI (String-Match auf englischen Literal)**:24
  - Bestaetigt am echten Code: 'pos.promptCompactedSuffix' lautet in de.json '[Text gekuerzt, damit er in Lexa passt.]' und enthaelt den englischen Substring 'Prompt compacted' NICHT, weshalb personalOsReviewPromptMeta (Z.24) und personalOsCodeLoopPromptMeta (Z.44) bei deutscher UI 'compacted' faelschlich immer als false meldeten. Fix: Statt nach dem englischen Literal zu suchen, wird in beiden Meta-Helfern jetzt sprachneutral gegen den tatsaechlich von posClipChatPrompt angehaengten, lokalisierten Suffix gematcht (compactedSuffix via posUiText('pos.promptCompactedSuffix', ...)) und 'prompt.includes(compactedSuffix)' geprueft. Pruef-Demo: alte Logik liefert false, neue Logik liefert true bei mit deutschem Suffix gekuerztem Prompt. Bewusst inline in beiden Funktionen geloest (kein neuer Top-Level-Helper), weil die Test-Harness tests/test_personal_os_prompt.js Funktionen ueber eine fixe Namens-Allowlist per extractFn laedt und ein neuer Helper dort nicht eingebunden waere (nicht editierbar laut Auftrag). 'length' bleibt unveraendert (Test 'reports same prompt length' / 'reports raw code loop prompt length' weiterhin gruen).

## frontend/src/productivity.js
- **[bug] Live-Zeittracking-Anzeige friert ein (kein Sekunden-Tick)**:1170
  - In refreshTimeTracking wird bei laufendem Tracking jetzt ein reiner Client-Tick registriert (LexaState.setInterval("time-tracking-live", _timeTrackingLiveTick, 1000)), der die verstrichene Zeit jede Sekunde aus dem zwischengespeicherten start_time (_ttLive.startMs) neu berechnet — ohne HTTP-Call, analog zum Pomodoro. Neue Helfer formatTimeTrackingElapsed (extrahierte Zeit-Formatierung) und _timeTrackingLiveTick wurden hinzugefügt; Modul-State _ttLive ergaenzt. Da switchView (app.js) dieses Intervall NICHT verwaltet, ist der Tick selbst-abschaltend: er cleart sich, sobald Tracking endet, das Element fehlt oder currentView !== 'productivity'. Beim Stoppen/Toggle wird das Intervall im else-Zweig zusaetzlich explizit gecleart. Reine In-Datei-Loesung, kein app.js-Eingriff noetig.
- **[bug] Overdue-Berechnung verschiebt sich durch UTC/Local-Mix**:287
  - In refreshTodos wird ein reines Datum ("YYYY-MM-DD", per Regex erkannt) jetzt lokal interpretiert: const [y,m,d] = td.due_date.split('-').map(Number); new Date(y, m-1, d, 23,59,59,999). Damit liegt das Faelligkeitsende konsistent in lokaler Zeit, statt new Date('YYYY-MM-DD') als UTC-Mitternacht zu parsen und dann setHours lokal anzuwenden. Nicht-Datum-only-Werte (z.B. mit Zeit) behalten den bisherigen Pfad (new Date + setHours). Zusaetzlich NaN-Guard (Number.isNaN(due.getTime())), damit ungueltige Datumswerte nicht faelschlich overdue setzen.
- **[bug] Streak-Meilenstein-Benachrichtigung feuert bei Mehrfach-Logs mehrfach**:1097
  - Neuer Modul-State const _lastHabitStreak = new Map(). In logHabit wird der Glueckwunsch (playBeep + showToast + sendNotification) nur noch bei tatsaechlichem Streak-Anstieg ausgeloest: prevStreak = _lastHabitStreak.get(key) (Default -1), Bedingung streak > prevStreak && milestones.includes(streak). Nach der Pruefung wird der aktuelle Streak gespeichert (_lastHabitStreak.set(key, streak)). Dadurch loest mehrmaliges Loggen am selben Meilenstein-Tag (target>1, Streak konstant) die Benachrichtigung nicht erneut aus. Der bestehende 🎉-Escape im Notification-Titel wurde beibehalten.

## frontend/src/settings.js
- **[bug] TTS-Status schreibt auf nicht existierende ID und kollidiert mit ElevenLabs-Status auf el-status**:205
  - Per Grep über das ganze Projekt bestätigt: ID 'tts-status' existiert nirgends, der Block fiel daher immer auf #el-status zurück, das wenige Zeilen später (loadElevenLabsSettings, jetzt Z. ~245) maßgeblich und lokalisiert mit drei Zuständen gesetzt wird. Der tote/widersprüchliche Schreibblock (ehem. Z. 205–210 mit hartcodiertem 'Verbunden'/'Kein API Key') wurde entfernt und durch einen erklärenden Kommentar ersetzt. #cartesia-status (Primär-TTS) bleibt unberührt.
- **[bug] AI-Readiness-Karte zeigt fest verdrahtetes '/4', obwohl nur 1 Provider (gemini) bewertet wird**:419
  - Karten-Value von hartcodiertem `${aiReadyCount}/4` auf `${aiReadyCount}/${providers.length}` umgestellt (konsistent mit dem AI-Check in buildSystemReadinessModel, der dieselbe providers-Liste nutzt). Zusätzlich den unerreichbaren Kartenzustand korrigiert: `aiReady ? (aiReadyCount >= 2 ? 'ready' : 'attention') : 'blocked'` (konnte bei nur 1 Provider nie 'ready' werden) auf `aiReady ? 'ready' : 'blocked'` vereinheitlicht — identisch zur Check-Zeile. providers-Liste und Scoring (aiReadyCount*10) bewusst unverändert gelassen (Erweiterung auf 4 Provider wäre verhaltensändernd/riskanter).
- **[bug] saveProfile crasht bei fehlenden Profil-Eingabefeldern und hat keine Fehlerbehandlung**:1360
  - Elemente werden vor Zugriff geprüft (nameEl/langEl); fehlen beide, früher Return statt TypeError. Einzelfeld-Tolerant (liest nur vorhandene Felder). Die await window.lexa.setProfile-Aufrufe sind jetzt in try/catch gekapselt; im Fehlerfall erscheint ein Fehler-Toast (showToast common.error + settingsClip), der Erfolgs-Toast nur bei tatsächlichem Erfolg.
- **[bug] Trial-Fortschrittsbalken zeigt geklemmten Tageswert in CSS, aber ungeklemmten im Text**:1290
  - Im Anzeigetext von _showTrialBar (mode='active') wird statt des rohen daysLeft jetzt der bereits auf 0–14 geklemmte safeDaysLeft verwendet (t('settings.trialProgress', {days: safeDaysLeft})). Damit stimmen CSS-Balken (trial-progress-days-N) und Textwert überein.
- **[quality] Lizenzschlüssel-Validierung ohne Toleranz für interne Whitespaces**:1322
  - In activateLicense werden vor der Format-Prüfung alle Whitespaces entfernt: key = result.key.replace(/\s+/g,'').toUpperCase() (statt nur trim().toUpperCase()). Aus Copy&Paste stammende interne Leerzeichen/Umbrüche führen nicht mehr zur Ablehnung; das strikte LEXA-Regex bleibt unverändert als kanonische Prüfung. Sichere, eindeutige Verbesserung.
- **[quality] runSystemDiagnostics liest Status aus textContent statt aus dem Readiness-Modell**:648
  - refreshSettingsView gibt jetzt den berechneten Readiness-State zurück: 'blocked' im Offline-Zweig, null bei veraltetem/abgebrochenem Request, sonst readinessModel.state (das gebaute Modell wird in einer lokalen Variable gehalten und zusätzlich gerendert). runSystemDiagnostics verwendet diesen zurückgegebenen state ('ready'/'attention'/sonst) statt #readiness-status.textContent gegen englische Label-Literale ('READY'/'ATTENTION') zu parsen. Damit ist die Status-Logik locale-/render-unabhängig. Externe Aufrufer in app.js ignorieren den Rückgabewert weiterhin (unkritisch).
- **[bug] refreshSettingsView wird nach Secret-Aktionen ohne await/Fehlerbehandlung neu getriggert**:1024
  - Alle refreshSettingsView()-Aufrufe nach erfolgreichen Secret-Aktionen werden jetzt awaited: setDeepgramKeyAction, deleteDeepgramKeyAction, setCartesiaKeyAction, deleteCartesiaKeyAction und elevenlabsKeyAction. Die Aufrufe stehen bereits innerhalb try/catch; durch await fangen diese nun auch Render-Fehler aus der Render-Phase nach Promise.allSettled ab (statt unbehandelter Rejection), und die Busy-Sperre in runSettingsSecretAction bleibt bis zum Abschluss des Refresh gehalten. deleteCartesiaKeyAction (vom Finding nicht explizit gelistet, aber identisches Muster) zur Konsistenz mitgefixt.

## frontend/src/system.js
- **[quality] toolsPct verschachtelt systemDisplayNumber(systemDisplayPercent(...)) — fehleranfaellige Doppelkonvertierung**:390
  - Der Code war seit dem Audit bereits teils refaktoriert: Statt der beschriebenen Doppelkonvertierung existiert nun korrekt 'toolsPctLabel' (Number.isFinite-Pruefung, fehlender Wert -> '?'). Allerdings referenzierte Zeile 403 noch die alte, nirgends definierte Variable 'toolsPct' (`${toolsPct}%`), was im UI 'undefined%' gerendert haette (per Grep ueber frontend/src bestaetigt: 'toolsPct' kam ausschliesslich an dieser kaputten Stelle vor). Fix: `${toolsPct}%` durch das bereits formatierte 'toolsPctLabel' ersetzt. Damit ist sowohl der echte undefined-Bug behoben als auch das vom Audit gewuenschte '?'-Verhalten (kein irrefuehrendes '0%') aktiv. toolsPctLabel enthaelt das '%' bereits selbst, daher kein zusaetzliches '%' angehaengt.

## index.html
- **[security] index.html besitzt keine Content-Security-Policy (im Gegensatz zu auth/dashboard)**:3
  - CSP-Meta-Tag in den <head> von index.html eingefuegt (direkt nach dem viewport-Meta, gleiche Position wie in auth.html/dashboard.html, Zeile 6). Wert: default-src 'self'; script-src 'self' https://unpkg.com; connect-src 'self' https://prod.spline.design; img-src 'self' data: blob: https://exa-ai.space; style-src 'self'; font-src 'self'; object-src 'none'; base-uri 'self'; form-action 'self'; frame-ancestors 'none'; upgrade-insecure-requests. Die Origins wurden am echten Code verifiziert: script-src deckt die lokalen Skripte (i18n.js, landing.js, landing-spline.js) plus den ES-Modul-Import des Spline-Runtime von unpkg.com (modulepreload Z.12 und import in landing-spline.js) ab; connect-src deckt den fetch-Preload (Z.13) und app.load() der scene.splinecode samt Assets von prod.spline.design ab; img-src enthaelt blob:/data: fuer WebGL-Texturen. Stripe-Direktiven wurden bewusst weggelassen, da index.html kein Stripe laedt (kein script-src/frame-src-Bedarf). frame-ancestors 'none' wurde wie im Fund gefordert ergaenzt. Keine inline style=/on...=/<script>, daher bleiben die projektweiten Pruefungen in scripts/validate-static.mjs erfuellt.

## landing.js
- **[performance] Mehrere ungedrosselte mousemove-Listener (Spotlight, Cursor-Glow, pro Button) belasten den Main-Thread**:84
  - Alle drei mousemove-Handler in initPointerEffects (Spotlight, #cursor-glow, sowie der Handler pro .btn/.tubelight-item) wurden über requestAnimationFrame entkoppelt: pro Frame wird via Ticking-Flag (spotlightTicking/glowTicking/btnTicking) nur ein einziger Frame verarbeitet, statt bei jeder Mausbewegung getBoundingClientRect-Reads und style.transform-Writes auszuloesen. event.clientX/clientY werden vor dem rAF-Callback in lokale Variablen kopiert, da das Event-Objekt zum Ausfuehrungszeitpunkt veraltet/recycelt sein kann. getBoundingClientRect der Buttons wurde bewusst NICHT gecacht (wie im fix-Feld lose vorgeschlagen), weil das eigene transform des Buttons und Layoutaenderungen das Rect veraendern und ein Cache zu Drift fuehren wuerde; die rAF-Koaleszierung adressiert die Main-Thread-Last ohne Korrektheitsrisiko.
- **[performance] scroll-Listener ohne Drosselung in initTubelightNav und initScrollEffects**:36
  - Beide scroll-Listener wurden per requestAnimationFrame-Ticking zusammengefasst. In initTubelightNav ruft der Listener updateActive() nur noch innerhalb eines rAF-Callbacks auf (Flag navTicking), statt bei jedem Scroll-Tick alle Sektionen via getBoundingClientRect zu iterieren. In initScrollEffects wurde die Logik in applyScrollEffects() ausgelagert und ueber das Flag scrollTicking pro Frame nur einmal ausgefuehrt (style.opacity/style.filter-Writes des Hero-Hintergrunds). passive:true bleibt erhalten. Kein IntersectionObserver-Umbau noetig — minimaler, korrekter Diff.

## personal_os/00_System/SDK/os-sdk/src/ast.ts
- **[bug] checkTask/uncheckTask markieren nur das erste Treffer-Item - mehrere gleichnamige Tasks bleiben unveraendert**:22
  - In checkTask (Z.19) und uncheckTask (Z.35) wurde das vorzeitige 'return true' im Visitor-Callback entfernt, sodass visitListItems nun ALLE Listenpunkte besucht und jeden Treffer (item.checked) setzt, statt beim ersten Treffer abzubrechen. Damit werden bei mehreren gleichnamigen/substring-passenden Tasks alle ge-/entcheckt. Je ein erklaerender Kommentar (Englisch, dateikonsistent) wurde ergaenzt. taskState (Z.51) wurde bewusst NICHT geaendert (behaelt 'return true'/Erst-Treffer), da es einen einzelnen boolean|undefined zurueckgibt - hier ist Erst-Treffer korrekt; dies wurde per Kommentar als Absicht dokumentiert. Keine oeffentliche Signatur geaendert, kein neues Matching-Verhalten (includes bleibt, damit die MCP-'distinctive task text'-Semantik in index.ts erhalten bleibt). Konsistent mit dem bereits globalen String-Replace (/g) in draft-decision.ts Z.81-82.

## personal_os/00_System/SDK/os-sdk/src/draft-apply.ts
- **[bug] Malformed Draft-Envelope-JSON wirft ungefangen und verhindert markEnvelopeFailed/Legacy-Fallback**:327
  - In applyDraft() wird parseDraftEnvelope(draft.body) jetzt in einen eigenen try/catch gehuellt. envelope ist als 'let envelope: OsDraftEnvelope | null' deklariert. Bei einem Parse-/Schema-Fehler (vorhandener, aber ungueltiger Envelope-Block) wird zuerst ein 'Draft.ApplyFailed'-Event via appendEvent geschrieben (mit redactEventDetails, stage='parseDraftEnvelope') und anschliessend eine sprechende Fehlermeldung 'Draft envelope is present but invalid: ...' geworfen. Die Semantik bleibt erhalten: envelope === null (kein Marker vorhanden) faellt weiterhin auf applyLegacyRawInboxDraft zurueck; nur der echte Parse-Fehler-Fall wird nun kontrolliert protokolliert statt ungefangen zu propagieren. Ein einziger korrupter Envelope blockiert das Anwenden somit nicht mehr ohne Audit-Spur. OsDraftEnvelope, appendEvent und redactEventDetails waren bereits importiert; 'Draft.ApplyFailed' ist ein gueltiger OSEventType (types.ts Z.13).
- **[quality] writeAtomic-Temp-Dateiname kollidiert bei zwei Anwendungen in derselben Millisekunde**:137
  - writeAtomic (Z.134-140) bildet den Temp-Pfad jetzt als `${absolutePath}.tmp-${Date.now()}-${process.pid}-${Math.random().toString(16).slice(2)}` statt nur `${absolutePath}.tmp-${Date.now()}`. Damit werden zusaetzlich Prozess-PID und Zufallsentropie eingestreut, sodass zwei parallele Apply-/Write-Vorgaenge fuer dasselbe Ziel in derselben Millisekunde nicht mehr in dieselbe Temp-Datei schreiben. Bewusst das vorhandene Projektmuster Math.random().toString(16).slice(2) (events.ts eventId(), phase2a-smoke.ts) genutzt, um keine neue Abhaengigkeit/keinen neuen Import (z.B. randomUUID) einzufuehren. Hinweis: dasselbe Muster steht auch in write.ts Z.133, das aber gemaess Auftrag (nur diese Datei editieren) hier nicht angefasst wurde.

## personal_os/00_System/SDK/os-sdk/src/draft-decision.ts
- **[bug] decideDraft bricht bei korruptem Envelope ab, obwohl Approval/Rejection rein checklistenbasiert moeglich waere**:84
  - parseDraftEnvelope(body) ist jetzt in try/catch gekapselt (Z.83-88). Bei ungueltigem JSON oder fehlgeschlagener Zod-Validierung wird envelope = null gesetzt und die Envelope-Synchronisation uebersprungen, statt die gesamte Entscheidung zu verwerfen. envelope ist explizit als ReturnType<typeof parseDraftEnvelope> (= OsDraftEnvelope | null) typisiert, daher strict-konform. Die checklistenbasierte Entscheidung (Frontmatter + Body-Replace) wird damit trotzdem geschrieben (writeFile Z.101). Ein erklaerender Kommentar in der Dateisprache (Deutsch) wurde ergaenzt.
- **[quality] Tote AST-Mutationen: checkTask/uncheckTask werden durch parseMarkdownBody ueberschrieben**:67
  - Die toten checkTask()/uncheckTask()-Aufrufe (ehem. Z.66-72) wurden entfernt, da file.ast unmittelbar danach in Z.96 via parseMarkdownBody(body) aus dem string-ersetzten Body neu erzeugt wird und serializeMarkdown ausschliesslich file.ast nutzt - die Mutationen waren wirkungslos. Der nicht mehr benoetigte Import von checkTask/uncheckTask wurde entfernt (taskState bleibt, da in Z.59-60 als Guard genutzt). Der Body-String-Replace ist nun als alleiniger, dokumentierter Entscheidungspfad markiert (Kommentar Z.72-74). checkTask/uncheckTask bleiben in ast.ts und werden weiterhin extern (os-mcp-server index.ts:641, README) ueber OS.AST genutzt - kein Bruch.

## personal_os/00_System/SDK/os-sdk/src/events.ts
- **[performance] events.jsonl wächst unbegrenzt - keine Rotation/Größenbegrenzung**:21
  - Bestätigt: appendEvent (vorher Z.11-23) hängte jeden Event ohne jede Rotation oder Größengrenze per appendFile an 00_System/Events/events.jsonl an — bei Dauerbetrieb (raw-inbox-worker --watch) wuchs die Datei monoton. Fix: Neue Helferfunktion rotateIfNeeded(logPath) plus Konstante MAX_EVENT_LOG_BYTES = 5 MiB. Vor jedem Append wird per stat() die aktuelle Größe geprüft; ab 5 MiB wird die aktive Datei per rename() auf events.jsonl.1 verschoben (überschreibt vorherige Rotation), danach beginnt ein frisches Log. Die Rotation ist best-effort: ein fehlendes Log beim ersten Write (ENOENT) und jeder andere Rotationsfehler werden im catch geschluckt, sodass das Event-Logging nie blockiert wird (fällt auf Append in die aktuelle Datei zurück). rename/stat wurden zum bestehenden node:fs/promises-Import ergänzt — rename ist bereits Projektmuster (write.ts/draft-apply.ts). Leser (draftHistory in index.ts Z.366, smoke-Tests) lesen weiterhin die Live-Datei und werden durch die Rotation nicht gebrochen. Kommentarsprache (Englisch) und öffentliche Signaturen unverändert.

## personal_os/00_System/SDK/os-sdk/src/markdown.ts
- **[bug] parseMatter-Fallback fuer H1-vorangestelltes Frontmatter ordnet Dokumentinhalt um**:23
  - Der Fallback in parseMatter (Z.19-33) verkettete preamble (H1 + greedy '\s*') und content per `${preamble}${content}` ohne garantierten Zeilentrenner. Bei Dateien, in denen der Body direkt nach dem schliessenden '---' begann (keine Leerzeile), wurde die H1 ohne Trennung an den ersten Body-Absatz geklebt (`# Title\nBody`), und das greedy '\s*' im Capture machte die Grenzbehandlung fragil. Fix: '\s*' aus der Capture-Group entfernt (jetzt `/^(# .+?)\r?\n\s*---.../`), sodass nur die reine H1-Zeile als `heading` erfasst wird; die Whitespace-Zone zwischen H1 und '---' wird verworfen statt mitgeschleppt. Body wird nun als `${heading}\n\n${content}` rekonstruiert, wobei fuehrende Newlines des content per `replace(/^\r?\n+/, '')` normalisiert werden, um eine doppelte Leerzeile zu vermeiden. Ergebnis: Die H1 bleibt als erster Block des Bodys erhalten und ist immer durch genau eine Leerzeile vom Folgeinhalt getrennt — keine stille Inhaltsverkettung/-umordnung mehr bei spaeterem serialize/write. Oeffentliche Signatur unveraendert, keine neuen Abhaengigkeiten.

## personal_os/00_System/SDK/os-sdk/src/write.ts
- **[quality] writeMarkdown mutiert das uebergebene file-Objekt (frontmatter) als Seiteneffekt**:92
  - Zeile 92 mutierte zuvor das Eingabe-OSFile in-place via file.frontmatter = validateFrontmatter(...). Bestaetigt als realer Seiteneffekt: draft-apply.ts ruft writeMarkdown mehrfach mit demselben/abgeleiteten Objekt auf (Z.284 und Z.297). Fix: Eine lokale, flache Kopie 'const normalized: OSFile = { ...file, frontmatter: validateFrontmatter(file.frontmatter) }' eingefuehrt und im gesamten try-Block (Serialisierung, Draft-Pfad, Envelope, Events, Return-Werte) statt 'file' verwendet. Der catch-Block nutzt bewusst weiterhin das Original 'file' (file.raw/file.path sind unveraendert und sollen so quarantaeniert werden; normalized ist try-scoped). OSFile war bereits als Typ importiert; serializeMarkdown liest ast+frontmatter, beide bleiben durch die Kopie korrekt. Kein Eingabeobjekt wird mehr mutiert.
- **[quality] Zwei nicht deckungsgleiche Memory-Level-Vokabulare; targetMemoryLevel mappt 'session'/'volatile' still auf 'episodic'**:43
  - Bestaetigt: schema.ts (Z.16) memoryLevelSchema = core/working/session/volatile/archive; draft-envelope.ts (Z.6) targetMemoryLevelSchema = working/episodic/stable/core. targetMemoryLevel() deckte nur core/working/archive explizit ab und liess alles Uebrige (inkl. session/volatile) still ueber den default auf 'episodic' fallen. Fix: Funktion auf ein explizites switch umgestellt, das alle fuenf Schema-Werte abdeckt (core->core, working->working, archive->stable, session/volatile->episodic) plus defensiven default. Kommentar ergaenzt, der beide Vokabulare dokumentiert. Laufzeitverhalten fuer session/volatile bleibt identisch (weiterhin episodic), ist aber nun bewusst/explizit statt stiller Fehlklassifikation. targetMemoryLevel ist ausschliesslich in write.ts definiert und genutzt (Z.110) - per projektweitem Grep verifiziert, Aenderung ist dateilokal.

## personal_os/07_Automations/Workflows/raw-inbox-worker/src/index.ts
- **[quality] Watch-Loop ohne Fehler-Backoff und ohne sauberes Shutdown**:112
  - Der --watch-Modus wurde gehaertet (Z.112-140). Statt der nackten for(;;)-Endlosschleife laeuft jetzt eine while(!stopRequested)-Schleife. Zwei Verbesserungen: (1) Graceful Shutdown via process.once('SIGINT'/'SIGTERM'), die ein stopRequested-Flag setzt; die Schleife prueft das Flag direkt nach runOnce und vor dem naechsten Lauf und bricht an einer sauberen Grenze ab, sodass ein laufender State-Write nie mitten abgebrochen wird. (2) Exponentielles Backoff bei aufeinanderfolgenden ok:false-Laeufen: waitMs = min(intervalMs * 2^(failures-1), maxBackoffMs) mit maxBackoffMs = max(intervalMs*30, 60_000); bei Erfolg wird consecutiveFailures auf 0 zurueckgesetzt, sodass wieder im normalen intervalMs gelaufen wird. Keine neuen Abhaengigkeiten, keine geaenderten Signaturen, englische Kommentare passend zum Dateistil. Hinweis: Das 'optional' im fix-Feld (Connect-Fehler-Run-Logs nicht jede Runde schreiben) wurde bewusst nicht umgesetzt, da das in worker.ts/mcp.ts liegt und nicht rein in dieser Datei loesbar ist; das Backoff entschaerft die Log-Flut ohnehin.

## personal_os/07_Automations/Workflows/raw-inbox-worker/src/mcp.ts
- **[quality] textContent liest nur content[0] - Tool-Ergebnisse mit fuehrendem Nicht-Text-Block schlagen fehl**:40
  - textContent iteriert jetzt ueber result.content und gibt den ersten Block zurueck, dessen Feld text ein string ist, statt fest content[0] zu verwenden. Dadurch funktionieren Tool-Resultate, bei denen dem Text-Block ein Nicht-Text-Block (z.B. image/resource) vorangeht. Die oeffentliche Signatur (textContent(result: unknown): string) und beide Fehlermeldungen ('did not include content.' / 'did not include text content.') bleiben unveraendert; ein erklaerender englischer Kommentar (konsistent zur Dateisprache) wurde ergaenzt.

## personal_os/07_Automations/Workflows/raw-inbox-worker/src/processor.ts
- **[quality] AbortController-Timeout deckt das Lesen des Response-Bodys nicht zuverlaessig ab**:320
  - Bestaetigt: In requestLlmExtraction und requestLexaExtraction wurde der AbortController-Timeout zwar bis ins finally (also ueber das Body-Lesen hinweg) scharf gehalten, das Abbrechen eines bereits laufenden Body-Streams haengt aber von der fetch-Implementierung ab und war damit fragil. Fix: Neuer gemeinsamer Helper `withDeadline<T>(processorName, timeoutMs, work)` (nach extractChatCompletionContent eingefuegt). Er legt einen harten Promise.race-Deadline um die GESAMTE Request-Operation (inkl. response.json()/response.text()), ruft beim Ablauf controller.abort() auf UND lehnt das Versprechen mit einer Timeout-Fehlermeldung ab, sodass der --watch-Loop garantiert nach timeoutMs weiterlaeuft, unabhaengig vom Runtime-Verhalten beim Body-Stream. Beide requestX-Funktionen wurden auf den Helper umgestellt (signal wird durchgereicht); dadurch entfaellt zudem die duplizierte setTimeout/clearTimeout-Plumbing. Kommentar auf Englisch (Dateikonvention), keine oeffentliche Signatur geaendert, keine neuen Abhaengigkeiten.

## personal_os/07_Automations/Workflows/raw-inbox-worker/src/worker.ts
- **[bug] Worker ignoriert das truncated-Flag von os_read_raw_file - >50 KB-Dateien werden stillschweigend gekuerzt verarbeitet**:250
  - In processEntry direkt nach der raw.body-Pruefung (jetzt Z.254-256) eine Pruefung 'if (raw.truncated === true) throw' ergaenzt. Der MCP-Server liefert nachweislich truncated:true und kappt den Body bei MAX_RAW_READ_CHARS=50000 (index.ts Z.594-595). Statt einen abgeschnittenen Draft stillschweigend vorzuschlagen, wirft der Worker nun einen Fehler, der ueber den bestehenden process_entry-Fehlerpfad in state.failed landet und nach maxAttempts als 'blocked' sichtbar wird. Datenverlust ist damit fuer den Reviewer sichtbar. Fix vollstaendig in worker.ts gehalten (Processor/Contract unveraendert, da nur diese Datei editiert werden darf). raw.truncated ist ueber die Index-Signatur von ToolPayload als unknown typisiert; '=== true' ist typsicher.
- **[bug] Run-Log-relativePath ist hartkodiert und ignoriert konfigurierbares runLogDir**:169
  - writeRunLog erhaelt jetzt einen zusaetzlichen osRoot-Parameter; das relativePath wird via path.relative(osRoot, absolutePath) berechnet und mit .split(path.sep).join('/') auf POSIX-Trenner normalisiert (konsistent zu den uebrigen OS-relativen Pfaden), statt fest auf '07_Automations/Runs/...' zu stehen. Alle 5 Aufrufstellen (Z.317, 342, 368, 399, 456) auf writeRunLog(options.osRoot, options.runLogDir, baseResult) umgestellt. options.osRoot ist bereits Teil von WorkerOptions und wird in index.ts gesetzt. Bei Default-runLogDir bleibt das Ergebnis exakt '07_Automations/Runs/...'; bei abweichendem --run-log-dir zeigt runLog nun auf den tatsaechlichen Pfad. writeRunLog ist projektweit nur in dieser Datei verwendet, keine oeffentliche Signatur gebrochen.

## personal_os/11_Integrations/MCP/os-mcp-server/src/index.ts
- **[improvement] Kein os_uncheck_task-Tool - per MCP gesetzte Tasks lassen sich nicht wieder oeffnen**:625
  - Neues Tool os_uncheck_task direkt nach os_check_task registriert (jetzt ~Z.684). Es spiegelt os_check_task exakt, ruft aber OS.AST.uncheckTask(file.ast, taskString) auf und schreibt ueber OS.write() mit requireApproval (Default true). Verifiziert: uncheckTask wird vom SDK exportiert (os-sdk/src/ast.ts Z.34, ueber OS.AST in os-sdk/src/index.ts Z.4/41) und serializeMarkdown (markdown.ts Z.52-55) serialisiert aus file.ast, daher wirkt die AST-Mutation beim Schreiben tatsaechlich. Gleiche inputSchema-Felder, gleiches Antwortformat (changed/path/...result).
- **[performance] draftHistory liest die komplette events.jsonl bei jedem Aufruf in den Speicher**:369
  - draftHistory liest events.jsonl nicht mehr per readFile komplett als einen String plus raw.split(...)-Array, sondern streamt zeilenweise via createReadStream + readline.createInterface (crlfDelay: Infinity) in einer for-await-Schleife. Neue Imports: createReadStream aus node:fs, createInterface aus node:readline (beide Node-Builtins, keine neue Abhaengigkeit). Die missingLog-Behandlung erfolgt jetzt vorab per stat() (statt try/catch um readFile). Semantik unveraendert: alle Zeilen werden gescannt, alle Treffer gezaehlt/sortiert, 1-basierte Zeilennummern in errors bleiben korrekt (manueller lineNumber-Zaehler). Peak-Memory ist nun durch die Treffermenge plus eine Zeile begrenzt statt durch das gesamte Log. Kein bounded Tail-Window gewaehlt, da das die Semantik (total + vollstaendige History eines Drafts) brechen wuerde.
- **[bug] truncated-Flag liefert False Positives bei exakt erreichtem Limit**:798
  - Sentinel-Ansatz (ein Eintrag mehr als das Limit sammeln) in allen drei vom Befund genannten Stellen umgesetzt: (1) listDirectory walkt jetzt bis maxEntries+1 (collectLimit); der os_list_directory-Handler setzt truncated = collected.length > maxEntries und schneidet bei Bedarf auf maxEntries. (2) listDrafts bricht erst bei maxDrafts+1 ab, berechnet truncated = drafts.length > maxDrafts, gibt visibleDrafts (auf maxDrafts gekuerzt) zurueck; counts werden konsistent ueber visibleDrafts gebildet. (3) graphIndex setzt ein truncated-Flag erst, wenn ein zusaetzliches gueltiges (nicht-Smoke) File ueber maxFiles hinaus gefunden wurde, ohne es in parsedFiles/nodes/edges aufzunehmen. Dadurch ist truncated nur noch true, wenn wirklich mehr Daten vorhanden waren. queryByTag/os_query_index wurde bewusst nicht angefasst (nicht im Befundumfang; minimaler Diff).

## requirements.txt
- **[bug] Fehlendes Paket: PyYAML (Import "yaml") nicht in requirements.txt**:67
  - Bestätigt: 'import yaml' wird in backend/plugin_manager.py:746 (YAML-Plugins) und backend/hermes_adapter.py:701 (Hermes-Config) genutzt, jeweils mit try/except gegen ImportError, war aber nicht in requirements.txt deklariert. Die im Projekt-venv installierte Version per 'pip show'/Import ermittelt (6.0.3) und 'PyYAML==6.0.3' in der Sektion '# Utilities' nach python-dotenv ergänzt.
- **[bug] Fehlendes Paket: dateparser nicht in requirements.txt**:68
  - Bestätigt: 'import dateparser' wird in backend/reminders.py:100 (natürlichsprachliche Zeitangaben) und companion/calendar_integration.py:127 genutzt, mit try/except-Fallback, war aber nicht in requirements.txt deklariert. Version im Projekt-venv ermittelt (1.3.0) und 'dateparser==1.3.0' in der Sektion '# Utilities' nach PyYAML ergänzt.

## tests/test_ai_engine.py
- **[bug] test_set_ai_model_accepts_provider_prefixed_ids schlaegt fehl: openai-Modell unbekannt**:1776
  - Verifiziert am echten Code: AI_MODEL_REGISTRY ist Gemini-only (keys nur gemini:gemini-3.5-flash/3.1-flash-lite/3.1-pro). Test auf 'gemini:gemini-3.1-pro' umgestellt; erwartet jetzt 'Gemini' im Ergebnis, current=='gemini:gemini-3.1-pro', provider=='gemini'.
- **[bug] test_set_ai_model_accepts_legacy_groq_ids schlaegt fehl: Groq-Modell nicht gesetzt**:1789
  - Groq existiert nicht mehr im Registry. Test in test_set_ai_model_accepts_legacy_raw_ids umbenannt und auf die noch funktionierende Legacy-Raw-ID-Aufloesung (_normalize_model_id) umgestellt: set_ai_model('gemini-3.1-flash-lite') ergibt current=='gemini:gemini-3.1-flash-lite'. Damit bleibt die Legacy-ID-Logik weiter getestet.
- **[bug] test_set_ai_model_accepts_anthropic_ids schlaegt fehl: Anthropic-Modell unbekannt**:1801
  - Anthropic ist nicht mehr registriert; set_ai_model('anthropic:...') liefert 'Unbekanntes Modell'. Test in test_set_ai_model_rejects_unknown_provider_ids umbenannt: prueft jetzt korrekt, dass eine unbekannte Provider-ID abgelehnt wird ('Unbekanntes Modell' im Ergebnis) und die aktive Auswahl unveraendert bleibt.
- **[bug] test_get_ai_models_returns_grouped_provider_data schlaegt fehl: kein openai-Block**:1814
  - grouped enthaelt nur 'gemini' (verifiziert: _PROVIDER_LABELS == {'gemini': 'Gemini'}). Assertions auf openai/anthropic entfernt; Test prueft jetzt 'gemini' in grouped und nicht-leere gemini-Modelle.
- **[bug] test_get_ai_status_reports_all_providers schlaegt fehl: KeyError 'groq'**:1874
  - get_ai_status liefert nur noch 'gemini' (plus active/selected/fallback-Meta), kein groq/openai/anthropic. Test in test_get_ai_status_reports_gemini_provider umbenannt: setzt valides 'gemini:gemini-3.5-flash', patcht _get_gemini_client, prueft gemini.available is True sowie das Fehlen der Alt-Provider-Schluessel und die selected/active/fallback-Felder.
- **[bug] test_chat_fallback_tries_configured_provider_after_selected_failure schlaegt fehl: KeyError im Registry**:1907
  - selected auf 'gemini:gemini-3.1-pro' geaendert (gueltiger Key); _iter_provider_fallback_models liefert dafuer 'gemini:gemini-3.5-flash' als Fallback. fake_chat liefert nun bei id=='gemini:gemini-3.5-flash', _provider_available_for_fallback patcht auf 'gemini'. Erwartetes calls==['gemini:gemini-3.5-flash']. Fallback-Logik bleibt voll getestet.
- **[bug] test_stream_fallback_returns_first_configured_stream schlaegt fehl: KeyError im Registry**:1931
  - Analog zum Chat-Fallback: selected='gemini:gemini-3.1-pro', fake_stream liefert bei 'gemini:gemini-3.5-flash', _provider_available_for_fallback patcht auf 'gemini'. Erwartetes meta['id']=='gemini:gemini-3.5-flash'.
- **[bug] test_chat_stream_falls_back_after_empty_primary_stream schlaegt fehl: KeyError im Registry**:1953
  - Beide Registry-Lookups auf gueltige Keys umgestellt: selected='gemini:gemini-3.5-flash', fallback='gemini:gemini-3.1-pro' (fallback ist hier nur das von der gepatchten _stream_with_provider_fallbacks zurueckgegebene Meta). Erwartete calls-Tupel auf 'gemini:gemini-3.5-flash' aktualisiert.
- **[bug] test_chat_stream_accepts_tool_delta_without_function_payload schlaegt fehl: KeyError im Registry**:2004
  - selected-Lookup von 'groq:llama-3.3-70b-versatile' auf 'gemini:gemini-3.5-flash' geaendert. Die Tool-Delta-Logik ist providerunabhaengig (_stream_with_selected_provider ist gepatcht), daher bleibt die Testabdeckung identisch.

## voice/conversation.py
- **[bug] _is_exit triggert Falsch-Beendigung durch Substring-Matching**:48
  - Das fehleranfaellige `any(p in clean for p in EXIT_PHRASES)` entfernt. Jetzt: exakter Treffer der ganzen Aeusserung (clean in EXIT_PHRASES) ODER fuer NUR mehrwoertige Phrasen ("bis spaeter", "das wars") wortgrenzen-basiertes re.search(\b...\b). Kurze Einzelwoerter wie "stop"/"halt"/"ende"/"bye" zaehlen nur noch als ganze Aeusserung, sodass "Halterung", "Endergebnis" usw. die Konversation nicht mehr faelschlich beenden.
- **[quality] Toter Code: _tts_executor und _collect_tts_results nie verwendet**:34
  - Statt Loeschen verdrahtet (Instruktion bevorzugt Anbindung verwaister Feature-Funktionen): In run_single_turn wird nun pro abgeschlossenem Satz _tts_executor.submit(_tts_for_text, sent) aufgerufen und die Ergebnisse via _collect_tts_results(tts_futures) eingesammelt. Beide Symbole sind jetzt live; der Threadpool laeuft nicht mehr im Leerlauf. Projektweiter Grep bestaetigte vorab, dass es ausser Definition keine Nutzung gab.
- **[performance] Streaming-TTS-Pipeline fehlt: nur eine TTS-Datei fuer Gesamttext**:283
  - Pro fertiggestelltem Satz wird die TTS-Synthese parallel via _tts_executor.submit gestartet (statt erst _tts_for_text(full_text) nach Stream-Ende). Der Rest-Puffer wird ebenfalls submittet. audio_paths werden in Stream-Reihenfolge via _collect_tts_results gesammelt, sodass das erste Audiostueck frueher bereitsteht. Bei Tool-Call und Text-Tool-Fallback werden ausstehende Futures via fut.cancel() verworfen, damit keine verwaisten Saetze synthetisiert/abgespielt werden.
- **[bug] Barge-in nutzt self._noise_floor statt lokal kalibrierter noise_floor**:421
  - Nach calibrate_noise_floor in run_conversation wird `self._noise_floor = noise_floor` gesetzt (nur wenn truthy). play_files_with_bargein (das self._noise_floor fuer adaptive_threshold = max(BARGEIN_THRESHOLD, noise_floor*4) nutzt) erhaelt damit denselben frisch kalibrierten Wert wie _record_utterance. Signatur von play_files_with_bargein bewusst nicht geaendert (keine API-Aenderung noetig).
- **[bug] Synthetischer Fallback-Tool-Call extrahiert Argumente fragil**:196
  - Neuer Helfer _extract_arg_phrase(words, start) begrenzt die heuristische Argument-Extraktion auf das sinnvolle Token-Fenster: stoppt an Konjunktionen (und/dann/danach/sowie/oder/bitte) und an Satzzeichen (,.;:!?). Sowohl die city- als auch die name-Extraktion nutzen ihn jetzt, statt " ".join(words[i+1:]) bis Satzende. "Oeffne Word und schreibe einen Brief" liefert nun name="Word" statt des ganzen Reststrings.
- **[quality] Leeres STT-Ergebnis nach Turn beendet Konversation kommentarlos**:430
  - Bei leerem Turn-Transkript wird einmalig (MAX_EMPTY_RETRIES=1) eine deutsche Rueckfrage ausgegeben ("Entschuldigung, das habe ich nicht verstanden. Kannst du das wiederholen?") per push_response_chunk + _tts_for_text + play_files_with_bargein und anschliessend derselbe Turn erneut aufgenommen, statt stillschweigend zu enden. empty_retries wird bei erfolgreichem Kommando zurueckgesetzt; nach Ueberschreiten des Limits faellt der Code regulaer in das bestehende Konversationsende. Import von push_response_chunk in run_conversation ergaenzt.
- **[bug] _clean_for_tts wandelt jede mit '-' beginnende Zeile in '. '**:67
  - Listenmarker-Regex auf `^\s*[-*]\s+(?=[A-Za-zAEOEUEaeoeuess])` erweitert (Lookahead auf folgenden Buchstaben). Zahlen-/Vorzeichen-Zeilen wie "-5 Grad" oder "- 20 % Rabatt" behalten ihren Inhalt, nur echte Aufzaehlungspunkte (Wort folgt) werden zu ". ".
- **[quality] _turn_legacy gibt inkonsistent audio_path + audio_paths zurueck**:360
  - _turn_legacy gibt nur noch einheitlich audio_paths zurueck (separater audio_path-Key entfernt). In run_conversation wurde der nun obsolete Fallback `if not audio_paths and result.get('audio_path')` entfernt. Damit ist die Rueckgabeform konsistent mit run_single_turn.
- **[improvement] Historie auf 20 gedeckelt, schneidet mitten im User/Assistant-Paar**:230
  - deque(maxlen=20) durch unbegrenzte deque + self._history_max=20 ersetzt. Neuer Helfer _append_turn(user, assistant) haengt das Paar an und trimmt paarweise von vorn (popleft; falls dann ein 'assistant' am Kopf steht, nochmal popleft), sodass die Historie nie mit einer dangling assistant-Nachricht beginnt oder mit einem orphaned user endet. Die Barge-in-Mutation self._history[-1] ("[unterbrochen]") bleibt funktionsfaehig.

## voice/playback.py
- **[performance] Barge-in-Aufnahme erzeugt pro 30ms-Chunk einen neuen sd.rec-Stream — Luecken und Overhead**:95
  - play_files_with_bargein oeffnet jetzt einen einzigen persistenten sd.InputStream fuer den gesamten Aufruf (analog zu _record_utterance in conversation.py) und liest in der Monitoring-Schleife mit stream.read(chunk_samples) statt pro Chunk sd.rec()+sd.wait() aufzurufen. Damit entfaellt die Geraete-Setup-Latenz pro 30ms-Iteration und die Audio-Luecken zwischen den rec-Aufrufen, die die ersten Silben des Barge-ins verschluckt haben. _record_rest bekommt denselben offenen stream uebergeben (Signatur von (sd, ...) auf (stream, ...) geaendert — rein interne Methode, einziger Aufrufer ist play_files_with_bargein) und liest ebenfalls ueber stream.read, sodass auch die Restaufnahme luecken- und overheadfrei direkt anschliesst.
- **[bug] Barge-in-Schleife endet faelschlich, sobald MCI kurzzeitig nicht 'playing' meldet**:34
  - Neue Helfer _mci_mode() (liefert den rohen MCI-Mode-String) und Methode _wait_until_playing(is_active, timeout_s=1.0). Nach play_file wird jetzt bis zu 1s gepollt, bis MCI 'playing' bestaetigt, bevor die Monitoring-Schleife startet — so wird die Schleife nicht mehr sofort nach dem Start beendet, weil is_playing direkt nach dem 'play'-Kommando noch False liefert. Bei Mode 'stopped' (sehr kurzer Clip bereits fertig) oder Timeout/abgebrochen wird die Datei uebersprungen (self.stop(); continue). Bei fehlendem winmm liefert _wait_until_playing False; das ist verhaltensgleich zum alten Code, in dem die Schleife mangels is_playing ebenfalls nie lief (keine Regression).
- **[bug] Ungenutzter Parameter timeout_s in _record_rest — Timeout greift nicht**:141
  - timeout_s wird jetzt tatsaechlich umgesetzt: timeout_chunks = int(timeout_s * sr / chunk_samples) und ein Flag speech_seen. Wird innerhalb der ersten timeout_s ab Beginn der Restaufnahme keine ueber-Schwelle-Sprache erkannt (silence_threshold = adaptive_threshold*0.5 bzw. BARGEIN_THRESHOLD*0.5), bricht die Schleife frueh ab, statt bis zu max_s=15s (near-)Stille aufzunehmen. Sobald echte Sprache erkannt wurde, greift wie bisher die 800ms-Stille-Erkennung bzw. der max_s-Cap. silence_threshold wurde aus der Schleife herausgezogen (einmalige Berechnung).

## voice/stt.py
- **[performance] _get_key fragt Keyring bei fehlendem Key bei jedem Aufruf erneut ab**:68
  - In _get_key wird _keys_loaded[provider] jetzt nach jedem Ladeversuch unbedingt auf True gesetzt (vorher nur bei gefundenem Key), und die Cache-Guard prueft nur noch das Lade-Flag (if _keys_loaded[provider]: return _keys[provider]) statt zusaetzlich auf is not None. Dadurch wird fuer nicht konfigurierte Provider (z.B. deepgram/groq, wenn nur openai gesetzt ist) keyring.get_password nicht mehr blockierend bei jeder Transkription erneut aufgerufen. Deepgram-Keys, die spaeter ueber set_deepgram_key/delete_deepgram_key gesetzt/geloescht werden, aktualisieren den Cache weiterhin korrekt; groq/openai werden ohnehin nur extern via keyring konfiguriert. Konsistent mit dem identischen Muster in voice/tts.py. Docstring entsprechend ergaenzt.
- **[improvement] Deepgram-Keyterm nur gesetzt, wenn 'Lexa' im Prompt — bei normaler Transkription kein Keyword-Boosting**:480
  - Neue Helper-Funktion _deepgram_keyterm(prompt) und Modul-Konstante _DEEPGRAM_KEYTERM (Default 'Lexa', ueber Env LEXA_STT_KEYTERM komma-separiert konfigurierbar — kein neuer Abhaengigkeits- oder Signaturbruch, konsistent mit dem Env-Muster in voice/config.py). In _cloud_transcribe wird der Deepgram-Lambda jetzt keywords=_deepgram_keyterm(prompt) uebergeben statt 'Lexa' nur bei 'Lexa' im Prompt. Dadurch wird der Assistentenname auch im normalen Gespraechs-Turn (transcribe_audio_data ohne Prompt) als Keyterm geboostet; distinkte Prompt-Tokens werden zusaetzlich gemerged. _cloud_transcribe_file bleibt unveraendert (Datei-Pfad ohne Prompt).

## voice/tts.py
- **[bug] Cache-Pruning-Race: _prune_cache kann gerade in Arbeit befindliche Chunk-Dateien anderer Threads loeschen**:516
  - In _speak_multi werden die temporaeren Chunk-Dateien jetzt in das Unterverzeichnis AUDIO_CACHE_DIR/chunks/ geschrieben (mit Fallback auf AUDIO_CACHE_DIR, falls mkdir scheitert). _cache_files() nutzt das nicht-rekursive glob('tts_*.*') mit is_file()-Filter, scannt das Unterverzeichnis also nicht. Dadurch koennen _prune_cache() (Zeile 516) und clear_cache() die in Arbeit befindlichen Chunks paralleler Synthese-Threads nicht mehr loeschen. Die Cleanup-Logik in _speak_multi bleibt korrekt, da sie die vollstaendigen Chunk-Pfade verwendet.
- **[performance] _get_key fragt Keyring bei jedem Aufruf erneut ab, wenn kein Key gesetzt ist (blockierendes I/O)**:51
  - _get_key setzt _keys_loaded[provider] jetzt unabhaengig vom Ergebnis auf True (vorher nur wenn Key != None) und prueft am Anfang nur noch 'if _keys_loaded[provider]:'. Ein fehlender Key wird damit gemerkt und keyring.get_password wird nicht mehr bei jedem _provider_available-Aufruf in der Fallbackkette erneut blockierend ausgefuehrt. Identisch zum bereits gefixten Muster in voice/stt.py:61-73. set_*_key aktualisiert den Cache weiterhin, falls der Key spaeter gesetzt wird.
- **[bug] except (ImportError, Exception) verschluckt ALLE Fehler stillschweigend bei MP3-Konkatenation**:454
  - Der redundante Sammel-Except '(ImportError, Exception)' mit leerem pass wurde aufgeteilt: 'except ImportError' loggt auf info-Level (pydub fehlt, normaler Fallback), 'except Exception as e' loggt jetzt auf warning-Level mit Fehlermeldung. Export-Fehler (z.B. ffmpeg fehlt, korrupter Chunk) sind damit sichtbar, bevor auf die reine Binaer-Konkatenation zurueckgefallen wird. Der Binaer-Fallback selbst wurde nicht geaendert (keine neuen Abhaengigkeiten/Risiko).
- **[bug] Cache-Hash beruecksichtigt die Sprache nicht — Sprachwechsel liefert falsches Cache-Audio**:156
  - Neue Modul-Variable _tts_language ('de') eingefuehrt und in _provider_cache_signature fuer alle Provider (openai, elevenlabs, cartesia, sapi) aufgenommen. Cartesia nutzt nun _tts_language statt des hartkodierten 'de'. Damit invalidiert der Cache bei einer kuenftigen Sprachumschaltung korrekt. Aktuell rein in tts.py gehalten (keine config.py-Aenderung), da ausschliesslich diese Datei bearbeitet werden darf.
- **[bug] SAPI-Pfad-Umschreibung kann Schreibpfad und erwarteten output_path divergieren lassen**:277
  - In _speak_with_provider wird bei explizitem .mp3-output_path fuer SAPI jetzt zunaechst eine WAV erzeugt und anschliessend per neuer Hilfsfunktion _transcode_wav_to_mp3 (nutzt das bereits im Projekt vorhandene pydub) nach exakt dem erwarteten .mp3-Pfad transkodiert; die WAV wird dabei aufgeraeumt. Ohne pydub wird ein RuntimeError geworfen, sodass die Fallbackkette SAPI sauber ueberspringt, statt einen divergenten .wav-Pfad zurueckzugeben (kein falsch deklarierter WAV-in-mp3-Inhalt). Der normale Cache-Pfad (output_path leer) bleibt .wav und nutzt den unveraenderten Direkt-Pfad — keine Transkodierung, keine neue Abhaengigkeit.

## voice/wakeword.py
- **[bug] _get_conversation_engine erzeugt bei jedem Aufruf eine neue Engine — verworfene Konversationshistorie**:160
  - _get_conversation_engine cached jetzt die ConversationEngine: sie wird nur erzeugt, wenn self._conversation None ist (if self._conversation is None: create), und ansonsten wiederverwendet. Dadurch bleibt _history ueber die zwei Aufrufe innerhalb eines Wake-Zyklus (Kommando-Aufnahme + run_conversation) sowie ueber Folge-Turns konsistent. Zusaetzlich wird self._conversation in stop() auf None gesetzt, damit jede neue Listening-Session frisch startet und keine veraltete Engine bestehen bleibt. Die Callbacks (_on_volume/_on_conversation_state) werden in router_voice.py einmalig nach Konstruktion gesetzt und aendern sich nicht, daher ist das Caching sicher.
- **[bug] Wake-Cooldown verwirft inline mitgesprochenes Kommando bei schneller Folgeaktivierung**:256
  - Die inline_command-Extraktion (detection.command bzw. _command_after_wake_phrase) wurde vor den Cooldown-Check verschoben. Der Cooldown-Check greift jetzt nur noch, wenn KEIN inline_command vorliegt (if not inline_command and now - self._last_wake < WAKE_COOLDOWN_S: continue). Ein bereits erkanntes mitgesprochenes Kommando ('Lexa stopp') wird damit auch innerhalb des 2s-Cooldown-Fensters verarbeitet statt verworfen; reine Re-Triggerung ohne Kommando wird weiterhin unterdrueckt. Erklaerender Kommentar ergaenzt.

## voice/wakeword_engines.py
- **[bug] openWakeWord: missing-Pfad-Pruefung bricht vor Auto-Download ab**:204
  - Die Pruefung der lokalen Modell-Dateipfade (missing = [...] if not Path(path).exists()) wurde in OpenWakeWordEngine._load aus der Position VOR dem try/Auto-Download-Block heraus und HINTER den download_models(...)-Aufruf verschoben. Damit laeuft der Auto-Download fuer benannte Modelle (model_names) zuerst, bevor lokale ONNX/TFLite-Pfade auf Existenz geprueft werden. Ein erklaerender Kommentar wurde ergaenzt, der klarstellt, dass download_models nur benannte Modelle bereitstellt und lokale Dateipfade nicht heruntergeladen werden koennen. Verhalten bei rein benannten Modellen bleibt unveraendert; Mischfaelle (Pfad + Name) brechen nicht mehr vorzeitig vor dem Download ab.
- **[bug] openWakeWord: threshold-Map wird bei patience<=1 nicht an predict uebergeben**:239
  - In OpenWakeWordEngine.detect wird die konfigurierte threshold-Map jetzt IMMER an self._model.predict uebergeben. Der else-Zweig ruft nun predict(pcm16, threshold=threshold) statt predict(pcm16) auf. patience wird wie zuvor nur zusaetzlich uebergeben, wenn das patience-Dict nicht leer ist (also self.patience > 1). Dadurch ist die Schwellwertbehandlung (WAKE_OPENWAKEWORD_THRESHOLD) unabhaengig vom patience-Wert konsistent. Ein erklaerender Kommentar wurde ergaenzt.

